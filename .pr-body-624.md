## PR 摘要

记录 Dev 验收中 savepoint 高水位与重新 seed 事件不匹配的真实根因、失败证据和已合并修复。

### 解决了哪些问题

补充 Dev 验收台账，避免使用固定 event_id 和低于 savepoint 的 revision 重跑导致误判失败。

### 使用了什么方式

记录诊断结果：Flink/Redis 高水位、relay delivery ledger 残留和 revision fence；记录 PR #622 的 nonce 与起始 revision 隔离修复。

### 数据与安全边界

仅记录合成 Dev 任务元数据和安全摘要，不使用生产或 Staging 数据，不修改任何业务数据，不删除 checkpoint、ledger、卷或失败证据。

### 建议怎么验收

使用新的验收编号、DEV_ACCEPT_NONCE 和高于 savepoint 的 DEV_ACCEPT_BASE_REVISION 重跑 seed/verify，并核对 MySQL、Redis、Kafka 与 Flink revision 一致。

### 测试与验证结果

主线 CI 34739128268 的 Backend、Frontend、Desktop clients 均成功；文档 git diff --check 通过。

### 风险、回滚与监控

保留旧失败目录；若新重跑仍出现真实组件不收敛则停止并记录新证据。文档变更可通过 Git 回退，不涉及运行时回滚。

### 后续发展方向

继续完成第 9 项及第 6–11 项 Dev 验收，再评估双轨比对和 Staging 晋级。

### 暂时无法解决的问题

本 PR 不签署完整业务双轨、Staging 脱敏副本或生产架构切换。

### 兼容性与发布信息

仅文档变更；不修改 VERSION，不发布 Production，不影响 Dev 运行资源。
