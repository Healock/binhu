# AGENTS.md — 滨湖智慧平台开发规则

这份文件同时写给项目管理人和 Agent 看。目标很简单：换一台电脑或换一个 Agent，也能安全地继续开发。

## Agent 开始工作前

1. 先读本文件。
2. 根据任务再读对应文档：
   - 想了解系统怎么工作：读 [架构说明](docs/architecture.md)。
   - 要开发功能或修问题：先读 [高效开发与验证流程](docs/development-workflow.md)。
   - 要换电脑或部署：读 [开发与运维手册](docs/operations.md)。
   - 要碰服务器、安全设置或数据库：先读 [风险清单](docs/known-risks.md)。
3. 文档和代码说法不同时，以实际代码和检查结果为准，并把文档改正确。
4. `docs/archive/` 是历史记录，只能参考，不能照着执行。
5. `AGENTS.local.md` 可能包含本机运维信息。只有用户明确要求处理服务器、凭据或故障恢复时才能读取，不能提交或公开其中的内容。

## 这个项目是做什么的

滨湖智慧平台以本地 MySQL 业务表作为在线任务唯一主数据源，也支持管理员上传走访明细 XLSX。全民防和居住证平台仅作为外部只读来源；腾讯文档已完成迁移并正式下线，当前只保留受限的历史快照、备份和审计材料，正常业务不得读取、写回、删除或依赖其物理行号。数据按人员、社区和日期整理并显示在网页上，管理员还可以把系统统计和人工表单合并，导出社区警务工作日志 PDF。

## 本地数据源切换约束

- 生产环境必须显式使用 `LOCAL_DATA_SOURCE_ENABLED=true` 和 `TXDOCS_ENABLED=false`；`TXDOCS_MIGRATION_MODE=readonly` 只代表历史迁移兼容状态，不得开启腾讯业务访问。
- 新任务、批量导入、快捷下发、编辑、研判、登记、导出和归档必须写本地业务表及本地来源记录；不得新增腾讯来源字段、OAuth 凭据或物理行号主键。
- 本地来源使用 `spreadsheet_id=0`、`sheet_id=local:<parser_type>` 和本地业务表 `id` 作为兼容定位信息。版本并发控制使用本地 `revision` 与内容哈希。
- 腾讯同步、写回、删除行和调照片腾讯名单调度器在本地模式必须不启动；旧接口只能返回明确的“腾讯数据源已下线”状态，不得发起外部请求。
- `/api/query` 在本地模式直接查询本地来源投影；`/api/query/migration/status` 仅向超级管理员返回不含敏感值的迁移统计。
- 迁移脚本必须幂等、只读外部数据、保留问题清单和历史备份；未完成迁移校验前不得删除旧来源缓存或 OAuth 历史表。
- 生产切换曾分为迁移基础、业务流程切换、腾讯运行路径下线三个独立 PR（历史计划，当前已完成）；每阶段都要写明备份范围、回滚开关和真实 MySQL 验证结果。

## 帮助中心文档约束

- 用户帮助的版本基线统一放在 `backend/help_docs/*.md`，不得把长篇操作说明硬编码在前端组件、数据库迁移或公告中。每篇文档必须包含 `slug`、`title`、`category`、`summary`、`order` front matter，正文必须以一级标题开始。
- 新功能、页面入口、权限边界或操作步骤变化时，同一个 PR 必须更新对应帮助文档；提交前要确认所有内置 Markdown 可以完整解析，且内容与当前本地数据源、自动保存和权限规则一致。
- 所有登录用户可以读取帮助文档；只有超级管理员可以在线修改标题、摘要和 Markdown 正文。保存和恢复内置版本都必须使用 `revision` 乐观锁，禁止覆盖其他管理员刚完成的修改。
- 超级管理员在线修改后标记为自定义内容，后续版本只更新系统管理的分类、顺序和内置摘要，不覆盖自定义标题、摘要与正文；恢复内置版本时才替换为当前代码版本随附的 Markdown。
- 帮助文档不得包含身份证号、手机号、密码、令牌、Cookie、真实地址、真实人员资料或内部凭据。编辑审计只记录文档 slug、版本和字符长度，不记录正文。
- 帮助页面使用安全 Markdown 渲染，不允许 `dangerouslySetInnerHTML` 或执行文档内 HTML/脚本；外部链接必须使用新窗口并带 `noopener`。桌面和窄屏都要验证目录、正文、表格、代码块和编辑预览可读。

## 地址匹配与小区分配约束

- 地址匹配首版只使用本地规则生成候选和建议，不得自动修改任务原始地址、原始社区或核查结果；原始地址必须只读保留。
- 标准地址不保证遵循固定模板。街道、道路、门牌、楼栋、房号和小区名都是可选特征，提取不到只能降低置信度，不能把记录判为格式错误。
- 唯一、有效、无街道或社区冲突的 `suggested` 小区结果统一显示为“自动匹配”，可直接用于单人分配或平均分配；不得把它伪装成人工确认。`ambiguous`、`unmatched`、`conflict`、`invalid`、停用小区和来源异常任务仍不得进入分配。
- 禁止恢复“地址无法匹配后按稳定顺序平均分配”的旧兜底；无法确认社区和小区归属的任务必须留在人工处理队列，并返回逐条跳过原因。
- 任何阻止用户继续操作的状态都必须同时提供可执行的解决路径。地址冲突、来源异常、权限不足、版本冲突或资料缺失不能只显示“禁止操作”；页面必须说明具体原因、下一步应检查或修改的对象、允许执行该处理的角色，以及处理完成后的重试/恢复入口。对于小区归属冲突，至少展示任务社区、候选小区、候选所属社区和冲突原因，并提供管理员修正社区映射、维护小区地址库或重新匹配的入口；普通用户应看到联系/转交对象和当前状态。
- 人工确认结果是最高优先级，规则重跑、批次重算和后续召回/精排都不得覆盖；小区停用或归属变化时只能转为复核状态。
- 候选必须先按“规范化小区名 + 正式社区”去重；去重后只有一个有效候选且不存在街道/社区冲突时直接作为 `suggested` 自动匹配，不能仅因分数低于旧阈值继续标成“多候选”。真正存在两个不同小区且分数接近时才进入 `ambiguous`。
- 人工确认会形成受控的精确地址反馈记忆：只对“同一规范化地址 + 同一正式社区”复用，反馈表只保存 HMAC、任务定位和小区 ID，不复制地址正文。相同键出现不同确认结果时必须标记冲突并立即停止自动复用；不得把一次人工确认模糊推广到相似地址，也不得声称这是在线深度学习。
- 人工反馈事件可以作为以后离线评估 MGeo 或其他模型的标签来源，但模型训练、阈值调整和上线必须另行脱敏评估；当前请求链路不得边使用边训练。
- Elasticsearch 和 MGeo 只能作为以后可插拔的候选召回与精排模块。模型分数不得绕过街道/社区硬约束，也不得直接触发分配或覆盖人工确认。
- 房屋档案小区匹配维护固定使用 `measure → migrate --apply → verify`，重复执行必须幂等。在线任务规则升级使用 `python -m migrations.address_match_feedback measure`、`migrate --apply`、`verify`，不得覆盖人工确认。生产执行前备份 `RegistryData`；涉及任务投影结构或反馈记忆发布时同时备份 `OnlineData`，真实 MySQL 未验证时必须在 PR 中明确说明。

## 指令核查编辑、研判与所内移交约束

- 普通指令核查详情使用自动保存：文本、地址和备注停止输入 700ms 后保存，下拉、日期、核查结果和核查人选择后立即保存；不得恢复常驻“保存”按钮。待登记结果和唯一拟登记房屋必须保持一次原子提交，结构化研判仍使用独立的阶段提交按钮。
- 自动保存必须以当前草稿为唯一显示状态，过期响应不得覆盖新输入；失败保留草稿并允许重试，只有 409 乐观锁冲突才重新读取详情。未分配组员首次保存必须复用自主领取确认，取消后不得循环弹窗或丢失草稿。
- 普通保存、领取、地址确认、批量分配和撤销分配只能增量重建实际变化的 `parser_type + row_key` 投影与任务图，禁止恢复整业务 `rebuild_projection()`；接口应返回更新后的任务快照供前端局部合并。
- 六类“无法核实”业务固定为全链条、出租房屋核查、寄递业、疑似返苏、苏州涉警和交通涉警，疑似未注销模型三不得进入研判。缺少流程的当前任务维护固定使用 `measure → migrate --apply → verify`，只把当前有效 canonical 本地来源计入唯一性判断。
- “移交（所内）”只能由基础管控及更高管理岗位处理；目标社区只有一名在岗组长时可自动选择，多名时必须明确选择，无在岗组长时必须阻止。社区与接手组长必须在同一事务中更新并记录不含人员正文的移交事件。
- 每个 `parser_type + row_key` 的第一社区和第一核查人只能在二者首次同时非空时固化，后续移交不得覆盖。当前任务归属和待办使用接手组长，日报、在线汇总和个人贡献只归第一核查人；历史证据不足时进入问题清单，不得用当前核查人猜测。
- 上述研判接管和第一核查人维护在生产执行前必须备份 `OnlineData` 和 `daily_report`，并严格按 `measure → migrate --apply → verify` 执行。本机没有真实 MySQL 时，PR 必须明确真实结构、驱动和维护命令未验证。

### 当前生产事实：腾讯表已正式下线（2026-08-29）

- 腾讯文档已经完成只读迁移和核对，当前生产环境不再把腾讯表作为业务数据源；本地 MySQL 业务表、本地来源记录和本地归档是唯一业务主数据源。
- 正常业务路径禁止访问腾讯文档 API，禁止 OAuth 登录、读取腾讯表、写回核查结果、删除腾讯行、等待腾讯回读或依赖腾讯物理行号。新功能、修复和重构都不得重新引入这些调用。
- 五类业务导入、快捷下发、编辑、分配、研判、待登记房屋关联、居住证自动确认、任务队列、导出和归档全部以本地事务为准；本地版本号、内容哈希和本地任务 ID 替代腾讯 `revision`、`row_hash` 和物理行号。
- 全民防和居住证平台只保留外部只读查询。全民防真实登记、模型三结果写入、照片上传、人员保存和完成反馈入口均已停用；不得恢复准备、执行、重试或后台写入链路。“只读”不等于可以把其数据写入腾讯表，也不允许把外部物理编号当作本地业务主键。
- `/api/query` 查询本地在线数据和本地归档；旧 `/api/sync/*`、`/api/spreadsheets/*` 及腾讯写回接口只能返回“腾讯数据源已下线”或迁移状态，不得发起外部请求。前端不得再提供腾讯配置、OAuth、同步、写回、删除行或腾讯额度页面。
- `TXDOCS_ENABLED=false` 必须保持；`TXDOCS_MIGRATION_MODE=readonly`、腾讯快照、旧 `_online_source_*` / `_online_writeback_*` 表和迁移脚本仅作为审计、回滚和历史只读材料保留，不得参与正常业务流程，也不得被新代码依赖。
- 代码审查发现腾讯相关调用时，先确认是否属于迁移脚本、历史只读备份或明确的兼容接口；除此之外应拒绝合并，并补充“本地数据源已是唯一业务数据源”的测试和说明。

### 迁移期间规则（历史兼容说明）

以下规则仅用于理解迁移历史和维护回滚材料，不代表当前生产可以继续使用腾讯表：阶段一完成前，腾讯表只能执行一次维护窗口内的只读快照；阶段一代码允许保留 `_online_source_rows`、`_online_writeback_*` 和 OAuth 历史结构作为迁移兼容与回滚材料；阶段二切换后，导入、下发、编辑、研判、登记、导出归档和任务队列全部以本地事务为准；阶段三完成后删除正常运行路径、页面和依赖。当前开发应遵循上面的“腾讯表已正式下线”规则。

本文件后文若仍出现“腾讯来源行、腾讯工作表、腾讯回写、腾讯删除、腾讯同步、腾讯原始行”或“全民防真实登记”等表述，均是旧版本行为的审计说明或兼容数据结构说明，不是当前实现要求。副开发不得根据这些历史文字恢复外部写入；遇到冲突时，以“当前生产事实：腾讯表已正式下线”、全民防/居住证只读边界及实际代码、接口和测试结果为准。

主要技术：FastAPI、MySQL 8、React 18、Ant Design 6、Tailwind CSS 4、Vite 6、Docker Compose。

## 前端排版和间距

## 无法核实两级研判与归档

- 适用业务固定为全链条、出租房屋核查、寄递业、疑似返苏、苏州涉警和交通涉警；疑似未注销模型三不进入该流程。
- “无法核实”必须经过初步研判和深度研判；成功分别延时 2 个、1 个完整自然日，截止日期由系统计算并保留原值，禁止人工填写延时日期。
- 研判决定必须使用结构化成功/失败接口并填写意见；普通任务编辑接口不得绕过阶段、版本、行哈希和权限校验。
- 初步待研判和深度待研判任务允许按页面当前筛选、排序导出统一 XLSX，并将同格式文件回导；回导必须逐行复用结构化研判接口并校验来源 ID、来源版本、行哈希、流程版本和当前阶段，不能把导入当成新建任务或批量覆盖。
- 延时到期由后台幂等推进器处理，未填写反馈只能记录逾期自动流转，不能伪造网格员反馈；来源删除、重复、版本/哈希/社区变化时必须暂停并标记来源异常。
- 只有进入最终无法核实的当前业务数据可预览、导出和归档；导出、平台归档和流程标记必须在本地事务及本地任务队列中完成，任一失败都保留冲突和当前任务。腾讯整行删除已永久停用，不得作为导出成功条件。
- 上一条所说的“导出”是最终归档导出；待研判工作簿仅用于基础管控离线填写和结构化回导，不触发归档、删除或任务状态跳转。
- 研判正文和反馈正文只能保存在受保护流程数据中，普通日志/审计只记录阶段、动作、业务类型和安全原因码。

### 多分辨率与高密度表格

- 流口业务列表和辖区档案的“导出当前结果”必须由服务端复用页面同一组筛选和排序规则查询完整结果；不得只导出浏览器当前分页，也不得在导出端另写一套顺序。页面第一行与导出第一条业务数据必须一致。

- 桌面客户端必须支持 1024×640 以上的窗口；不得把根节点、桌面窗口或页面内容的最小宽度写死为 1280px。
- 响应式判断使用实际页面容器宽度和视口高度，统一通过 `useResponsiveLayout` 分为 `compact`、`standard`、`wide`；不要在业务页面自行读取固定屏幕分辨率。
- 新增业务表格优先使用 `AppTable`。列应声明 `responsivePriority`（`always`、`standard` 或 `wide`）；操作列必须使用 `responsiveAction` 或明确的 `action/actions` key，并固定在右侧。
- 紧凑模式优先保证姓名、状态、主要结果和操作按钮可见；普通数据表的低优先级列进入展开详情，编辑型表格不得隐藏正在编辑的字段。
- 表格必须设置明确列宽和可控横向滚动，禁止依赖全局 `transform: scale()`、无限内容撑宽或让操作列随普通列滚出视口。
- 高密度列表按“标题—筛选/工具栏—表格滚动区—分页/批量操作”组织，表格高度由可用容器空间计算；页面、表格和抽屉不得无意义地叠加多层纵向滚动。
- 响应式验收至少覆盖 1280×720、1280×960、1680×1050、1920×1080（含 Windows 125% 缩放），并检查浅色、深色、长地址、多操作按钮和侧边栏展开/收缩。

- 新页面或较大的功能区必须明确区分页面、区块、字段和操作区四个层级，不能只把组件按源码顺序紧邻摆放。
- 同级组件优先由父容器使用 `gap` 管理间距，不要依赖相邻元素零散叠加 `mt-*`、`mb-*`。页面和主要区块间距通常为 16–24px，标题与内容、控件与辅助说明通常为 8–12px。
- 带边框或背景的连续业务区块（例如 `Alert`、`Panel`、`Card`、表格和空状态）禁止只依赖 Tailwind 的 `space-y-*` 或子项 `margin` 来分隔；必须使用页面语义容器的 `display: grid/flex` 与明确 `gap`。Ant Design 组件会重置外边距，逻辑外边距还可能发生折叠或被后置规则覆盖，导致截图中“下方有空隙、上方贴死”或桌面/手机表现不一致。新增这类区块时要为父容器命名并在桌面、窄屏分别检查实际渲染间距。
- 条件渲染的工具栏、提示、筛选区、表格和分页如果是连续业务区块，必须放进一个明确的 `grid`/`flex` 父容器并由父级 `gap` 分隔；禁止只给其中一个子项添加单向 `mb-*` 或 `mt-*`。`mb-*` 只能隔开后续内容，不能保证它与前一个条件区块之间存在间距，这正是多个页面出现“下方有空隙、上方却贴死”的常见原因。
- 列表页面的搜索、状态、社区、类型等筛选控件必须进入 `ListToolbar.filters`，状态摘要与业务操作进入对应的 `meta/actions`；不要在 `ListToolbar` 后面另接一个临时 `Space` 或 JSX Fragment 作为第二套筛选行。确需独立区域时使用有语义的页面专用容器，并明确设置桌面约 16px、手机约 12px 的父级 `gap`。
- `ListToolbar` 只负责工具栏内部排版，不提供与表格、卡片、空状态或分页之间的外部间距。列表结果区必须与工具栏共同放入 `ListContent` 或具备同等明确 `gap` 的语义容器；禁止在 `Panel` 中直接并列 `ListToolbar` 与 `Table`/`AppTable`，也禁止只用 `m-*`、`mt-*` 或 `mb-*` 临时撑开。无内边距 `Panel` 使用 `ListContent inset` 统一提供内容内边距。
- 样式验收不能只看 JSX 中是否写了 `mb-*`、`space-y-*`。应检查实际相邻区块的渲染边界：桌面和手机下，工具栏—提示、工具栏—筛选、筛选—表格之间都应有稳定的可见间距；条件区块显示/隐藏、数据为空和按钮换行时也不能塌陷为 0。
- 筛选条件、已选数量和批量操作要分成可换行的布局区域；窄屏时必须自然换行或改为单列，不能挤压输入框、按钮或文字。
- Ant Design `Input.TextArea` 使用 `showCount` 时必须为字数计数预留底部空间，后续按钮、提示或表格不能覆盖或紧贴计数。
- 覆盖第三方组件的原生控件时，不能只修外层尺寸；如果控件通过 `::before` / `::after` 绘制图标，必须同时约束两根伪元素的长度、中心坐标、厚度和旋转状态，并在测试中锁定这组几何关系。尤其是表格展开、折叠、排序等小按钮，不得用全局按钮最小尺寸规则拉伸后再靠 `line-height` 补救。
- 详情抽屉中的标题、描述表、操作按钮、流程、附件、评论和事件记录应使用独立分节，分节间距由父容器统一控制。
- 处理“贴边”问题时必须同时扫描详情抽屉、移动端卡片列表、预览区、运维工作台和条件渲染区块；优先为这些容器建立语义化类名并使用 `display: grid/flex` + `gap`，不能只修截图中的一个组件或继续堆叠 `space-y-*`。桌面间距和窄屏间距都要写入布局测试并完成生产构建。
- 前端改动提交前至少检查桌面和窄屏布局；涉及主题颜色时同时检查深色模式，确认文字、控件和提示不重叠。
- 数据库和接口返回的服务器时间统一按 UTC 解析，再使用 `/api/app/bootstrap` 返回的系统时区显示；页面不能直接截取或原样输出 `created_at`、`updated_at`、`due_at` 等时间。业务日期和用户手填的本地日期不做 UTC 转换。

下发任务处理支持原始数据审核和基础管控已处理数据直发两种导入模式。已处理模式必须校验社区、登记情况、姓名、身份证号、手机号和地址；未知社区、未知状态、缺失手机号或同批身份证重复的记录仍需人工审核，且不能绕过最终发布和审计。数据上传中心只负责上传、预览、确认导入和查看处理进度，不提供发布；审核与发布统一在下发任务工作台完成。发布必须先进入选择模式，支持逐卡多选和覆盖所有分页的“全选当前筛选”，后端按明确任务 ID 整次校验后发布；不得恢复整批发布入口。当前批次选择器旁必须提供明确的“批次详情”入口，详情页只用于历史倒查、复盘、反馈导出和发布异常处理。

## 本机和服务器的环境边界

- 当前这台开发电脑只用于编辑代码、运行前端构建和不依赖真实数据库的测试。
- 本机没有本项目的 Docker 运行环境，也没有本项目的 MySQL 服务或业务数据。不要重复运行 `docker info`、`docker compose ps`、本机 MySQL 连接或端口探测来确认这件事。
- 本项目的 Docker 容器和三个 MySQL 数据库只在生产服务器上。容器状态、Compose 配置、数据库、备份和日志检查都应通过 SSH 在服务器上完成。
- 生产服务器不能代替开发测试环境。需要真实 MySQL 才能验证、但当前没有独立测试库时，先运行现有自动化测试并明确标注“真实 MySQL 未验证”，不要为了补检查而在生产库制造测试数据。
- 本机没有 Docker 时，Compose 改动可以先做文件检查；真正的 `docker compose config`、镜像构建和容器验证留到服务器部署前执行。不要反复探测或尝试启动本机不存在的服务。

## 不能随意改变的设计

- 项目标识是 `binhu`，本地仓库目录继续使用 `Bhzh`，不要改目录名。
- v0.16.0 继续使用同一个 MySQL 实例，并按业务域规划八个数据库：
  - `OnlineData`：当前正在使用的数据。
  - `OnlineDataArchive`：已经从在线表格移除的数据。
  - `daily_report`：每日快照和统计结果。
  - `PlatformData`：账号、人员、组织、权限、通知、审计、出勤和工作事件。
  - `VisitData`：走访批次、走访明细和导入异常。
  - `DispatchData`：下发批次、任务和发布结果。
  - `RegistryData`：辖区房屋、地址、机构、人员标记和任务快照。
  - `WorkflowData`：流程、工单、节点、事件、评论和附件元数据。
- 数据库之间不建立外键；分域迁移默认只读，只有维护窗口明确执行 `--apply` 后才切换开关，旧表不删除。
- 查询多天数据时，要从内部任务流水重新计算，不能把多天的日报直接相加，否则同一任务会重复。
- 单日报表纳入前期未完成且当天仍在线的结转任务，以及当天新增或地址、核查结果发生变化的任务；同一任务当天只算一次，按当天最终状态归类。未核查、已核查、已完成三列之和必须等于数据总数。
- 支持哪些表格，以 `backend/services/parsers/__init__.py` 为准。
- 支持哪些日报，以 `backend/services/report_builders/__init__.py` 为准。
- 后端有哪些接口，以 `backend/main.py` 为准；前端有哪些页面，以 `frontend/src/App.tsx` 为准。
- 全民防“疑似未注销模型三”入口和全部登记接口统一要求 `qmf.registration.execute` 权限；默认授予基础管控、中队长、所队领导、管理员和超级管理员，片长、社区民警、组长和组员默认不拥有，后续只能由权限组明确授权，不能使用固定用户名、前端隐藏或请求参数代替后端鉴权。单条支持“在吴、近期返吴、离吴（页面称离开不返吴）”；真实登记禁止批量、定时、自动执行、写接口自动重试和面向无权限用户开放，已批准的只读反馈扫描按下方独立边界执行。实现或复审前必须完整阅读 `docs/全民防模型三API封闭测试设计.md`；原 `docs/全民防自动登记分阶段开发计划.md` 只是 Redroid/Appium 历史备选。“在吴、近期返吴”执行原九步人员登记链路；“离开不返吴”只执行任务查询、社区代码核对、模型注销反馈和最终复核四步，绝不读取或写入人员和照片。离开不返吴的 `communityCode` 必须来自平台启用社区正式配置的10位全民防代码，社区由下发社区及小区管理地址唯一匹配；`qwdxzqh` 只填通过校验的身份证前六位纯数字，`qwdxz` 单独填行政区划历史库解析出的完整中文户籍地。居住证照片必须复刻 APP 按钮链路：从刚查询到的完整 `PersonnelInfo.personID` 取值，调用 `POST enterHouse/queryPeoplePhotoByJzz` 并提交 `personID`、`source=android`，按 JSON `data` Base64 解码和校验；不得继续用页面自动预取的 `GET jzz/queryLocalPhoto` 代替。同步最终投影重建必须在一个事务中发布，登记前应区分来源真实删除和投影刷新中。纯写入前失败仅在所有写步骤从未进入发送、成功或不确定状态时允许人工重新准备；任一写步骤开始后仍永久冻结自动重放。不再设置预演时间冷却或固定确认短语，但单任务并发锁、数据库执行锁、短时准备和最终二次确认必须保留。人员枚举翻译仅用于显示，原始代码继续用于写入；出生日期只可从通过完整校验的身份证提取。登录和登记前只读 HTTP 只对明确的传输故障或 HTTP 502/503/504 最多重试一次，结构错误、业务错误和 HTTP 500 不重试；真实写接口始终单次调用。只读错误只记录步骤、状态码和安全传输分类，不记录响应正文；全民防凭据、设备标识、APK、抓包、真实人员资料和照片不得进入 Git、普通日志、审计或运行记录。
- 全民防真实登记在创建准备前和进入任一写接口前，必须通过全民防管理端 `queryYysList` 精确复核外部反馈状态。已完成一致、结果冲突、多条、派出所不符、未知或查询不可用均停止；管理端未找到不能视为未反馈，仍须由手机待办接口确认唯一任务。另有明确批准的只读反馈扫描：人工覆盖全部已完成模型三任务，每日仅扫描新增/变化及超过7天未核对任务，固定四路并发、单批次互斥，不调用写接口、不修改业务数据；其缓存永远不能替代真实登记前的实时复核。
- 根目录 `VERSION` 是前后端共同使用的唯一版本号，格式遵循 SemVer。发布功能、修复或不兼容改动时分别提升次版本、修订号或主版本，并在上线验证通过后创建同名 `vX.Y.Z` Git 标签。
- 小型且不紧急的界面调整可以按用户要求先留在本机积攒，不必每项单独发布。用户确认发布版本后，再统一建立发布分支、检查、提交、合并、上线并创建标签；“先不提交、合并或上线”的要求优先于默认同步服务器规则。
- 走访明细导入和外部只读查询是两条独立的数据入口；腾讯文档同步已下线。走访明细按“本地业务日期 + 标准化地址 + 网格员姓名”去重。星级评定不单独建业务表，而是按标准化地址和前后 24 小时内最接近的采集时间补充到 `t_visit_details`。走访汇总直接按业务日期查询这张表，不进入现有核查日报、归档和总汇总流程。
- 人员管理包含岗位和独立请假操作。在线汇总固定只统计人员管理中岗位为“组长、组员”且有有效社区部门的人员，不读取旧的在线汇总岗位配置，也不保留名册外人员；出租房走访汇总继续读取超级管理员配置的统计岗位，自购房走访汇总固定使用“自购房”岗位。人员管理中尚未登记的真实走访姓名只保留在出租房中，不能同时进入自购房。
- 在线总汇总的“每日人均核查数”使用区间有效工作量除以实际在岗人日。除模型三外，空白首次变为“无法核实”计一次，之后跨日从“无法核实”变为最终结果再计一次；同日两步只计一次、结果类别修正不增加、每条任务最多两次。只累计有同步快照的日期；工作日排除请假和离岗人员，周末只计入当天备勤人员。已发生的周末缺少排班时不得猜测该指标。旧日报物理表中的“当日人均核查数”列只为回退兼容保留，页面和接口统一显示新名称。
- 在线数据汇总和走访汇总可以把当前查看结果导出为 XLSX。导出必须沿用当前账号已经过后端权限和社区范围过滤的数据；电脑端存在表头筛选或排序时，只导出当时仍显示的行和顺序，并按这些行重新计算总计，不能重新请求或导出被过滤掉的数据。在线汇总、走访汇总和下发任务反馈表的每次 XLSX 导出都要写入操作记录；记录只保存类型、日期范围和行数等安全摘要，不保存工作簿正文、身份证号、手机号或敏感搜索词。在线数据查询的 Univer 排序必须通过后端 `sort_by/sort_order` 对完整查询结果执行，并与关键词及表头筛选共同生效；禁止只重排浏览器当前片段，草稿行继续独立保留在真实数据末尾，切换业务类型或当前/归档来源时清除排序。
- 走访社区表的人均指标必须使用在岗人日，不能退回“实际参与人数”。工作日默认在岗；哪些岗位需要参加双休日备勤由超级管理员配置，默认是“组长、组员”。周末在岗日期来自每周排班，请假优先；历史周未排班时不得猜测人均值。人员管理在双休日也按当天排班显示“今日备勤”“休息”或“未排班”，不能继续把周六、周日休息人员显示成正常在岗。缺排班只检查所选区间内不晚于服务器业务当天的周末，未来周末不能提前提示或阻止历史区间的人均值。
- 工作日志日报按业务日期保存唯一草稿。系统取数在创建草稿时形成快照，除非管理员主动刷新，否则不能随底层数据自行变化；人工覆盖值必须保留。指令核查读取对应日期的在线数据总汇总表并固定采用两列口径，出租房走访读取对应日期的社区汇总；两张系统表允许修改，概览随表格联动后再允许单独覆盖。最终只导出 PDF，不再生成 DOCX。
- 人员的组织归属以“部门”为准：片长、中队长、基础管控和所队领导属于内勤；组长、组员必须且只能选择一个社区部门；社区民警必须选择至少一个社区部门并且可以同时属于多个社区。人员—部门关系以 `_grid_member_department_links` 为准，旧 `department_id` 和 `community` 只同步第一项供旧版本回退。
- 业务人员必须恰好关联一个登录账号。新增人员时必须关联尚未使用的普通账号，或同时创建一个继承岗位权限的新账号；人员管理允许把账号改绑到空闲账号，或与另一名人员互换账号，改绑后双方按新岗位继承权限且现有会话立即失效。用户管理仍不能解除、改绑或单独删除已关联账号。系统、公共和超级管理员账号可以不关联人员。
- 人员身份证号只能由超级管理员查看和编辑。普通管理员即使拥有人员敏感资料权限，接口和页面也不能返回身份证是否已填写、遮盖号码或完整号码；审计、日志和操作结果不得保存完整号码。
- 人员管理中的电话对所有具备 `personnel.basic.view` 的账号可见并可搜索；备注、请假原因和完整出勤记录仍按人员敏感资料或出勤管理权限控制。公开个人主页仍不得返回手机号。
- 业务访问必须同时检查功能权限和数据范围。本社区账号按业务数据中的社区过滤，社区别名先换成正式名称；不能只隐藏前端菜单或按钮。
- 辖区档案查看是所有登录账号的基础能力：所有现有和新建权限组都必须包含 `registry.property.view`，未关联权限组账号也要保留；数据仍按账号原有范围过滤，维护、导入、问题审核和人员标记权限不得随之扩大。
- 所有登录用户在电脑和手机访问 `/` 都先进入岗位仪表盘，当前“在线数据汇总”固定使用 `/summary`，原 `online_summary` 导航配置 ID 继续保留。电脑侧栏固定保留仪表盘；手机 Dock 只显示首页之外的最多四个分类，顶部“滨湖智慧平台”品牌固定返回仪表盘。仪表盘只返回“岗位职责范围”和当前具体功能权限数据范围的交集：组员本人、组长本社区、片长负责片区、社区民警关联社区、基础管控/中队长/所队领导全所；无关联管理员和超级管理员使用全所管理视角。实时待办读取当前来源投影，当日新下发、结转和完成读取当日快照，没有快照时必须明确显示未知。仪表盘只展示组织聚合和本人贡献，不提供实名工作量排名；桌面双列卡片采用瀑布流避免高低卡片留下整行空白，手机保持单列。所有数字卡跳转后的页面仍由后端按同一职责范围过滤。仪表盘可以在当前浏览器会话中按账号缓存最多 5 分钟：30 秒内直接复用，之后先显示旧值并后台刷新；手动刷新必须请求最新数据，登录和退出必须清除缓存，不能让不同账号共用缓存。
- 在线数据查询的当前数据来自同步时保存的腾讯来源行和业务投影，归档数据永久只读。电脑端使用延迟加载的 Univer 工作表，普通业务值必须显式声明为字符串；身份证、证件号、手机号和电话类长数字必须使用隐藏标记的精确文本类型，避免进入 JavaScript 数字路径后丢失尾数，且公式栏不能显示前导单引号。日期、时间、身份证和电话类列即使腾讯元数据为数值型也固定按文本回写。编辑结束、粘贴和清空只允许处理本次明确涉及的单元格，社区和核查人必须支持工作表内部及外部文本复制粘贴；禁止重新扫描整张工作表并把内部值变化当成用户修改，检测到长数字精度变化或文本尾零丢失时必须恢复原值且不得发送回写。保存成功后只更新被修改行及待同步状态，不能重建工作簿、重绘整张工作表或因当前存在关键词和列筛选而自动重新查询；写回失败或来源版本冲突时才重新读取真实来源。腾讯范围读取接口不会返回数值单元格的原始输入或小数显示位数，因此需要保留尾零的日期在腾讯来源表中也必须存成文本，平台不得根据一位小数猜测并补零。全链条来源表同时兼容旧 14 列布局和在“地址、创建时间”之间增加“登记情况”的 15 列布局，读取、发布、写后校验和失败后的同步对账都必须先按真实表头选择物理列布局；旧 14 列表不能因为缺少“登记情况”而误报发布冲突。“登记情况”是正式业务字段，进入当前库、来源缓存、投影、快照和归档，但不参与任务完成状态或统计口径，旧 14 列来源和旧快照按空值兼容。列宽先按当前数据自动调整，再按字段设置合理的最小和最大宽度。已有来源行只允许编辑后端声明的蓝色单元格，新增数据直接填写在数据下方自然空白行并通过选中行操作条提交；重复父行、结构操作、拖动填充和本地撤销保持只读或禁用，不能让本地表格状态绕过腾讯行版本。点击、编辑、选择或拖动嵌入式工作表时必须同时保持内容主区域和浏览器文档的纵向位置，不能因为 Univer 聚焦、松开指针后的布局更新或实际滚动容器变化把页面滚回顶部。全屏时必须让文档根节点进入浏览器全屏，再由表格卡片覆盖视口，不能只全屏卡片而把 Univer 的筛选和颜色浮层隔离在外。组长、组员可以按权限组扩大查看范围，但编辑始终限本人社区；片长查看全所、编辑限社区管理中配置的负责片区；基础管控、中队长、社区民警、所队领导、管理员和超级管理员可以编辑全所，只有这些全局岗位同时具备整行管理权限时才能新增或删除腾讯原始行。读取时必须先取得腾讯工作表的 `rowTotal` 并把分页范围限制在现有行数内，不能用超界读取的业务错误充当分页结束。新增原始行使用腾讯官方支持的区域更新请求写入下一空行，不发送未公开的插行请求；HTTP 成功和腾讯业务返回码都成功且写后回读一致，才算回写成功。回写前必须重新读取腾讯物理行并比较行哈希，同步和回写必须共享工作表锁；平台回写后不直接修改业务库、归档、快照或日报，等待下一次正常同步。超级管理员可以随时关闭在线回写总开关。
- 组长、组员在手机和电脑端都可以使用独立任务工作台：全端 `/` 先显示岗位仪表盘，手机 `/query` 继续转到 `/tasks`，电脑端从侧栏“指令核查”进入 `/tasks/home`，同时保留 `/summary` 在线汇总和 Univer 查询。片长、基础管控、中队长、所队领导以及管理员、超级管理员和对应管理员权限组账号也可以进入同一工作台，按岗位和具体权限范围查看五类流口岗任务；电脑侧栏和手机 Dock 都必须提供独立“流口指令核查”入口，不能与仍需使用的 Univer 在线查询互相替代。能否回写仍按 `online.raw.edit`、岗位硬限制、在线回写总开关、来源版本和行哈希分别校验，不能因为能查看就自动获得编辑能力。组长、组员的“本人”按关联人员姓名匹配核查人，“本社区”按正式社区及别名过滤。任务列表卡片和详情摘要都要直接显示来源行的身份证号、手机号和地址；全链条“来源”按空白和常用分隔符拆为去重标签，不修改腾讯原值。默认优先级排序在每个优先级桶内按去空白后的业务地址聚类，相同地址相邻，已完成仍沉底。详情只能返回当前账号有权查看的腾讯来源行。组长可以分配本人社区任务；片长、基础管控、中队长、所队领导和管理员账号只能在自身在线编辑范围内按任务所属社区批量分配，选择第一条后锁定同一社区，候选人必须是该社区在岗组员；组员不能分配，后端必须再次校验岗位、权限范围、社区别名和在岗状态。批量分配默认仍由组长手动触发，允许在同一社区选中任务后预览并确认平均分配，后台不自动均分；选择模式下已选数量必须在滚动时持续可见。疑似未注销模型三的“备注”是手机任务正式可编辑字段，但不参与完成口径。腾讯空白结果单元格只返回空选项时，平台优先复用同一工作表中已缓存的真实选项文本，完全找不到时才使用该业务的固定默认结果，不能向用户显示空白下拉项。核查人、社区和核查结果在界面中仍可用选择器；保存时先按真实选项校验合法值，再按腾讯官方区域更新接口支持的文本值写入并回读确认，不能把范围读取返回的 `select` 对象直接写回，否则腾讯会清空选中值。电话号码字段可能包含多个相连或分隔保存的号码，手机端必须拆成独立选项后拨打，不能把整串数字直接交给 `tel:`；多号码必须使用浏览器和手机系统提供的原生选择控件，不能再用容易受页面动画影响的自制蒙版弹窗。多字段保存必须先整体校验权限、来源版本和行哈希，再用一次腾讯批量请求写入并整体回读；保存后只更新来源缓存和待同步状态，不自动跳转、不直接改业务库或日报。
- 在线数据概览中的结转、新下发、已有任务变化、待完成和已完成卡片都可以查看同口径分页明细。明细必须复用概览的日期、业务类型、固定人员范围、社区权限、区间去重和分类函数，并读取对应日期快照显示对象、号码、地址、社区、核查人和结果；不能用当前在线投影或前端筛选猜测历史分类。同步部分失败留下的流水运行记录只有在对应快照表真实存在时才能参与概览，前一张快照缺失时按首张可用快照处理，不能直接查询缺失表。明细还要优先显示快照业务字段中的“下发日期/下发时间”作为首次下发日期，旧业务没有该字段时才回退区间首次活动日期。每个分类的明细总数必须与卡片数字一致。
- 岗位和账号都可以叠加多个权限组。功能权限取并集；数据范围必须按当前请求的具体权限分别合并，只有授予该权限的组才能扩大它的数据范围，不能让两个权限组交叉拼出越权范围。旧单权限组字段继续保存排序后的第一个组，只用于旧版本回退。
- 同一账号最多保留一个电脑端和一个手机端有效会话。登录只替换相同端类型的会话，另一端不受影响；同端旧会话在下一次请求时返回 `session_replaced`。User-Agent、请求头和 device_id 只能辅助端类型分槽，不能绕过认证；device_id 只保存服务端哈希，普通日志不得记录原始值。修改密码、账号改绑、权限变化、停用和删除必须撤销全部设备会话。在线状态不能依赖单一 `active_session_id`。空闲时间默认 30 分钟，只有页面跳转、主动查询、明确提交和“继续使用”会续期，后台轮询、自动保存、滚动和键盘输入不能续期。
- 工作日志表格中的“网格员数”按业务日期、固定在线汇总岗位“组长、组员”、人员所属社区和当天出勤自动计算。请假人员排除，周末使用双休日备勤；排班不完整时不得猜人数。刷新已有草稿只补空白人数，不能覆盖人工修改。
- 在线日报只能随手动或自动同步生成，页面和公共接口不提供单独的“生成日报”操作；需要重算时由有权限的管理员重新同步。
- 工作日志草稿可以由管理员在独立页面集中查看和删除。删除只影响被选中的草稿，不得连带删除在线数据、走访数据或日报统计；删除动作必须二次确认并写入操作记录。
- “文件生成”页面使用两个独立功能 Panel：工作每日明细 XLSX 不依赖草稿或人工填写，工作日志单独创建草稿并导出 PDF。每日明细人员行读取当前在用名册；出租房和自购房走访按岗位分别取数；指令核查读取所选日期日报快照，核查指令数固定为“已核查＋已完成”，完成指令数固定为“已完成”。出租房、自购房目标数默认 10、15 并按账号记忆。原始参考表不能提交，发布包只允许包含清空示例姓名、数字、备注和背景色的脱敏结构模板。
- 走访汇总社区表必须在“在岗人日”前显示“网格员人数”，口径与在线汇总一致：按区间结束日统计岗位为组长、组员且有有效社区部门的实际在岗人员。该列进入页面总计和 XLSX，但不参与人均指标计算。
- 社区民警的人员—社区关系是唯一来源。社区管理可以从已登记的“社区民警”岗位人员中为社区选择多位民警，修改会直接更新同一份人员关系；`_communities.police_officers` 只保留兼容镜像。新建工作日志草稿时自动带入这些姓名，多位民警在表格中使用“、”连接；已经存在的草稿继续保留原快照和人工内容。
- 消息中心对所有登录用户开放，并区分全员公告和个人提示。公告内容只保存一份，但已读状态必须按账号分别保存；只有超级管理员可以发布或删除公告。
- 所有登录用户都可以查看其他人的公开个人资料，但不设置独立“人员主页”导航或人员目录；界面入口统一为人员管理中的可点击姓名，旧 `/people` 地址重定向到人员管理，详情地址继续使用 `/people/{user_id}`。公开资料只包含姓名、岗位、部门、社区、加入平台日期和按日汇总的实际工作贡献，不得返回用户名、身份证号、手机号、权限组、权限、登录或会话信息。实际工作只包括成功写回腾讯的核查业务字段、逐条下发任务审核和工作日志编制；登录、浏览、查询、导出、同步、上传、发布、任务分配和系统配置不计入。贡献事件以关联人员为主键，账号改绑后继续归到人员；事件表只保存人员/账号键、工作类型、数量、时间和幂等键，不保存业务正文。
- 流口任务标记为“已研判”时，列表卡片和详情摘要必须直接显示来源行“研判”字段的具体内容，不能只显示状态标签或要求用户展开全部原始字段；该展示不改变“无法核实”的完成口径，也不授予网格员编辑研判的权限。
- 流口任务卡片顶部固定显示姓名和右上角任务状态，不再显示进入箭头；中间用统一行高、间距和字重显示身份证号、首个手机号及地址，身份证号和手机号继续支持复制，多个手机号显示“首号 +N”，复制和拨打都使用系统原生号码选择器并阻止卡片跳转。研判结果显示在来源标签云上方。社区、核查人和截止日期统一放入卡片底部的中性深色信息带，截止日期显示为 `MM-dd`，不能再被首次下发日期覆盖。疑似未注销模型三的正式结果为“近期返吴、在吴、离吴”，三者均算已完成；历史错拼值“近期反吴”只作兼容，不能继续作为新下拉选项。
- 腾讯“调照片名单”普通行一行对应一个照片工单，A 列独占时间文本只作为批次边界。在线任务快捷申请先落本地工单，再通过幂等队列追加腾讯 A–F；成功找到照片后独立把 G 列写为“滨湖平台已调照片”。历史导入不改腾讯旧行，完全重复申请也不去重。读取和写入分别开关，写入还受全局在线回写开关控制；行移动时只能按 A–F 摘要唯一重定位，多个候选必须进入冲突列表，不能猜测覆盖。
- 照片 ZIP 导入形成的照片附件对用户显示和下载时使用“姓名_身份证号.扩展名”；历史平台生成名称可动态转换，但不得重命名或搬动既有存储文件。流口任务卡片的“已调照片”只能来自精确任务关联、已完成/批准工单、有效图片附件和当前账号附件权限，不得只按身份证号推断。个人头像保存在独立持久化目录，替换时必须先成功更新数据库再删除旧文件，数据库失败时清理新文件；头像访问要求登录，普通日志不得记录文件正文。
- 流口任务列表的筛选必须使用 POST 正文传递关键词；社区和核查人选项由后端按当前业务、职责范围和实际任务生成，并支持空社区、未分配核查人。选择一个或多个社区后，核查人选项必须只按这些社区中的实际任务重新生成，数量也必须按所选社区重新统计；社区选项本身保持当前职责范围内的完整集合，不因联动而消失，已经不适用的核查人条件要自动移除。默认优先级固定为已研判、来源异常、待同步、普通待处理、等待研判、已完成，更新时间排序也必须让已完成沉底；数量卡统计当前筛选条件下的全量任务，非敏感条件可写入 URL，关键词不得进入 URL 或普通访问日志。
- `online.task.manage` 只表示管理现有流口任务及研判。片长、基础管控、中队长、社区民警、所队领导、管理员和超级管理员默认拥有该权限，并在流口任务工作台内查看全所、编辑现有任务字段和分配核查人；它不能开放 Univer、腾讯原始行增删或其他管理模块。研判内容只允许通过专用详情和保存接口修改，普通任务详情不得暴露研判输入框，审计不得保存研判正文。
- 业务与管理列表统一使用两层工具栏：上层按关键词、业务条件即时筛选，下层显示结果/已选数量和明确业务操作。关键词约 350ms 防抖，筛选变化回到第一页，旧请求不得覆盖新结果；手机端筛选改为单列、操作区可换行。统计报表、Univer、文件编辑器、权限配置和流程配置器不套用该规范。
- “我的任务流”内测页只向超级管理员开放，复用现有业务接口，不复制任务正文或建立第二套任务状态。网格员视角按核查人聚合五类流口待办；基础管控视角聚合网格研判、调照片和下发批次。任务节点必须是自由 React Flow 节点，不能通过 `parentId` 或 `extent` 限制在固定区域；按钮和连接端口使用 `nodrag/nopan`，卡片正文可自由拖动。三个状态只做提示，不改变用户布局。自动流程必须使用不参与坐标和 `fitView` 的固定 `Panel` 流程条；用户虚线不能改变真实业务状态。自动刷新必须使用被动请求，不能续期会话。
- 局下发的全链条 `.xls/.xlsx` 先进入独立批次和内勤共享审核队列，平台建议不能自动替代人工审核。手机号为空的记录优先标为“待研判”，不能参与社区匹配或平均分配；基础管控补齐手机号或改为其他最终动作前，后端必须拒绝下发到社区。小区地址库只按社区正式名称和社区管理别名解析；公寓只做社区映射。早期映射表已经一次性入库，新程序只保留“小区管理”的增删改查，不再提供重复导入映射表的入口。小区管理和任务工作台必须同时具备对应功能权限和该权限的“全部社区”数据范围；关联人员只允许基础管控、中队长，无关联人员的管理员或超级管理员系统账号允许，其他人员岗位不能靠叠加管理员组进入。敏感任务关键词只允许放在 POST 请求正文，下发接口的 nginx 与后端访问日志不得记录查询参数或正文。基础管控、中队长全端 `/` 先进入岗位仪表盘，手机 `/query` 继续分流到 `/police-tasks`，电脑从侧栏进入同一“下发任务处理”共享队列。只有超级管理员可以撤销下发批次；只要批次没有任何任务成功进入本地任务池，待审核、待发布、可重试、内容冲突、发布结果和后台运行记录都可以在同一事务中安全清理。后台发布仍在运行或已有成功下发任务时必须阻止整体撤销；内容冲突关联到的是既有本地来源，不得因为撤销批次而修改或删除该来源。撤销操作必须记录不含人员正文的审计。
- 任务处理导航统一包含“下发任务处理、研判、调照片”。“研判”必须同时承载两类业务：默认的“网格核查研判”显示核查结果为“无法核实”且研判字段仍为空的数据；“下发数据复核”处理导入文件中无法直接确定去向的记录。两类业务不得混用数据表或保存接口：前者继续走流口来源版本、权限和腾讯回写，后者继续留在原下发批次并受整批审核后才能发布的约束。“调照片”独立页只承载基础管控照片待办队列；“工单中心”归入工作台。旧手机 Dock 中的入口应平滑迁移，不要求用户重新配置。
- 社区启用状态同时控制人员新分配、小区地址匹配、平均分配和新业务选项。有任何人员关联或未完成下发任务时不能停用；有小区地址或历史任务引用时不能物理删除，只能停用。停用不删除历史批次、地址、别名和已完成数据。
- 公安任务全批审核后才能向唯一启用的腾讯全链条表发布；原移交备注拼入“地址”而不是“研判”，发布必须冻结首次日期、按业务主键防重并逐条保存结果。写入前明确失败才可重试；请求可能已到腾讯、回读失败或结果不确定时进入“等待同步对账”，只能由下一次完整正常同步确认成功、内容冲突或可安全重试。发布、回读和同步对账只比较下发拥有的日期、社区、来源、人员和原始业务字段；核查人、现住址、核查结果、研判和二次反馈属于下发后可编辑字段，腾讯用户并发填写时必须保留且不得误判为发布冲突。内容冲突只能采用腾讯现有内容，或在重新校验行哈希并二次确认后覆盖已有行，不能新增重复行；覆盖时也不能清空上述可编辑字段。原文件、完整身份证号、手机号、地址和反馈正文不得进入 Git、普通日志或普通审计。
- 下发任务“发布所选”使用持久化后台运行并展示进度。同一启用全链条表同一时间只允许一个发布运行；`queued/checking` 表示尚未发送，可在中断后安全重试，`sending` 表示请求可能已经到达腾讯，中断后必须转为等待同步对账。运行表只保存内部任务 ID、数量、状态和安全错误码，不复制业务正文或敏感字段。
- 全链条、出租房屋核查、寄递业和疑似返苏中，结果“无法核实”属于已核查但未完成，继续每日结转；二次反馈和研判不决定完成。核查完成率是最终完成数除以唯一任务数，核查见底率是最终完成数除以“最终完成数＋当前无法核实数”，两列模式把无法核实归到未完成一侧。历史有效工作量回算必须先只读预览影响范围、备份 `daily_report`，再按日期顺序幂等重建；一次性执行包装只能放 `scratch/` 并在完成后清理。
- 全链条、出租房屋核查、寄递业、疑似返苏、苏州涉警和交通涉警的正式核查结果包含“待登记”。上线前已经存在的全链条“待登记”按历史口径保持已完成；上线后新增“待登记”属于已核查但未完成，必须关联辖区档案内任务社区的唯一有效房屋，自动或受控人工确认成“已登记”后才计入完成率、见底率和已完成任务。疑似未注销模型三不进入这套闭环。腾讯旧下拉元数据缺少业务新增结果时，页面显示选项和写回校验选项必须一起补齐，同时保留已有腾讯选项 ID；禁止出现页面可选但保存时报“无效的下拉选项”。
- 普通用户不能直接把上述六类任务写成“已登记”，也不能用自由文本地址冒充房屋关联。“待登记”结果、稳定 `property_id`、房屋版本和腾讯“现住址”投影必须在同一事务中保存；切换为其他结果时取消未完成的关联，但不得擅自清空腾讯原文本。跨库只保存房屋 ID，不建立物理外键，不持久化居住证登记地址正文、完整外部响应或新的人员敏感信息副本。
- 居住证自动确认只能使用有效且未注销的登记状态，并要求任务身份、社区、来源版本和房屋版本未变化；登记地址标准化后必须与房屋当前规范地址、启用别名或地址历史唯一精确相等。禁止包含、前缀、相似度或其他模糊匹配。必须由两个不同扫描周期连续返回同一地址 HMAC，首次匹配只进入等待再次确认；地址不一致、多房同址、房屋停用、社区冲突、来源变化或查询失败均进入复核，不能自动改写用户所选房屋。
- 自动确认“已登记”必须复用本地优先写回队列：锁定并复核来源行、写入本地修改、重建投影、后台安全同步腾讯；同步冲突或失败时保留复核和后台任务记录，不得提前归档。基础管控、中队长、所队领导、管理员和超级管理员的人工确认也只能用于居住证已有有效登记但地址不一致或不唯一的情形，必须选择原因并留审计；查无登记、已注销或查询失败时禁止强制确认。
- 全链条“已登记”反馈归档不再读取公安网原始数据。只有居住证连续两个独立周期确认同一房屋、腾讯“已登记”写回明确成功、登记链接保持 `confirmed` 且 `confirmed_at` 已完整经过 24 小时，才允许进入候选；完整 24 小时按时间点计算，不按跨自然日计算。
- 创建已登记归档批次时必须冻结确认时间、来源版本/摘要、身份 HMAC、房屋 ID/版本和候选规则版本；腾讯删除前再次复核这些证据、来源唯一性、房屋有效状态和本地写回状态。证据变化必须在删除前停止，腾讯已经明确删除后的平台补偿只能使用冻结快照，不能再次发送删除请求。
- 旧腾讯“已登记”但缺少居住证确认链的数据不得自动归档或伪造 `confirmed_at`。公安网原始数据上传、预览和确认入口已经停用，旧写接口必须返回 410；历史文件和上传记录暂时按原权限只读保留，不得在普通日志、审计或新表中复制原始身份证和居住证地址正文。

## 两台电脑怎么开发

## Pull Request 与远程 CI 门禁

### GitHub Actions 构建内存故障记录（2026-09-01）

- 2026 年 9 月 1 日，PR #479 的 `Frontend checks` 和 `Desktop clients` 在 GitHub-hosted runner 上于 Vite 渲染完整 bundle 阶段失败，日志为 `FATAL ERROR: ... heap out of memory`，Node 堆接近约 2 GiB；同一提交在本地构建通过，后端测试也正常。这类结果应判断为 runner 的 Node 堆上限/资源约束，不得误报成业务代码测试失败。
- 重跑同一 run 后仍在相同构建阶段 OOM，说明不是简单网络抖动。读取 `gh run view <run-id> --log-failed` 的失败步骤和堆栈后再归类，不能只凭红叉猜测。
- 前端和桌面 Vite 构建步骤统一设置 `NODE_OPTIONS=--max-old-space-size=8192`；这是 CI 构建资源配置，不等于放宽业务运行时内存，也不应通过无意义空提交“刷绿”。
- OOM 修复后必须重新触发一次有实际工作流配置变化的 `pull_request` 检查，并确认新 run 检查的是最新提交。远程检查未全部进入 `success` 前，仍不得建议合并。
- 其他常见失败要区分：PR 正文模板校验、后端测试、前端测试、桌面架构检查、依赖安装、构建资源和实现缺陷。每类都要保留失败 run、错误摘要和修复依据；环境问题不能被写成代码通过，代码问题也不能借口环境跳过修复。

本项目的“本地检查通过”不等于 Pull Request 已通过。任何 Agent（包括副开发 Codex）都必须把 GitHub 远程检查当作合并门禁，不能凭本机测试结果提前汇报“可以合并”或“已上线”。

### 创建或更新 PR 前

- 先读取仓库当前的 `.github/pull_request_template.md` 和 PR 正文校验脚本；模板章节、标题和顺序以仓库当前版本为准，不能凭记忆写简化版。
- PR 正文必须保留全部必需章节并填写实际内容：问题、实现方式、数据与安全边界、验收步骤、测试结果、风险与回滚、后续方向、暂时无法解决的问题、兼容性与发布信息。不得保留“问题 1/2/3”、空的验收前置条件或其他模板占位项。
- PR 中只写可复核的技术事实，不把猜测、未验证的生产现象或真实敏感数据写进正文、评论、日志和测试产物。
- 提交前至少运行 `git diff --check`、相关本地测试、构建和敏感信息扫描；把每项命令和结果写入 PR 的“测试与验证结果”。本地未运行的检查必须明确标为“未验证”，不能写“通过”。
- 如果 PR 只改文档或流程，也必须说明它没有改变业务运行代码、是否需要部署，以及为什么。

### PR 创建后必须核对远程检查

- PR 创建或推送后，必须等待 GitHub Actions 结束，再检查 `gh pr checks <PR号>` 和对应 workflow run；不能只看本地终端或“任务已开始”。
- 需要定位失败时，先看失败任务名称和结论，再用 `gh run view <run-id> --log-failed` 读取日志，判断属于 PR 正文门禁、后端测试、前端测试、桌面检查、构建、依赖/环境还是实现问题。不得把环境失败改写成代码通过，也不得把代码失败归因于环境而跳过修复。
- 远程检查必须达到终态：所有 required checks 为 `success`，没有 `failure`、`cancelled`、`timed_out` 或仍在运行的关键任务。只要有一个关键检查未完成，就只能汇报“等待 CI”，不能建议合并。
- PR 正文修改后，旧的 `pull_request` 事件任务可能仍携带旧正文；GitHub 的“重新运行”不会重新生成事件载荷。此时先确认远程任务实际读取的正文，再通过一次有实际意义的分支更新触发新的 synchronize 事件，或关闭后重新打开 PR 触发新事件。禁止为了刷 CI 添加空提交、无意义测试或无关代码。
- 若发现 PR 正文校验器只能读取旧事件正文，应优先在独立修复中让工作流读取当前 PR 正文并补测试；在修复合并前，按上一条规则处理，不得声称“模板已补齐所以旧检查应当自动变绿”。

### 合并前、合并后和发布汇报

- 合并前必须记录：PR 编号、分支最新提交、远程 CI run 编号、所有检查最终状态、是否有未验证项目、是否需要数据库备份和是否允许部署。
- 合并后必须重新核对 `main` 上的主 CI；PR 的绿勾不能替代合并提交的主线检查。目标提交未进入 `main` 或主线检查未完成时，不得触发生产部署。
- 汇报必须分开写清以下状态：`代码已提交`、`PR 已创建`、`远程 CI 已通过`、`PR 已合并`、`main CI 已通过`、`生产已部署`、`桌面 Release 已发布`、`实机已验收`。任何一个状态未完成，都不能笼统写成“已上线”。
- 失败后先保留失败 run、错误摘要和修复依据；不要删除检查记录、覆盖日志或直接重跑到成功为止。修复后要确认新 run 检查的是新提交和最新 PR 正文。
- 副开发只负责其授权范围内的代码、测试、PR 和报告；除非主开发或项目管理人明确要求，不自行合并、部署、创建标签、修改生产数据或执行真实外部写入。

- `origin/main` 是正式主线。家里和单位不要各建一个长期分支。
- 每个功能单独建短期分支，例如 `feat/example`、`fix/example`。Codex 创建的分支使用 `codex/` 开头。
- 开工前先更新 `main`，再创建功能分支。
- 换电脑前，把当前进度提交并推送到 GitHub。
- 在另一台电脑上拉取同一个功能分支继续开发。
- 功能完成并检查通过后，通过 Pull Request 合并到 `main`。
- 不要对共享分支强制推送，也不要用 `git reset --hard` 掩盖冲突。

具体命令见 [开发与运维手册](docs/operations.md)。

## 一次性代码放在哪里

- 临时部署、迁移、诊断、核验或数据修复代码统一放在项目根目录的 `scratch/`，不要再散落到根目录、`backend/` 或正式业务包中。
- `scratch/` 整体由 Git 忽略，不参与提交、构建和部署；正式程序不得导入或依赖其中的代码。
- `scratch/` 不能保存密码、令牌、私钥、真实名单、业务数据或数据库备份。需要凭据时只能从本机私密环境或 `AGENTS.local.md` 读取，且不能输出到日志。
- 一次性任务完成并留下必要的提交、标签、验证和回退记录后，应清理对应文件。若代码后来成为长期能力，必须移入正式目录、补测试和文档后再使用。

## 默认同步服务器

- 用户已经长期授权：会影响平台运行的改动，在检查通过并合并到 `main` 后，默认同步到服务器。
- 用户明确说“不同步”“只改本地”或“先不要部署”时，必须停止在本地或 GitHub。
- 默认同步按改动风险准备回退，不再要求每次都备份全部数据库：
  - 纯前端更新只保留上一版前端。
  - 不改数据库结构和数据写入逻辑的普通后端更新只保留旧镜像。
  - 同步、导入、报表计算或数据库结构发生变化时，备份受影响的数据库；影响范围不明确或执行八库迁移时使用 `backup_scope=all`。
- 每日数据库备份继续按原计划执行，部署后仍要检查健康接口、关键页面和日志。
- 只新增表或可空字段、旧版本仍能继续运行的兼容性数据库变更，在测试通过、受影响数据库备份有效且回退办法明确后，也按默认规则上线。
- 删除或覆盖数据、恢复备份、删除/重建表和数据卷、不可逆迁移、轮换凭据等高风险操作不属于默认上线，仍要单独确认。
- 正式发布统一使用 GitHub 的 `Deploy production` 手动工作流。普通 push 和 Pull Request 只能运行 CI，不能自动连接生产机；发布人必须明确填写版本和数据库备份范围，目标提交必须已经进入 `main`。
- `Deploy production` 的 `ref` 必须填写已经合并到 `main` 的完整 40 位小写 Git 提交号，禁止填写 7/8 位短提交号、分支名或标签。触发前必须校验提交号长度，并用 `git merge-base --is-ancestor <完整提交号> origin/main` 确认它属于生产主线；`gh workflow run ... --ref main` 只用于选择工作流文件所在分支，不能代替表单中的 `-f ref=<完整提交号>`。
- 发布工作流的 `release_scope` 默认使用 `auto`：生产提交祖先可验证时，纯后端运行代码使用仅含 `VERSION + backend/` 的后端精简包，纯前端运行代码使用仅含 `VERSION + frontend/dist` 的前端精简包；前后端同时变化、Compose、Nginx、部署脚本、未知路径或状态不确定时必须使用完整包。前端精简发布复用现有后端镜像，但仍重建后端和运维代理容器，使挂载的版本号及静态资源立即生效。
- GitHub 只能使用专用 `binhu-deploy` SSH 身份。该身份由服务器端强制命令限制，只能把校验过的发布包交给固定部署脚本，不能取得普通 Shell、端口转发、Docker Socket 或任意 root 命令。日常 root SSH 密钥不能复制到 GitHub。
- 服务器部署脚本必须先校验版本、提交、文件哈希、磁盘空间、Compose 配置及同步/备份空闲状态，再构建候选镜像、保存旧程序和旧镜像、按发布人选择的范围备份数据库并切换。健康检查失败只自动恢复旧程序和旧镜像，绝不能自动导入数据库备份。
- GitHub Environment 中的 SSH 私钥、主机指纹和地址只作为加密 Secret 保存。仓库、工作流日志、构建产物和服务器发布历史都不得出现这些值。紧急情况下可以使用私密运维资料进行人工发布，但必须说明为什么绕过固定工作流并留下验证记录。

### Windows 桌面客户端发布

- 桌面客户端与平台服务端共用根目录 `VERSION`。修改版本号后必须运行 `python desktop/scripts/sync_versions.py`，并提交脚本同步的 Electron、Tauri、Velopack、安装器、`desktop/package-lock.json` 和配置文件；发布前用 `python desktop/scripts/sync_versions.py --check` 证明没有漏同步。桌面客户端不单独维护版本号，也不能只改某一个 `package.json`。
- 桌面发布只允许通过进入 `main` 的 PR 触发。合并后的主线 CI 成功后，由 `.github/workflows/desktop-release.yml` 自动完成前端构建、Win7 Electron/VxKex 构建、Win10/11 Tauri 构建、全量包和增量包生成、更新服务器发布及 GitHub Release 归档；不得从开发电脑手工上传更新包，也不能把 GitHub Release 当作正式更新源。
- 发布前必须确认根目录 `VERSION`、所有桌面同步文件、前端构建和桌面架构检查均通过。0.25.15 是首个全量基线；从 0.25.16 起，Win7 与 Win10/11 两个平台都必须同时提供当前版本全量包和基于上一版本全量包生成的增量包。缺少任一平台全量包、增量包、清单或校验文件时，发布必须失败。
- Win7 与 Win10/11 的上一版本全量包必须按平台隔离存放和读取，禁止把两个平台的基线包放入同一目录后用“取第一个文件”的方式选择。Win7 只能使用 `com.bhzh.binhu.win7.x64`，Win10/11 只能使用 `com.bhzh.binhu.win10.x64`；构建前必须校验基线文件名、Nuspec PackageId 和上一版本号，构建后必须校验清单中的每个包仍属于当前平台。
- 桌面版本门禁允许同一版本标签指向较早提交的情况，但仅限于当前提交相对已发布提交没有改变 `VERSION`、`frontend/`、`desktop/` 或 `mobile/`。只改 CI/CD、发布脚本、文档或其他发布元数据时可以跳过重复构建；只要上述发布面发生变化而版本号没有增加，必须拒绝发布。版本降低、同版本不同发布提交、文件哈希错误、非法文件名、目录穿越和缺少上一版本全量包都必须拒绝。

#### Client release 版本门禁与失败排查

- `Client release` 会在每次 `main` 的 CI 成功后自动触发，并不表示项目已经明确准备发布新版本。Pull Request 分支的 CI 也可能产生结论为 `skipped` 的同名运行；这是触发条件正常拦截，不是发布失败。必须区分 `skipped`、`failure` 和真正完成发布的 `success`。
- 近期多次 `Client release` 失败的共同根因是 `prepare / Apply release version gate` 返回 `frontend, desktop or mobile changed without increasing VERSION`。这表示线上版本与仓库 `VERSION` 相同，但从线上发布提交到当前主线，`frontend/`、`desktop/` 或 `mobile/` 已发生变化；它是构建前的主动版本保护，不是编译、签名、增量包、证书或服务器上传失败。
- 同一个提交反复重新运行不会绕过版本门禁，也不会自行变绿。禁止无意义重跑、添加空提交、手工上传包、移动旧标签、覆盖同版本发布或放宽门禁掩盖问题；重跑前必须先确认外部环境确实发生了可改变结果的变化。
- 如果客户端变化需要发布，必须在所有准备纳入该版本的客户端 PR 合并并通过主线 CI 后，只创建一个最终版本 PR：提升根目录 `VERSION`，运行 `python desktop/scripts/sync_versions.py`，补齐 `release-notes/v<version>.md`，再用 `python desktop/scripts/sync_versions.py --check` 验证同步完整。不得每合并一个客户端 PR 就单独尝试发布或连续提升版本。
- 如果当前阶段明确不发布客户端，可以暂时保留版本门禁失败，但汇报必须写成“主线 CI 已通过，Client release 因版本尚未提升而被门禁阻止”；不能说“客户端已发布”，也不能笼统归类为代码构建失败。如果改动本不应影响客户端发布面，应检查是否误改 `frontend/`、`desktop/` 或 `mobile/`，而不是关闭门禁。
- 排查失败必须先看失败 Job 和 Step，再决定处理方向：
  - `prepare / Apply release version gate`：核对线上版本、线上发布提交、根 `VERSION` 及从该提交至当前提交的 `VERSION`、`frontend/`、`desktop/`、`mobile/` 差异；
  - `baseline`：核对上一版本 Full 包、校验文件、平台隔离和下载来源；
  - `frontend`：检查依赖安装与前端生产构建；
  - `android`：检查 Android 构建、签名和产物；
  - `build`：检查 Win7/Win10 架构、Full/Delta、签名、打包路径与产物校验；
  - `publish`：检查更新服务器、平台清单、校验文件和 GitHub Release 审计归档。
- Android 正式构建会在 Tauri `beforeBuildCommand` 中再次执行完整前端生产构建。若日志已经完成模块转换和产物统计，随后出现 `heap out of memory`，这是 Node 默认堆上限不足，不是 Android 签名、Gradle、Rust 或证书故障。正式工作流必须为该步骤保留明确的 `NODE_OPTIONS=--max-old-space-size=8192`；修复后重新走主线 CI 和完整 Client release，不得手工补传 APK 或只重跑失败 Job。
- 准备正式版本前必须先读取更新服务器当前 Win7、Win10/11 和 Android 的版本与发布提交，再比较该线上提交到待发布提交的客户端发布面。发现发布面有差异时，必须先提升版本并准备发布日志；不能等工作流失败后才补版本号。
- 发布状态汇报必须留下可核对的层次：主线 CI 是否通过、`Client release` 是 `skipped` 还是 `failure`、失败 Job/Step、版本门禁是否通过、更新服务器是否发布、GitHub Release 是否归档、实机是否验收。只有后四项按流程完成后，才能报告客户端新版本可供验收。
- 更新服务器发布使用受限的 `binhu-update-publish` 账号和固定网关。并发锁只能使用 `/srv/binhu-updates/state/publish.lock`；禁止重新使用更新根目录下的 `publish.lock`，也不能把 root SSH、Docker Socket 或任意 Shell 权限交给 GitHub Actions。正式更新地址固定为 `https://47.100.44.36/updates/win7-x64/` 和 `https://47.100.44.36/updates/win10-x64/`。
- 桌面发布需要上一版全量包来生成增量包；GitHub Runner 不得直接假定更新服务器 HTTPS 下载一定可达。受限网关可以提供只读的 `fetch <平台> <当前全量包文件名>`，但只能读取当前状态文件指定的 `public/<平台>/` 全量包，禁止读取归档、任意文件或执行其他命令。修改 `desktop/server/` 后，必须先由运维按 `desktop/server/install-server.sh` 更新服务器固定入口，再触发桌面发布工作流。
- 所有会被 Linux 执行或解析的文件必须使用 LF 换行；无扩展名脚本必须在 `.gitattributes` 中逐项声明 `text eol=lf`。更新服务器安装脚本必须在覆盖线上文件前拒绝 CRLF，systemd 定时任务还必须通过独立失败处理单元记录包括 `203/EXEC` 在内的启动失败，不能只依赖业务脚本内部计数。
- 若服务器网关尚未升级或 GitHub Runner 临时无法读取更新服务器，构建阶段可以从上一版本的 GitHub 审计 Release 获取同名全量包作为兜底，但必须同时下载对应平台的 `checksums.sha256` 并校验通过。此兜底只用于生成增量包，客户端更新地址、发布清单和正式安装包仍只能由更新服务器提供。
- 桌面发布完成后，先检查两平台 `releases.stable.json` 均包含 `0.25.16` 的 `Full` 与 `Delta`，再以 0.25.15 客户端验收“发现更新、优先增量、增量失败回退全量、重启后版本与本地会话/配置保持”。GitHub Release 只用于审计和备用归档；实机验收通过前不得手工改标签或重复发布同一版本。
- 更新服务器发布门禁必须拒绝跨平台包、跨平台 PackageId 和跨平台清单引用；发布后必须从公网分别读取 Win7、Win10/11 清单，确认不存在其他平台的包。发现平台污染时不得覆盖原版本清单或重写原 Delta，必须提升修订版本重新生成 Full/Delta，并明确标记受影响版本的增量包不可依赖。
- 以后处理桌面 PR 时，汇报必须区分“代码已合并”“CI 已通过”“更新服务器已发布”“GitHub Release 已归档”和“Win7/Win10 实机已验收”五个状态；任何一项未完成都不能笼统写成“已上线”。
- 桌面版本发布流程固定为：PR 评审 → 合并 `main` → 主 CI 成功 → 确认服务器网关版本 → Desktop release 自动构建 Full/Delta → 更新服务器两平台清单核验 → GitHub Release 审计归档 → Win7 与 Win10/11 实机验收。发布失败时先区分代码门禁、构建、基线包获取、服务器发布和客户端验收，不得手工补传包或提前创建标签；只有更新服务器和 Release 都成功后，才向使用者报告“可验收”。
- 如果 Desktop release 已经成功发布更新服务器，但仅在 GitHub Release 资产上传或归档阶段失败，必须将其视为“服务器已发布、审计归档待补齐”：禁止回滚或再次发布同一版本，禁止把当前版本当作下一次增量基线；应从该次成功构建产物或线上已核验的当前版本 Full/Delta、安装器和清单中恢复同一个 `v<version>` Release，并记录原始运行号、发布提交和资产哈希。恢复完成后再继续实机验收。
- Velopack 的内部打包暂存目录、执行文件名和临时包标题必须保持 ASCII；产品对用户显示的中文名称可以继续保留在 Electron/Tauri 和安装器元数据中。Windows 构建工作流必须在 ASCII 暂存根目录内运行，并在打包前检查路径，避免 zstd 因中文字典文件名或路径回退到 bsdiff。
- Desktop release 必须用 `desktop/scripts/generate_release_notes.py` 从上一个版本标签到当前发布提交之间的已合并 PR 生成 Markdown 和 JSON 更新日志。PR 只提供技术事实；在准备具体版本时，Codex 必须一次性阅读本次版本包含的全部已合并 PR，编排一份发布级 `release-notes/v<version>.md`，按业务主题和用户可理解的语言总结，不能要求用户逐个 PR 撰写日志。发布脚本读取该发布级文件，同时保留 PR 编号用于技术追溯；若文件暂时不存在才使用安全的 PR 摘要兜底。不得写入身份证号、手机号、地址、Cookie、密码或外部系统返回原文；Markdown 写入 GitHub Release，结构化 JSON 必须随桌面前端打包并由更新弹窗按主题展示。
- 桌面端升级后第一次进入已登录业务页面时显示“版本已更新”弹窗，内容读取随包 JSON 并优先按业务主题和条目展示，旧格式才回退到 PR 摘要；首次安装、同版本重启、网页端和离线模式不显示。用户确认后通过原生升级元数据清除待展示标记，不能只依赖可被清空的前端 localStorage。

## 安全底线

- `.env`、`backend/.env`、`AGENTS.local.md`、密码、令牌和私钥不能提交到 Git。
- 共享文档只写变量名和示例值，不能写真实服务器地址或真实凭据。
- 删除数据库、重建数据卷、恢复备份、部署服务器和轮换密码都是高风险操作，必须先确认备份和回退办法。
- 不要把 `docker compose down -v` 当成普通排错命令，它会删除数据卷。
- 运维中心只能提供只读状态、只读日志、备份创建和下载；不能增加网页终端、任意 SQL、恢复、删除或容器重启。
- 运维中心的操作记录默认展示当前人员姓名或账号显示姓名，并把操作代码、目标类型、结果和详情字段翻译成中文摘要；账号、原始操作代码、原始目标和原始 JSON 只能放在展开项中保留审计能力。读取时动态生成展示字段，不改写 `_admin_audit_log` 历史记录；人员或账号已经删除时回退到当时保存的用户名。“触发在线数据同步”的结果读取时关联真实同步任务状态，不能永久停留在创建时的“等待处理”。
- 只有内部 `ops-agent` 可以访问 Docker Socket，主后端不能挂载它；容器名、数据库名和备份文件名必须使用固定白名单。
- 普通代码和前端更新按上面的默认同步规则执行；高风险服务器操作不能据此自动扩大范围。
- 当前还有哪些问题没有解决，见 [风险清单](docs/known-risks.md)。

## 完成任务前要检查

- 开工前先确定目标、改动范围、主要风险和验收证据；具体做法见 [高效开发与验证流程](docs/development-workflow.md)。
- 所有改动：运行 `git diff --check`，查看 `git status`，并扫描待提交文件中是否有敏感信息。
- 改了前端：在 `frontend/` 运行 `npm.cmd run build`。
- 改了 Python：至少做语法或导入检查；涉及数据库时只能使用测试库或只读检查。
- 改了 Compose：本机只做文件检查；服务器部署前运行 `docker compose config --quiet`。
- 改了文档：检查链接是否能打开，内容是否与代码一致。
- 某一层没有变化时不要重复检查；复杂的远程 SQL 或命令必须提前写成脚本，同一种验证方式连续失败两次就停止临时拼命令。

不要只说“应该没问题”或“100% 确定”：

- 能检查的就实际检查。
- 不能检查的要说清楚原因。
- 如果还可能造成数据丢失、密码泄露或服务中断，先停下来询问用户。

## 写文档和汇报的方式

- 先说结果，再说原因和步骤。
- 使用日常中文，少用专业词；必须使用时，第一次出现就解释。
- 给项目管理人看的内容放前面，技术细节放后面。
- 不复制容易过期的源码行号和大段文件清单。
- 架构、流程或风险发生变化时，同一个任务里更新对应文档。

_最后核对：2026-08-31_
