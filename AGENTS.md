# AGENTS.md — 滨湖智慧平台协作规则

本文件是仓库内所有开发代理的共享入口，必须随 Git 同步。它只记录稳定约束、验证规则和文档入口，不保存密码、令牌、生产地址或个人电脑路径。

## 1. 证据优先协议

- 不以“感觉正确”或字面上的“100% 确定”代替证据。
- 开始高风险操作前，列出主要失败模式和可能影响的数据、服务与人员。
- 能从源码、配置、测试、日志、接口或运行状态验证的事实必须先验证。
- 无法验证的事项要明确标记；若仍存在可能造成数据丢失、凭据泄露或生产中断的未知项，停止并请求确认。
- 完成声明必须附带与风险相称的证明；不要把计划、历史记录或推断表述为当前事实。

## 2. 真相来源与阅读顺序

按以下优先级判断项目事实：

1. 当前源码、配置、数据库结构与实时只读验证结果。
2. [项目文档索引](docs/README.md)及其指向的受 Git 管理文档。
3. `docs/archive/` 中的历史计划，仅用于追溯，禁止直接执行。
4. 本机 `AGENTS.local.md`，仅在用户明确要求服务器运维、凭据配置或故障恢复时读取；不得引用、提交或输出其中的敏感值。

接手任务时先阅读本文件，再按任务类型阅读架构、运维或风险文档。文档与源码冲突时，以验证后的源码和运行事实为准，并同步修正文档。

## 3. 项目定位与稳定约束

- 展示名：滨湖智慧平台；内部标识符：`binhu`；仓库目录名保持 `Bhzh`，不要因旧命名历史重命名目录。
- 技术栈：FastAPI、MySQL 8、React 18、Ant Design 6、Tailwind CSS 4、Vite 6、Docker Compose。
- 核心流程：腾讯文档表格 → 解析与去重 → 在线库 → 快照/归档 → 单日或区间统计 → 前端展示与可选回写。
- 数据库分为 `OnlineData`、`OnlineDataArchive`、`daily_report`；区间统计从 DWD 快照计算，禁止直接累加多日 DWS/ADS 日报。
- 解析器类型以 `backend/services/parsers/__init__.py` 的注册表为准；日报能力以 `backend/services/report_builders/__init__.py` 为准，不能从文件名或旧文档推断。
- 路由注册以 `backend/main.py` 为准；前端路由以 `frontend/src/App.tsx` 为准；不要维护容易漂移的行号和重复接口清单。
- 详细结构见 [架构说明](docs/architecture.md)。

## 4. 开发与分支规则

- `origin/main` 是两台开发电脑的唯一稳定主线；不要创建“家里”“单位”等长期地点分支。
- 每项功能、修复或文档工作使用短期分支。遵守执行环境要求的前缀；人工分支建议 `feat/`、`fix/`、`docs/`，Codex 分支使用 `codex/`。
- 开工前执行 `git switch main`、`git pull --ff-only`，再创建任务分支。
- 换电脑前必须提交并推送任务分支；跨电脑继续时先 `git fetch origin`，再跟踪或更新同一远端分支。不要依赖本地 `stash` 交接。
- 完成后通过 PR 或显式合并进入 `main`。禁止对共享分支强制推送，禁止用 `git reset --hard` 掩盖冲突。
- 具体命令见 [运维与开发手册](docs/operations.md)。

## 5. 安全与数据边界

- `.env`、`backend/.env`、`AGENTS.local.md` 及任何凭据文件不得加入 Git。
- 共享文档和示例只写环境变量名与占位符，不写真实主机、密码、令牌、OAuth 凭据或管理员口令。
- 数据库删除、卷重建、恢复导入、远程覆盖、部署和凭据轮换都属于高风险操作；先确认目标、备份、回滚路径和验证方法。
- 不得用 `docker compose down -v`、递归删除或清理卷作为普通排障步骤。
- 远程部署必须由用户明确要求；部署前备份，部署后验证健康接口、容器状态、端口、日志、鉴权和关键数据。
- 当前未解决问题见 [风险登记](docs/known-risks.md)。登记中的风险在完成验证前不得标记为已解决。

## 6. 最低验证要求

根据改动范围执行最小充分验证：

- 所有改动：`git diff --check`，检查工作树与暂存范围，扫描待提交文件中的敏感信息。
- 前端：在 `frontend/` 执行 `npm.cmd run build`；有相关测试时一并运行。
- Python：在可用环境中做语法/导入检查；涉及数据库逻辑时必须使用测试库或只读查询，不能拿生产库试错。
- Compose：具备 Docker 环境时执行带占位环境变量的 `docker compose config --quiet`。
- 文档：检查内部链接、源码入口、归档标记和凭据扫描。
- 不能执行某项验证时，必须说明缺少的运行时或外部条件，不能暗示已经端到端验证。

## 7. 文档维护规则

- 架构边界、开发流程、安全结论或已知风险发生变化时，同一任务内更新相应受 Git 管理文档。
- `AGENTS.md` 保持精简；可从源码推导的目录清单、接口细节和历史命令放到专门文档或直接引用源码入口。
- 运行状态必须注明验证日期与证据；过期状态不得继续写成当前事实。
- 历史计划只能移动到 `docs/archive/`，不得作为现行操作手册重新引用。

_最后核对：2026-07-26_
