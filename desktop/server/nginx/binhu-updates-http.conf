server {
    listen 80;
    listen [::]:80;
    server_name 47.100.44.36;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/binhu-acme;
        default_type text/plain;
    }

    location / { return 404; }
}
