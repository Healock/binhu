# 开发与运维手册

## 全链条已登记归档改用居住证自动确认（待合并）

- 数据上传中心不再提供公安网原始数据上传。旧预览和确认接口返回 HTTP 410；历史上传列表、受控文件下载、历史表和 HMAC 摘要继续只读保留，不能在本次发布中删除。
- 全链条“已登记”只有在居住证双周期唯一精确匹配、腾讯写回明确成功并记录 `confirmed_at` 后，完整保留 24 小时才进入归档候选。旧“已登记”没有确认链时保持不可选，禁止批量补造确认时间。
- 发布会给 `OnlineData._fullchain_archive_export_items` 幂等增加登记确认冻结字段。建议使用 `backup_scope=online / release_scope=full`；如果同批发布还修改其他业务库，则把备份范围提升为 `all`。
- 上线后先只读核对新增列、历史公安网文件数量和旧已登记不可选状态；随后使用一条自然产生的待登记任务完成“两轮匹配—腾讯写回—等待完整 24 小时—候选—导出归档”验收，不制造人员测试数据或人工缩短确认时间。
- 归档任务显示 `registration_archive_evidence_changed` 时说明腾讯删除前登记、房屋或来源证据发生变化，应修正当前业务状态后重新生成批次；不得手工修改归档条目或强制重放腾讯删除。
- 回退程序时保留新增列、历史文件和已经冻结的归档条目。已发出腾讯删除的条目继续按既有幂等对账恢复，不能因回退而再次按旧物理行删除。

## 0.25.17 客户端与服务端版本显示统一（待合并）

- 根目录 `VERSION` 继续作为唯一版本源，并纳入 `frontend/package.json`、前端锁文件、Electron、Tauri、Velopack 和安装器版本同步检查；本版版本为 `0.25.17`。
- 登录页和电脑端侧栏显示本地构建的客户端版本，不再用 `/api/app/bootstrap` 返回的旧服务端版本覆盖界面。服务端版本仍保留在认证上下文中供兼容性判断和诊断使用。
- 正式发布必须从同一提交构建并部署后端和桌面客户端，验收时确认登录页、侧栏、健康接口、客户端更新状态均为同一版本；若发现客户端与服务端暂时不一致，应先完成服务器切换，不应重新制作客户端包。
- 本版不修改业务接口、认证协议、Cookie 策略或双端会话逻辑。发布使用 `backup_scope=none / release_scope=auto`；合并后再按新的桌面发布门禁决定是否构建更新包和创建 `v0.25.17` 标签。

## 0.25.14 桌面客户端 CORS 与跨来源 Cookie

- 生产后端明确允许 `http://tauri.localhost`、`https://tauri.localhost` 和 `binhu://app`，并允许 `X-Binhu-Device-Id` 参与预检。不得使用 `*` 扩大来源。
- Compose 默认传入 `BINHU_SESSION_COOKIE_SECURE=true`、`BINHU_SESSION_COOKIE_SAMESITE=none`、`BINHU_MULTI_DEVICE_SESSION_ENABLED=true` 和三个桌面来源；生产 `.env` 如覆盖这些值，必须保持相同安全语义。
- 本版不修改数据库和业务数据，发布使用 `backup_scope=none / release_scope=full`。Compose 发生变化，不能只发布后端精简包。
- 上线后分别以三个来源对 `/api/auth/login` 发送预检：应返回 HTTP 200、对应的具体 `Access-Control-Allow-Origin`、`Access-Control-Allow-Credentials: true`，且允许 `X-Binhu-Device-Id`。
- 登录响应的 Cookie 应包含 `HttpOnly`、`Secure`、`SameSite=None` 和 `Path=/`。最终仍需真实 Tauri 客户端验证登录、刷新、重启、退出及手机/电脑双端同时在线；自动化预检不能替代 WebView2 的实际 Cookie 行为。
- 回退程序时恢复上一版完整发布包；若只需紧急停止桌面跨域访问，可从生产白名单移除桌面来源，但不得关闭 `Secure`、`HttpOnly` 或改用通配 CORS。

## 0.24.1 任务图预览口径

- `0.24.0` 首次生产预览只统计当前无法核实任务，漏报了已经形成最终结果但仍保存历史研判的完成链；`0.24.1` 将两类合并为预计依赖链，并单列历史研判链。
- 此修复不改变已经回填的节点和依赖。上线后重新执行只读预览，预计依赖链应与任务图唯一 `source_ref` 数一致；重复回填应为零变更。

## 0.24.0 个人任务依赖图

- 本版在 `WorkflowData` 幂等新增 `task_graph_nodes`、`task_graph_dependencies` 和 `task_graph_events`，只保存任务引用、状态、负责人/队列和安全事件摘要。
- 发布使用 `backup_scope=all / release_scope=auto`，任务图开关默认关闭。部署完成后先在超级管理员 Rete 页面执行只读预览，核对无法核实、已研判、核查人为空、账号映射异常和预计依赖链数量。
- 回填只写任务图三表，不修改核查结果、腾讯来源或已有工单。回填成功后再启用开关，并分别检查网格员和基础管控代入视角。
- 首次验收使用自然产生的数据：基础管控研判节点可处理，原核查任务在基础管控视角只读；网格员视角显示只读研判前置和等待协作的原任务，并确认原网格员仍可修改普通核查字段。不要制造测试居民数据。
- 回退程序时保留任务图表和事件即可；旧版本不会读取这些表。若需关闭功能，只关闭 `task_graph_enabled`，不得删除图表或篡改依赖状态。

## 全民防模型三反馈状态扫描（待合并）

- 本功能在 `OnlineData` 幂等新增 `_qmf_status_scan_runs`、`_qmf_status_scan_items` 和 `_qmf_status_snapshots`。旧版本不会读取这些表，程序回退时保留扫描历史即可，不需要删除表。
- 发布前建议使用 `backup_scope=all / release_scope=auto`。上线后先确认三张表已建立、每日扫描默认关闭、设置页时间为空，并检查全民防管理端只读配置仍可使用。
- 首次人工验收可启动一次全量扫描，观察四路进度、分类合计和服务日志；不要同时执行全民防真实登记，也不要通过中断生产服务主动制造恢复测试。
- 扫描失败只允许从页面重新发起新的人工批次。不得修改运行表、伪造快照或把“全民防无记录”解释成“未核查”。
- 开启每日扫描后按 `Asia/Shanghai` 执行；每日只创建一次增量批次。扫描缓存只供列表展示，真实登记继续实时预检测。

## 0.21.11 下发任务后台发布

- “发布所选”返回 `202` 和发布运行 ID，服务器后台继续执行；页面显示处理总数、成功、冲突、待对账和可重试数量。用户离开页面不会取消发布。
- 本版在 `DispatchData` 新增 `_police_dispatch_publish_runs` 与 `_police_dispatch_publish_run_items`，发布应使用 `backup_scope=all / release_scope=auto`。旧版本不读取这两张表，程序回退不要求删除表。
- 上线后只使用自然待发布数据验收：选中少量任务，确认请求立即返回、进度持续更新、页面重进可恢复，并核对腾讯表没有重复行。不得通过主动中断生产服务制造结果不确定测试。
- 服务重启恢复只释放从未发送的任务；可能已发送的分块进入同步对账。上线后若存在遗留运行，应检查“待对账”而不是反复点击重试。

## 0.21.9 全民防社区代码

- 本版首次部署启动时会执行一次带完成标记的迁移，向 `PlatformData._communities.qmf_community_code` 补入12个已确认的苏州居住证平台社区代码；仅更新当时的空值，不覆盖社区管理中已有的非空设置。迁移完成后再次启动只读取完成标记，不再补值；管理员以后清空或修改代码也不会被启动过程恢复。
- 社区代码合同调整为10位大写字母/数字混合码。社区管理输入会自动转为大写，离开不返吴反馈沿用同一校验规则。
- 发布包含生产数据补值，主开发如决定上线应使用 `backup_scope=all / release_scope=auto`。上线后只读核对12个启用社区均有代码且与清单一致，不主动执行全民防反馈测试。

## 0.21.8 全民防反馈状态复核

- 本版复用生产已有的 `VISIT_SOURCE_BASE_URL`、`VISIT_SOURCE_USERNAME`、`VISIT_SOURCE_PASSWORD` 或 `VISIT_SOURCE_AUTHORIZATION`，只读访问全民防管理端登录和模型三状态查询。不得在全民防设置页重复保存这组管理端凭据。
- 任务详情会显示全民防未反馈、已反馈一致、结果冲突或状态无法确认。已反馈一致时不再生成登记准备；结果冲突和查询故障也会阻断，以避免重复登记。
- 发布不新增数据库表或字段，若主开发在 #263 后决定上线，可使用 `backup_scope=none / release_scope=auto`。上线后只用一条已知手工完成任务和一条未完成自然任务做只读核对，不执行批量查询或制造真实反馈。
- 本功能分支基于并依赖 PR #263，必须按 `#263 → 本 PR` 顺序合并。本 PR 只由副开发提交，不合并、不部署、不创建或推送标签。

## 0.21.7 全民防更多核查结果

- 发布会向 `PlatformData._communities` 增加可空 `qmf_community_code`，并创建、全量导入 `_administrative_areas`，因此主开发如决定上线应使用 `backup_scope=all`。
- 上线后先确认行政区划导入行数与随包 CSV 一致，并核对社区管理已按2026-08-17确认的12社区清单补入10位大写字母/数字全民防代码。迁移只补空值，不覆盖管理员已有设置；未填写的社区不会执行“离开不返吴”反馈。
- 首次业务验收只选一条自然任务，先核对页面显示的正式社区、10位社区代码、六位去往行政区划和中文去往地址；不要制造批量任务或重复反馈。
- 本功能分支只创建 PR，不由副开发合并、部署或推送版本标签。

## 0.21.6 全民防登记前只读超时修复

- 登录和登记前只读查询遇到明确的传输故障时会自动重试一次；只读 HTTP 502、503、504 也可重试一次。HTTP 500、业务错误、结构错误及四个真实写接口均不自动重试。
- 若准备仍失败，在操作记录中查看安全步骤 `login/query_task/query_person/query_photo/precheck`、HTTP 状态和传输分类。日志不会保存上游 URL、响应正文、身份证号、手机号、地址或照片。
- 本版不修改数据库结构，发布使用 `backup_scope=none / release_scope=auto`。上线后检查健康版本和近期全民防失败审计，不主动执行真实登记；由项目管理人再次点击原自然任务的“全民防登记”完成准备窗口验收。

## 0.21.5 全民防恢复按钮显示兼容

- 如果详情页已经是 `v0.21.5`，但旧运行接口没有返回正确的 `can_reprepare`，页面会依据第 5–8 步实际状态显示“重新核对并准备”。这只是恢复入口，不会绕过后端检查。
- 点击后若后端发现任一步已经写入，会返回冻结提示并保持原状态；不会自动重试全民防写入。

## 0.21.4 全民防旧运行状态兼容恢复

- v0.21.3 上线前已经存在的运行，如果状态显示为“已停止并冻结”但第 5–8 步仍全部是“等待”，新版会显示“重新核对并准备”。这只重新执行四步登记前读取，不会重放任何全民防写接口。
- 只要第 5–8 步任一步已有发送、成功或不确定状态，仍保持冻结；不能用本兼容规则绕过人工核查。

## 0.21.3 全民防登记前来源竞态修复

- 本版修复在线来源同步最终投影重建的瞬时空窗。来源行真实存在但投影正在刷新时，不再误报为“来源已删除”；最终重建在单个事务中发布，登记前校验只做有限的本地一致性复核。
- 明确在四个写步骤开始前失败的运行可显示“重新核对并准备”。重新准备会再次读取任务、人员、照片和 `checkCk`；任何写步骤曾进入发送、成功或不确定状态的运行仍保持冻结。
- 受限核对页把性别、民族、文化程度和婚姻代码翻译为中文；未知代码明确提示待确认。出生日期缺失时仅从通过完整校验的身份证号提取，并标记来源。写接口继续使用原始代码。
- 不再要求等待预演冷却，也不再输入固定确认短语；同一时刻单任务限制、数据库执行锁、准备有效期、最终二次确认和失败冻结均保留。
- 本次生产故障的安全核查确认四个写步骤均未开始。发布后应从原任务点击“重新核对并准备”，重新核对完整资料和照片后再决定是否执行；不得手工修改运行表或直接跳过准备。
- 本版不修改数据库结构，发布范围由主开发按生产流程决定。副开发 PR 不执行真实登记、不部署、不创建标签。

## 0.21.2 全民防居住证按钮照片链路

- 本版把登记前照片读取从页面自动预取 GET 切换为 APP 实际“从居住证获取人像照片”按钮的 POST 接口。请求使用本次人员查询返回并再次核对的 `personID`，同时提交 `source=android`；响应按 JSON 业务码和 `data` Base64 解析。
- 本版不修改数据库结构，也不改变四个全民防写接口、九步顺序、一次性准备、二次确认或失败冻结。发布使用 `backup_scope=none / release_scope=auto`。
- 上线后必须先关闭全民防 APP 的同账号会话，使用此前只在旧 GET 返回 500 的同一授权任务重新打开“全民防登记”，确认能够显示正确人像和登记前资料。先只核对准备窗口；项目管理人明确确认前不要点击最终执行。
- 若新接口失败，只记录安全步骤和状态码，不导出响应正文。立即停止验收，不回退旧 GET、不跳过照片、不重放任何写接口。

## 0.21.1 WARN 降噪与照片写回队列

- 启动初始化阶段只过滤明确可接受的 `Table already exists` 和幂等 `Duplicate entry` 警告；未知表、SQL 错误、字段截断和同步错误仍会进入日志。
- 工单提醒使用无副作用的 `ON DUPLICATE KEY UPDATE`，重复扫描不会持续刷重复键 WARN。
- 照片名单维护与工单提醒由两个独立后台循环执行。腾讯读取或 G 列写回超时不会拖延逾期提醒。
- 照片名单写回失败使用 5 分钟起步、最多 6 小时的指数退避，连续 12 次自动失败后进入“已暂停”，不再无限重试。管理员可在工单流程配置的“同步记录与异常 → 待写回队列”手动恢复，恢复会清零自动重试次数并记录操作。
- 每轮写回需要按 A–F 摘要重定位多条来源行时，完整名单只读取一次并在该轮内复用；读取本身失败时也只请求一次。升级时会把旧版本中已经重试 12 次以上的任务迁移为“已暂停”，不会继续消耗腾讯额度。
- 运维中心只显示待写回、重试中和已暂停数量，不显示工单正文、姓名、证件号、手机号或地址。

开始较大的功能、修复或数据库改动前，先看 [高效开发与验证流程](development-workflow.md)，提前确定检查范围，避免上线时临时追加和重复验证。

## 当前运行基线（2026-08-12）

- 当前准备发布版本为 `0.19.14`；实际运行版本仍应以健康接口为准。八库分域迁移记录继续按 `0.16.0` 保留，不能把本地构建结果直接当作生产迁移完成。
- 正式发布统一使用 GitHub Actions 的 `Deploy production` 工作流；开发电脑不再逐文件上传程序。
- 生产服务由 `binhu-backend`、`binhu-mysql` 和 `binhu-ops-agent` 三个容器组成。后端只监听服务器本机，公网统一经过 nginx HTTPS。
- `binhu-backend` 单独配置 `119.29.29.29` 和 `223.5.5.5` 两个 DNS 上游，避免腾讯文档同步依赖单一运营商 DNS；不修改服务器系统 DNS。部署后用容器内解析检查确认 `docs.qq.com` 可解析。
- 所有登录用户访问 `/` 先进入仪表盘；在线数据汇总使用 `/summary`；电脑端 `/query` 为 Univer 在线工作表，手机端按岗位转入相应任务工作台。
- 数据库使用同一个 MySQL 实例，目标域划分为 `PlatformData`、`OnlineData`、`OnlineDataArchive`、`daily_report`、`VisitData`、`DispatchData`、`RegistryData` 和 `WorkflowData`。截至 2026-08-08，走访、下发、小区地址和平台基础域已完成迁移并切换；日报、辖区档案/人员标签新功能和工单域仍按维护窗口推进。本地开发电脑没有项目 MySQL 或 Docker 运行环境。
- 2026-08-06 完成一次容量整理：磁盘使用率从 96% 降至 83%，保留当前版和上一版后端镜像，数据库卷、每日备份、发布源码留档及自动发布记录均未删除。
- GitHub 旧的已合并功能分支已经清理，目前远端只保留 `main`。`main` 尚未启用 GitHub 分支保护，因此仍必须人为坚持“功能分支 → PR → CI → 合并”的流程。
- 生产业务已经运行在物理服务器；旧云的平台容器和 HTTPS 透明代理均已于 2026年8月9日退役。旧云只保留原数据库、卷、程序和历史备份作为人工回退材料，WireGuard 与异地备份接收能力也已停用；主机 Nginx 继续服务旧云上与本平台无关的站点。

版本号、容量和容器状态会变化。执行生产操作前仍要以健康接口、GitHub 工作流和服务器只读检查为准，不能只照抄本节数值。

## 最常用：两台电脑怎么开发

记住三句话：

1. 开工前先拉取。
2. 换电脑前先提交并推送。
3. 功能完成后才合并到 `main`。

### 开始一个新功能

不管在家里还是单位，都先更新正式代码：

```powershell
git switch main
git pull --ff-only
```

然后建立一个功能分支。项目管理人手工开发可以使用 `feat/`，Codex 创建的分支使用 `codex/`：

```powershell
$branchName = "feat/example-topic"
git switch -c $branchName
git push -u origin $branchName
```

把 `example-topic` 换成简短的功能名称。不要建立“家里分支”或“单位分支”。

### 准备换电脑

离开当前电脑前，把进度保存到 GitHub：

```powershell
git status
git add -- path/to/reviewed-file
git commit -m "开发进度：简单说明做了什么"
git push
```

功能没写完也可以提交到功能分支，只要暂时不合并进 `main`。

### 在另一台电脑继续

这台电脑第一次使用该分支：

```powershell
git fetch origin
git switch --track origin/feat/example-topic
```

这台电脑已经有该分支：

```powershell
git switch feat/example-topic
git pull --ff-only
```

### 功能完成

检查通过后推送最后一次修改，在 GitHub 创建 Pull Request，然后合并到 `main`。

合并时删除对应远端功能分支；不要删除仍有开放 PR、尚未合并或需要跨电脑继续开发的分支。正式版本还要继续执行文末的 [正式部署入口](#正式部署入口)，普通 `git push` 和 PR 合并本身不会更新生产服务器。

合并后，两台电脑分别运行：

```powershell
git switch main
git pull --ff-only
```

`.env`、`backend/.env` 和 `AGENTS.local.md` 不会通过 GitHub 同步，两台电脑要分别配置。

### GitHub 分支收尾

每次 PR 合并后应删除该功能分支，并在两台电脑同步远端状态：

```powershell
git switch main
git pull --ff-only
git fetch origin --prune
```

批量清理远端分支前必须同时满足：已经合并进 `origin/main`、没有开放 PR、不是 `main`、不是保护分支。删除分支不会删除已进入 `main`、PR 或版本标签的提交，但没有合并且没有标签的独立提交可能随分支删除而失去正常入口。

建议在 GitHub 为 `main` 开启分支保护，至少要求通过 PR、后端检查和前端检查后才能合并。启用以前要先核对工作流中的真实检查名称，避免把 `main` 锁死；启用后也不能允许普通发布流程绕过检查直接推送。

## 第一次配置电脑

复制项目配置模板：

```powershell
Copy-Item .env.example .env
```

然后编辑 `.env`，把所有 `replace-with-*` 示例值换成本机使用的值。不要把真实密码发到聊天或提交到 Git。

如果使用 Docker，可以检查配置：

```powershell
docker compose config --quiet
```

如果直接启动后端，还要在 `backend/` 中建立自己的 `.env`。字段示例：

```dotenv
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=binhu
MYSQL_PASSWORD=<本机数据库密码>
MYSQL_ONLINE_DATA_DB=OnlineData
MYSQL_ARCHIVE_DB=OnlineDataArchive
MYSQL_DAILY_REPORT_DB=daily_report
ENCRYPTION_KEY=<本机应用密钥>
```

## 本地启动

前端：

```powershell
Set-Location frontend
npm install
npm.cmd run dev
```

前端修改完成后检查生产构建：

```powershell
npm.cmd run build
```

后端：

```powershell
Set-Location backend
python -m pip install -r requirements.txt
python -m uvicorn main:app --reload --port 8000
```

后端启动前需要三个可用的 MySQL 数据库，具体用途见 [架构说明](architecture.md)。

## 工作日志日报

管理员和超级管理员可以在“工作台 → 文件生成”的“工作日志”Panel 选择业务日期并创建日报草稿。周报和月报在收到对应模板前保持禁用；“工作每日明细”使用另一个独立 Panel，不创建或修改草稿。

- 同一天只有一份日报草稿。创建者编辑，其他管理员可以查看、导出或主动接管。
- “草稿管理”集中显示全部日报草稿，并支持按日期、创建人或当前编辑人查找。管理员和超级管理员都可以删除草稿，删除前必须二次确认；删除不可恢复，但同一天可以重新创建。删除不会影响在线数据、走访数据或统计报表，并会写入超管操作记录。
- 页面约 1 秒自动保存。出现“保存冲突”时应重新读取草稿，不能反复覆盖。
- 创建草稿后系统统计不会自行变化；确需更新时点击“刷新系统数据”，人工填写和人工覆盖会保留。
- 工作日志中的“网格员数”由人员管理、统计岗位、请假和双休日备勤共同计算。已有草稿点击“刷新系统数据”后会补齐尚未填写的人数；已经人工修改的数字不会被覆盖。周末排班没完成时人数保持空白，应先完成人员管理中的排班。
- 导出前会列出尚未填写的字段。确认后可以带空白继续导出。
- 导出的 PDF 不保存在服务器，草稿保存在 `OnlineData._work_log_drafts`。
- 业务日期为 7 月 14 日时，正文和系统取数都使用 7 月 14 日，文件名以 `0714日报` 开头，落款为 7 月 15 日。

PDF 版式由工作日志结构和后端打印样式共同生成。旧 `.doc`、`.docx` 样例只能在本机参考，不能复制进仓库。发布前要检查 PDF 可以打开、中文正常、十六张表格没有截断、没有未替换占位符，也不能出现旧样例中的姓名、身份证号、电话号码或警情文字。

`0.8.0` 使用 `daily-v2` 表单结构。访问旧版草稿时先把原始 JSON 完整保存到新快照的 `legacy_v1` 区域，再映射能够识别的字段，不删除旧内容。部署前备份 `OnlineData` 并保留旧后端镜像和前端；部署后检查页面权限、旧草稿升级、自动保存冲突、PDF 下载、容器日志和 `/api/health` 版本。

`0.8.1` 只调整 PDF 公文版式，不修改草稿字段或数据库。发布时保留旧后端镜像即可；验收重点是红色标题和分隔线、基础数据与章节顺序、正文层级、问题分析颜色、表格分页和中文字体。

`0.8.2` 调整叮咛行动、矛盾纠纷和出租房责任落实文字，并把自购房走访人员汇总接入工作日志
系统快照。新建草稿会自动带入自购房数据；已经存在的草稿要由当前编辑人点击“刷新系统数据”
后才会读取，避免后台数据悄悄改变旧草稿。出租房屋核查同步同时允许把同一身份证号和手机号的
重复行合并为一个核查对象；两个身份字段都为空时仍停止同步，不猜测是否同一人。

`0.8.3` 在社区管理中增加社区民警名单，一个社区可以填写多位。新建工作日志草稿会把姓名带入
全部按社区生成的表格，多位民警用“、”连接。升级只给 `_communities` 增加可空的
`police_officers` 字段，旧版本会忽略该字段；上线前按兼容数据库变更流程备份三库和旧程序。

`0.8.4` 累积界面细节调整，增加工作日志草稿管理，以及面向全员的公告和个人提示。草稿删除只
移除被明确选中的记录；公告升级会新增两个兼容表。发布前应检查草稿筛选、指定日期打开、删除确认、
同日重建、普通账号查看公告、个人提示隔离、公告独立已读和超管发布删除权限。

## 历史版本上线记录

下面以版本号开头的段落用于解释既有数据库结构、兼容边界和回退证据，不是当前版本的日常部署命令。当前发布一律使用文末的固定 GitHub 工作流；只有处理对应历史结构或回退时才查阅这些记录。

## 0.9.0 权限和账号名单上线

`0.9.0` 增加部门、权限组、账号关联、唯一登录和空闲退出。数据库变更都采用新增表、可空字段和
兼容字段，旧程序会忽略它们；但正式启用社区范围以前，必须先完成名单预览。

固定上线顺序：

1. 备份 `OnlineData`、旧后端镜像和旧前端，并把旧账号的角色、人员关联和有效会话清单保存到私密回退材料中。
2. 部署新程序，让后端只创建兼容结构。此时 `permission_enforcement_enabled` 仍为 `0`，未关联的旧低权限账号继续沿用原查看能力。
3. 把名单临时放入后端容器，只运行预览。预览必须逐行显示用户名、姓名、所属部门、岗位、默认权限组和“新增或跳过”。
4. 只要出现重复用户名、重复人员、人员姓名不唯一、部门不符合岗位、未知名单职位或岗位没有默认组，整批停止，不写入数据库。
5. 项目管理人确认预览无误后，才正式创建账号、发布初始密码公告并启用新权限。
6. 分别使用流口岗、片长、基础管控、管理员和超级管理员账号检查，再创建 `v0.9.0` 标签。

名单文件至少包含“用户名”“姓名”和“职位”，属于私密文件，不能复制到仓库、提交 Git 或写进共享文档。
已存在的用户名整行跳过，不补人员关联、不改权限且不重置密码。姓名能与人员管理唯一匹配时，
继续使用该人员的岗位默认组；未匹配人员只允许按以下固定规则处理：

- “社区民警”和“所（队）领导”创建为不关联人员资料的管理员账号。
- “内勤岗”创建为不关联人员资料的内勤业务组账号。
- “流口岗”先创建“组员、待分配部门”的人员记录并关联账号，之后由超级管理员在人员管理中分配社区。

手机号和身份证号没有参与当时的账号导入。一次性名单工具已在导入和核验完成后从当前源码移除；
历史实现可从 `v0.9.0`、`v0.9.1` 标签追溯，但不得直接复制到当前生产环境执行。以后再次批量建号时，
应针对新名单重新制作只预览、整批校验、密码脱敏和事务回滚都可验证的短期工具。

上线后固定检查：

- 旧超级管理员仍可登录，且至少保留一个超级管理员。
- 流口岗只能看到业务记录所属社区的数据，社区别名可以正确归入正式社区；无社区部门时显示空数据说明。
- 片长只能全局查看；中队长和基础管控可以同步、上传、排班、请假和管理工作日志，但不能修改人员或社区。
- 管理员可以修改人员和社区；超级管理员可以管理账号、权限组和岗位映射。
- 在另一浏览器登录同一账号后，旧会话显示“账号已在另一台设备登录”；同一浏览器多个标签页保持正常。
- 默认空闲 30 分钟，到期前 2 分钟提示；后台轮询、自动保存和文字输入不续期，“继续使用”、主动查询和提交会续期。

权限组调整会在账号下一次请求时生效。若上线检查失败，先切回旧程序；恢复账号或会话快照会
覆盖当前状态，属于高风险操作，必须另行确认，不能在普通回退中自动执行。

## 0.9.1 账号姓名和个人中心上线

`0.9.1` 在 `_users` 增加兼容字段 `display_name`。用户名继续只用于登录，显示姓名只用于页面展示；
旧程序会忽略该字段，可以直接回退程序。当时的私密名单姓名回填已经完成，一次性工具不再保留；
历史实现可从 `v0.9.1` 标签审计。上线时已检查健康接口版本、个人中心、用户管理姓名列、普通账号
改密和近期后端日志。

## 0.9.2 多权限组和在线汇总固定口径上线

`0.9.2` 新增 `_position_permission_group_links` 和 `_user_permission_group_links`，分别保存岗位和账号的
多个权限组。后端启动时会把旧单权限组关系写入新表；旧 `_position_permission_groups` 和
`_users.permission_group_id` 继续保存排序后的第一个组，所以旧程序可以直接忽略新表并回退。

本版同时改变在线汇总展示口径：单日、区间、总汇总、概览和工作日志自动取数固定只保留人员管理中
有有效社区部门的组长、组员。旧日报表和原始业务数据不重写，查询时动态过滤并重算社区合计。
日报不再有单独生成入口，只在手动或自动同步的末尾按“分汇总 → 总汇总”生成。

上线前固定准备：

1. 备份 `OnlineData` 和 `daily_report`；`OnlineDataArchive` 本版不变，不重复备份。
2. 保留旧前后端镜像，并确认准备上线的提交、`VERSION` 和前端产物属于同一版本。
3. 在服务器运行 `docker compose config --quiet`，再构建和切换容器。
4. 后端启动后只读核对两张新关联表：旧自定义账号和岗位的单组关系都已迁移，关联表中没有重复主键。
5. 使用普通组员确认可以读取同步状态和所属社区汇总、不能发起同步；使用多权限组测试账号确认逐权限数据范围没有交叉越权。
6. 检查在线汇总中没有名册外人员、非组长组员或未分配社区人员；重新同步一次后确认日报和总汇总自动生成。
7. 检查人员三段列表、社区民警部门校验、所队领导内勤归属，以及排班提示不会列出业务当天之后的周末。
8. 健康接口返回 `0.9.2`、容器和近期错误日志正常后，才创建并推送 `v0.9.2` 标签。

本版不补写任何历史或未来周末排班。若上线检查失败，先切回旧镜像；新关联表是兼容新增结构，
无需删除。恢复数据库备份会覆盖上线后的账号或权限调整，仍属于高风险操作，必须另行确认。

## 0.9.3 多社区人员和账号强关联上线

`0.9.3` 新增 `_grid_member_department_links` 保存人员的完整部门关系。组长、组员仍只有一个社区；
社区民警至少一个社区并可多选。旧 `department_id`、`community` 和社区表中的民警 JSON 继续同步
第一项或完整姓名镜像，供旧程序回退，但新程序不再把它们作为权威来源。本版还统一了 Axios 与
浏览器 `fetch` 的会话失效处理。

固定上线顺序：

1. 完整备份 `OnlineData`、`OnlineDataArchive` 和 `daily_report`，验证 gzip/SQL 可读且文件非空；保留旧前后端镜像。
2. 确认待部署提交、`VERSION` 和前端产物一致，在服务器运行 `docker compose config --quiet` 后再构建并切换程序。
3. 后端启动后只读核对关联表已从旧主部门回填，非社区民警没有多条部门关系，人员和账号唯一索引正常。
4. 当时使用的私密名单和岗位覆盖文件只在服务器临时使用，未进入 Git 或共享日志。
5. 迁移结果为 17 人：社区民警 13 人、所队领导 2 人、基础管控 2 人，其中多社区民警 2 人。
6. 一次性迁移工具已经在结果核验后移除；历史实现可从 `v0.9.3` 标签审计，不得当作日常运维命令复用。

上线后固定验证：每名业务人员
恰好一个账号；允许保留的系统、公共和超级管理员账号可以无人员；两名多社区民警各有两条唯一
关系；三类新增人员继承正确岗位权限；社区管理、工作日志民警姓名和多社区数据范围一致；会话
被顶替与空闲超时都能保存退出原因并返回登录页；健康接口为 `0.9.3` 且近期日志无新增错误。

失败时先回退旧前后端镜像。新关联表和兼容字段保留不删除；恢复数据库备份会覆盖上线后的人员、
账号或权限修改，属于高风险操作，不能自动执行。

## 0.9.4 人员账号改绑与身份证权限上线

`0.9.4` 不新增数据库表或字段，但会增加人员账号改绑和身份证号人工修正写入。上线前备份
`OnlineData`，并保留旧前后端镜像；另外两个数据库不受影响，无需为本次版本重复备份。

部署前确认待发布提交、根目录 `VERSION` 与前端产物都为 `0.9.4`，在服务器执行
`docker compose config --quiet` 后再构建和切换。上线后只做只读验证：健康接口返回 `0.9.4`、
人员与账号仍保持一对一、普通管理员的人员接口没有任何身份证字段、超级管理员响应才包含完整
号码字段、账号选项不包含超级管理员账号，且近期日志没有新增错误。本次验证不得在生产库实际
互换人员账号或修改真实身份证号。

回退时切回旧前后端镜像。`0.9.4` 没有结构迁移，旧程序可以直接读取现有数据；只有确实发生错误
写入且项目管理人另行确认后才允许恢复 `OnlineData` 备份，因为恢复会覆盖上线后的正常账号和
人员资料变更。

## 0.10.0 在线查询与腾讯安全回写上线

`0.10.0` 新增片区、腾讯来源定位、业务投影和 90 天完整回写审计。旧程序会忽略这些新增表和
`_communities.area_id` 可空字段，因此程序回退不要求删除新结构。在线回写配置首次创建时默认关闭，
只有完成只读核验后才启用。

固定上线顺序：

1. 完整备份 `OnlineData`、`OnlineDataArchive` 和 `daily_report`，检查文件非空、gzip/SQL 可读，并保留旧前后端镜像。
2. 确认待发布提交、根目录 `VERSION` 和前端产物均为 `0.10.0`；在服务器运行 `docker compose config --quiet`，再构建并切换程序。
3. 保持 `online_writeback_enabled=0`，只读核对 `_areas`、`_area_leader_links`、`_online_source_rows`、`_online_source_projection`、`_online_source_cache_state` 和 `_online_writeback_audit` 已创建，且东片、中片、西片及十二个社区映射正确。
4. 等待或执行一次正常同步建立来源定位；核对每个启用来源表的缓存行数、物理位置唯一约束、业务父行数量、重复来源数量和待同步数量。同步本身会照常更新业务库、快照和日报，不要为了测试回写制造业务数据。
5. 分别核对组员、片长、基础管控和超级管理员的查询范围、可编辑字段和新增删除按钮；归档、尚未定位的来源和回写开关关闭状态必须只读。
6. 通过系统设置启用在线回写，确认查询接口返回开关已开启，但 Agent 不对腾讯测试表或业务表执行新增、修改或删除。真实回写由项目管理人自行测试。
7. 检查健康接口为 `0.10.0`、容器正常且近期日志没有新增错误后，创建并推送 `v0.10.0` 标签。

异常时先在系统设置关闭在线回写，查询和同步仍可继续使用。程序问题则切回旧前后端镜像；新增表
和兼容字段可以保留。恢复三库备份会覆盖上线后的真实同步和业务操作，属于高风险操作，必须另行确认。
删除腾讯原始行没有自动恢复按钮；需要人工恢复时只能由超级管理员查看完整审计内容并复制旧值。

`0.10.2` 修正新增原始行：不再向腾讯批量更新接口发送未公开的插行请求，而是用官方区域更新
请求写入最后一条非空记录后的现有空行；同时检查 HTTP 状态和腾讯业务返回码，并在回读一致后才
向页面返回成功。部署后只做来源行、业务码错误日志、表头过滤和日期显示的只读核验，真实新增仍由
项目管理人操作。如果腾讯提示目标区域不存在，说明工作表已无现有空行，应先在腾讯表格中扩充行数，
再重新新增；平台不能绕过官方接口伪造插行。
同版同时按腾讯文件元数据中的 `rowTotal` 限制读取分页范围。若部署后的自动同步出现全部来源均为
`400001 / range invalid`，说明读取边界没有生效，应立即回退旧镜像，不能通过重新忽略全部业务码
来掩盖问题。

`0.11.0` 只替换电脑端查询组件：删除 AG Grid，改用按路由延迟加载的 Univer 工作表。后端接口、
数据库结构、权限硬限制、腾讯行哈希和审计规则均不变，因此部署不执行数据库迁移，也不需要为本次
纯前端替换单独备份三库；保留 `0.10.3` 前端产物即可快速回退。部署后检查普通页面不会提前下载
Univer 资源，进入在线数据查询后能显示行列坐标、冻结表头、蓝色可编辑格、下拉选项和自然空白行；
归档、重复父行和未授权格必须只读。Agent 不执行真实腾讯写入，项目管理人再人工验证单格修改、
多格粘贴、草稿提交、失败恢复和重复来源详情。

`0.11.3` 正式启用 Univer 经典工具栏和列筛选。筛选值列表来自当前页，但选定的值或条件会发送到
后端并对完整查询结果重新分页，不能只隐藏当前页行；尾部新增空白行不进入筛选范围。工具栏中的
临时格式不会回写腾讯表格，按颜色筛选也暂不开放。深色模式只向工作表写入一套源配色，由 Univer
统一转换画布颜色，避免单元格背景被二次转换回浅色。本版不改数据库结构或腾讯回写协议，部署时
保留 `0.11.2` 前端和旧后端镜像即可，不重复备份数据库；仍不得用腾讯业务表测试写入。

`0.12.5` 修复 Univer 全屏浮层：浏览器全屏目标从表格卡片改为文档根节点，表格卡片仍覆盖整个
视口，从而让筛选、填充颜色和工具栏菜单继续处于全屏文档内。本版只改前端和版本号，不修改数据库、
后端业务或腾讯回写协议；上线保留 `0.12.4` 前端与旧后端镜像，不重复备份数据库，也不执行腾讯写入测试。

`0.12.6` 为在线数据汇总和走访汇总增加 XLSX 导出。导出直接使用当前页面已经过权限过滤的结果；
电脑端存在表头筛选或排序时，只写入当前可见行并重新计算总计。每个文件包含人员（或网格员）汇总和
社区汇总两张工作表，数字、百分比和空白值保持可编辑的 Excel 类型。本版只改前端和版本号，不修改
数据库、后端业务或腾讯回写协议；上线保留 `0.12.5` 前端与旧后端镜像，不重复备份数据库。

`0.12.7` 统一两个汇总页的导出入口：在线数据汇总不再把按钮放在页面右上角，改为和走访汇总一样
放在业务类型、日期和查询操作所在的中部工具栏。本版只调整前端布局和版本号；上线保留 `0.12.6`
前端与旧后端镜像，不重复备份数据库。

`0.12.8` 修复走访汇总吸顶表头因长标题单独扩宽而与表体错位的问题，并把 Univer 的腾讯来源值
固定为文本单元格，避免 `7.30` 被显示成 `7.3`。在线数据查询的列宽改为按当前内容自动调整，日期、
姓名等短列会收紧，地址和反馈等长列设有最大宽度。本版只修改前端显示和版本号，不改变数据库、
统计口径或腾讯回写协议；上线保留 `0.12.7` 前端与旧后端镜像，不重复备份数据库。

`0.12.9` 紧急修复 `0.12.8` 的两项前端问题：Univer `0.25.x` 在工作簿初始化阶段调用内置自动列宽
会因撤销栈未就绪导致 `/query` 崩溃，现改为纯文本测宽后逐列设置；走访汇总不再让吸顶表头和表体
分别使用 `max-content` 猜测宽度，而是复用同一组列宽和数字横向总宽度，长表头在列内换行。本版不改
数据库、统计口径或腾讯回写协议；上线保留 `0.12.8` 前端与旧后端镜像，不重复备份数据库。

`0.12.10` 修复日期数值被错误补零的问题。腾讯 v3 范围读取接口没有格式化值参数，数值 `7.30`
和 `7.3` 都只返回 `7.3`；程序不再把所有一位小数补成两位，避免真实 `8.3` 显示为 `8.30`。
需要保留尾零的来源日期必须在腾讯表格中保存为文本，平台新增和回写仍使用文本。本版不改数据库
结构或腾讯写入协议；部署后需要经过一次正常同步刷新来源缓存，Agent 不在业务表执行写入测试。

## 0.12.0 流口岗手机任务工作台上线

`0.12.0` 只为组长、组员增加手机首页和五类任务卡片；桌面汇总、桌面 Univer 和其他岗位手机页面
保持原样。数据库只给 `_online_source_projection` 增加兼容的 `inspector`、`task_state` 字段和查询
索引，后端启动时从已有来源缓存回填，不访问或改写腾讯表格。旧程序会忽略这两个字段，可以直接
切回旧镜像而无需删除结构。

上线顺序：

1. 备份 `OnlineData`，确认备份非空且可读取；保留上一版前后端镜像。`OnlineDataArchive` 和 `daily_report` 本次没有结构写入，不重复备份。
2. 确认部署提交、根 `VERSION` 和前端产物都是 `0.12.0`，在服务器运行 `docker compose config --quiet` 后再构建并切换。
3. 后端启动后只读核对投影新增字段已建立；五类现有投影的核查人、任务状态和组合索引已从缓存回填，来源行数和父投影数没有被迁移改变。
4. 使用一名有社区的组员或组长检查手机 `/`、`/tasks`、本人/本社区切换、五类列表、搜索和详情；桌面 `/` 与 `/query` 仍分别显示原汇总和 Univer。
5. 核对其他社区任务不能通过详情地址读取；重复来源只显示当前社区的腾讯原始行。无当日快照时新下发、结转和完成显示未知，不得显示 0。
6. Agent 只检查回写开关、权限、来源版本和日志，不在腾讯业务表制造修改。项目管理人用现有任务人工保存一次，确认页面停留原详情、腾讯内容一致、审计只有一条且页面显示“汇总待同步”。
7. 健康接口返回 `0.12.0`、容器与近期日志正常后，再创建并推送 `v0.12.0` 标签。

若手机接口或回填异常，先关闭在线回写总开关并切回旧前后端镜像；新增兼容字段和索引保留即可。
恢复 `OnlineData` 会覆盖上线后的正常账号、配置和来源状态，属于高风险操作，不能作为普通回退步骤。

`0.12.1` 不修改数据库结构。手机任务中的电话号码先拆成独立选项，只有真实手机浏览器才启动
`tel:`；组长可以把任务转派给本社区在岗组员，组员不能转派，后端会校验岗位、社区和在岗状态。
部署前运行手机任务权限测试和前端测试；上线不制造腾讯测试数据，由项目管理人使用现有任务分别
验收多号码选择和一次真实转派。健康接口返回 `0.12.1`、容器及近期日志正常后创建同名标签。

本机没有项目 MySQL，自动化测试只能覆盖 SQL 分支、权限和请求结构，不能声明真实 MySQL 已验证。
服务器也不是集成测试环境；生产部署只在有效备份和回退材料齐全后执行兼容建表与只读核验。

## 走访明细导入

“数据工作台 → 走访汇总”支持管理员和超级管理员分别上传走访明细、星级评定 `.xlsx`：

- 单个文件不超过 20MB，最多处理 10 万条有效记录。
- 页面会显示现有数据的起止日期和无数据日期，上传重叠区间不会重复增加记录。
- 去重按“业务日期＋标准化地址＋网格员姓名”判断；同一网格员同日同地址只保留最新一条，不同网格员分别保留。
- 错误行会跳过，其他正确行继续入库；上传完成后可查看错误和提醒。
- 组长和组员只能查看数据范围，后端也会拒绝他们上传。
- 社区管理可以为正式社区维护多个别名。别名必须按来源数据中的完整文字填写，系统只清理首尾空格，不会自动删除“社区”或“村”；匹配成功后，走访汇总和在线数据的单日、区间、总汇总都会统一显示正式社区名称。例如正式名称“南厍”可以添加别名“南厍村”。原始在线表格和快照不改写，历史汇总在读取时按当前别名配置归并。
- 网格员跨社区走访属于正常情况，不会因为社区部门与走访社区不同而产生提醒。
- 星级评定按标准化地址关联采集时间前后 24 小时内最接近的走访，只更新 `t_visit_details`，不建立第二张星级业务表。
- 无法匹配或时间距离完全相同而无法判断的评定不会强行写入；页面会显示提醒。
- 页面会显示已星级评定数量，以及“仅入户走访、没有星级评定”的数量。
- 页面上方的全库概览显示数据库日期范围；查询后还会显示当前类别和日期区间的走访户数、
  参与人员、在岗人日、总变动数、星级评定和仅走访未评定。
- 页面可以按入户业务日期选择一天或一段时间，同时查看网格员汇总和社区汇总。
- 汇总类型可以选择“出租房”或“自购房”。出租房默认统计组长和组员，也可以由超级管理员
  修改岗位范围；自购房固定统计人员管理中的“自购房”岗位。
- 汇总中的社区按本次实际走访社区统计；同一网格员跨社区走访时会分成多行。
- 社区表显示在岗人日、人均日走访户数和人均日变动数。请假、长期离岗和未被排到的周末日期
  不进入分母；跨社区走访按当天各社区走访户数分配这 1 个人日。
- 如果页面提示某周没有完成双休日排班，先去“人员管理 → 双休日备勤”补齐该周。补齐以前，
  两个人均日指标显示为 `-`，系统不会用参与人数代替。
- 网格员表和社区表的后端结果包含整段日期的总计；网页使用表头筛选后，表底总计和标题行数
  会立即按当前显示内容重算。后续导出筛选结果时也要按导出内容重新计算，不能使用整表总计。

这一功能新增了三张表，并给网格员资料增加身份证号字段。第一阶段已于 2026-07-28
完成上线，上线时未导入测试走访数据。以后部署相关改动时仍要检查：

1. 确认待部署版本，并完成三个数据库的有效备份。
2. 检查 `backend/init.sql` 和 `backend/database.py` 中的结构一致。
3. 更新后端依赖并重建镜像，再确认新表和字段已经创建。
4. 使用自动化测试验证上传、重叠去重、权限和身份证号遮盖，不向生产库导入测试业务数据。
5. 检查原有业务表数量、登录、同步、日报和日志没有异常。

走访汇总和当前筛选结果的 XLSX 导出已经接入。

## 人员岗位和请假

“基础资料 → 人员管理”用于维护姓名、所属部门、岗位、电话和备注。已有人员升级后默认是“组员”，
避免升级后原来的汇总人员突然消失。

超级管理员可以在“设置 → 系统设置 → 岗位范围”分别配置在线汇总、出租房走访汇总和双休日备勤
涉及的岗位，默认都是“组长、组员”。自购房走访汇总固定使用“自购房”岗位。修改后重新查询或
重新打开排班页面即可看到新结果，不需要重建数据库。

人员请假使用列表中的独立“请假”按钮：

- 临时请假：选择开始和结束日期。
- 长期：不选择日期，恢复工作时点击“恢复正常”。
- 已经结束的日期可以用同一个按钮补录，补录不会改变当前状态。
- 编辑按钮只修改人员资料和岗位，不再修改请假状态。

“人员管理 → 双休日备勤”每周维护一次：

1. 选择要安排的周，通常先点“沿用上周”。
2. 电脑端可以拖动整张人员卡片到周六、周日区域；选错后可以批量“移回待安排”。
3. 手机端不使用拖动，直接点人员卡片上的“周六”“周日”或“双休”，已经安排的人也可以点“双休”移回第一栏。
4. 请假一天的人员只能安排另一天；两天都请假的人员自动免排。
5. 保存请求必须包含当周全部备勤人员；仍在第一栏的人保存后明确记为“周六、周日都休息”，不会继续被当成漏排。
6. 页面显示“已确认全部人员”后代表本周已经完整保存。历史周也可以重新选择并补录。

在周六或周日打开人员管理时，人员状态会同时读取本周备勤表：当天被排到备勤显示“今日备勤”，排在另一天或明确双休显示“休息”，当周排班尚未保存显示“未排班”。临时请假和长期离岗优先于备勤状态。工作日仍按正常在岗、请假和长期离岗规则显示。

这项功能新增 `_personnel_attendance_history` 和 `_personnel_weekend_duty`。旧字段继续保留供旧版本
回退；首次上线以前的请假历史无法自动还原，管理人可以按已知记录补录。部署时按兼容数据库
变更规则备份三个数据库和旧程序，并检查两张新表、周排班唯一约束和走访汇总的人日恒等关系。

业务数据中有姓名、但人员管理还没有登记的人会继续显示在汇总中。管理人确认身份后应及时把
该人员加入人员管理并设置正确岗位，之后系统才能按岗位配置准确判断是否纳入统计。

## 版本号与发布

项目使用 SemVer（语义化版本），根目录 `VERSION` 是唯一版本来源：

- 修复已有功能但不增加能力：提升最后一位，例如 `0.1.0` → `0.1.1`。
- 增加兼容的新功能：提升中间一位，例如 `0.1.1` → `0.2.0`。
- 出现不兼容改动：提升第一位。项目仍在早期开发阶段时，主版本可以继续保持 `0`。

前端侧栏和 `/api/health` 都会显示这个版本。先修改 `VERSION` 并完成测试、合并和上线验证，
确认服务器确实运行新版本后，才能创建并推送同名标签，例如 `v0.1.0`。不要提前给未上线的
提交打正式版本标签。

小型且不紧急的界面调整可以先集中在本机，不必每改一处就单独发布。用户明确说“先积攒”时，
停止提交、合并和上线；等用户指定版本后，再把这一批改动放进同一个发布分支，统一完成测试、
Pull Request、合并、部署和版本标签。这样可以减少频繁上线，同时仍保持每个正式版本可追查。

只修改共享文档、注释或开发说明，且不改变运行产物时，通常不提升 `VERSION`、不触发生产部署；仍应通过功能分支和 PR 合入，确保文档修改可审查。文档如果同时描述了尚未上线的程序行为，则必须明确标注“未上线”，不能提前把计划写成现状。

## 在线日报结转与历史回算

从 `0.2.0` 开始，日报会把前期未完成、当天仍在线的任务结转到当天。单日报表显示当天应处理的
总量；区间查询按业务类型和任务主键去重，只保留区间内最后状态。页面不会增加“结转数”列。

从 `0.3.0` 开始，页面在汇总表上方增加数据概览。概览会把任务分成结转、新下发和已有任务变化，
三项互不重复并与当前任务总数相等；“新下发”必须通过相邻快照判断，不能把已有任务的内容变化
误写成新下发。概览只读取现有任务流水和快照，不需要新增数据库字段或执行历史迁移。

从 `0.4.0` 开始，电脑端自动使用表格，手机端自动使用卡片或精简列表；旧的表格/卡片偏好只为
旧前端保留。手机默认使用浮空 Dock，账号可以改回侧边栏，也可以保存自己的分类和页面顺序。
这些设置保存在 `OnlineData._users` 新增的 `mobile_navigation_mode` 和 `mobile_dock_config`
字段中。两个字段由后端启动时兼容性补齐，旧版本会忽略它们。

部署 `0.4.0` 时只需要即时备份 `OnlineData`，并保留旧后端镜像和旧前端目录；不需要因为这两个
账号设置字段额外备份另外两个数据库。上线后要确认 `_users` 已新增两个字段、旧账号默认使用
Dock、手机可以切回侧边栏、普通账号看不到用户管理和运维中心。正式标签仍要等这些检查通过后
再创建。

`0.2.0` 上线时使用受控工具完成了已有历史快照的任务流水回算：先生成临时流水和日报，验证后再
替换正式结果，且没有修改 `OnlineData`、`OnlineDataArchive` 或走访数据。该一次性工具已在
`0.9.5` 移除，历史实现可从 `v0.2.0` 标签追溯。

如果以后出现任务流水缺失，不能把旧工具复制出来直接运行。应根据当前表结构重新编写迁移，先在
测试库跑通，再备份并校验 `daily_report`、确认没有同步任务、审计所有快照证据，最后检查流水唯一性
和每张日报中“未核查＋已核查＋已完成＝数据总数”。

如果需要回退本次计算，先停止后端，再把保留的 `_v010_` 日报表按明确清单换回；这会覆盖新版日报，
属于高风险操作，必须重新确认后执行。

## 部署服务器前

生产服务器已经在 2026-07-28 完成 HTTPS、端口收紧、凭据轮换和运维中心上线。
仍然要先看 [风险清单](known-risks.md)，尤其不要忽略 OAuth 明文、单机备份和凭据风险。

用户已经授权正常代码、前端更新和兼容性数据库变更默认同步到服务器。改动检查通过并合并到 `main`
后，除非用户明确说“不同步”“只改本地”或“先不要部署”，否则继续完成部署。

兼容性数据库变更是指只新增表或可空字段，而且旧版本仍能继续运行。部署前必须通过测试、
备份受影响的数据库并准备回退办法。删除或覆盖数据、恢复备份、删除/重建表和数据卷、不可逆迁移、
轮换凭据等高风险操作仍要单独确认。

每次部署前必须确认：

1. 要部署的是哪个版本。
2. 前端构建和后端检查已经通过。
3. 服务器上的 `.env` 和数据卷不会被覆盖。
4. 已按下面的风险等级准备回退办法。
5. 涉及数据写入或数据库结构时，相关数据库备份不是空的并已通过完整性检查。

部署备份按改动范围处理：

- 纯前端更新：保留上一版 `frontend/dist`，不额外备份数据库。
- 不改数据库结构和数据写入逻辑的后端更新：保留旧 Docker 镜像，不额外备份数据库。
- 修改同步、导入、报表计算等可能写入数据的逻辑：备份受影响的数据库；无法准确判断影响范围时备份三个数据库。
- 修改数据库结构、执行数据迁移或其他高风险操作：完整备份三个数据库，并按高风险规则再次确认。
- 修改 nginx、Compose 或证书配置：只备份相关配置，除非这次改动同时涉及数据库。

平台的每日数据库备份继续保留 7 天。它是日常兜底，但不能替代数据库迁移或高风险操作前的即时备份。

部署后要检查：

1. 健康接口能否访问。
2. 容器是否正常运行。
3. 日志是否有报错。
4. 登录和退出是否正常。
5. 同步、统计和关键数据是否正常。

## 服务器容量与发布材料保留

自动发布会保留历史源码包、发布记录、上一版镜像和按风险范围创建的即时备份，但不会自动执行宽泛清理。日常以 5GB 可用空间作为提前整理线；固定发布脚本在空间低于其硬性最低值时会拒绝切换，但这个保护只用于避免发布时耗尽磁盘，不能代替容量管理。

固定保留顺序：

1. 当前生产后端镜像及其 `latest` 标签。
2. 上一稳定版本的回退镜像。
3. 当前与上一版的发布记录、源码留档和必要即时备份。
4. 平台每日备份保留策略要求的最近成功备份。
5. MySQL 数据卷、证书续期镜像和正在运行容器依赖的镜像。

容量不足时先只读执行 `df -h /`、`docker system df`、发布目录分项统计和日志占用统计。确认当前版本、上一版和所有运行容器的真实镜像 ID 后，只能按完整仓库名和标签白名单删除更旧的 `binhu-backend` 镜像。系统日志可以压缩到明确上限；构建缓存可以在空间再次紧张时清理，但会让下一次构建变慢。

不得使用 `docker system prune -a`、`docker compose down -v`、针对 Docker 根目录的递归删除，或根据未验证变量批量删除文件。不得把 MySQL 数据卷、平台每日备份或当前发布记录当作普通缓存。`/root/binhu/releases`、`/var/lib/binhu-deploy/releases` 和 `deploy-backups/automated` 只有在逐项确认当前与回退版本后才能清理，不能因为目录名含有“旧”就整目录删除。

2026-08-06 的实际整理删除了 122 个更旧的后端镜像标签，并把 systemd 日志从约 632MB 压缩到约 184MB；当前 `0.15.2` 与上一版 `0.15.1` 镜像均保留。整理后可用空间为约 6.7GB，健康接口、三个容器和近期日志正常。此次没有删除数据库卷、数据库备份、发布源码留档或构建缓存。

## 安全入口怎么部署

生产环境由 nginx 统一提供 HTTPS，MySQL 和 Uvicorn 不能直接响应公网请求。
当前端口关系是：

- 滨湖平台使用 nginx 的 HTTPS 443。
- 后端只绑定 `127.0.0.1:37125`，由 nginx 转发。
- MySQL 不发布宿主机端口，只在 Compose 内部使用 `mysql:3306`。

当前服务器的 HTTP 80 还承载另一套独立应用，滨湖平台没有接管或重定向它。不能直接用
仓库里的默认 80 配置覆盖服务器现有站点；更新 nginx 时要保留 ACME 验证目录和原站点。

服务器没有域名时，可以使用支持 IP 地址的短期证书。部署时先用 Certbot 5.4 或更高版本
申请证书，再把实际证书软链接到：

```text
/etc/nginx/ssl/binhu/fullchain.pem
/etc/nginx/ssl/binhu/privkey.pem
```

IP 地址证书只有约 6 天有效期。生产服务器使用 `binhu-certbot-renew.timer` 每天检查两次，
续期成功后执行 `nginx -t` 和重新加载。日常检查：

1. 80 端口的 ACME 验证目录可以访问。
2. `nginx -t` 通过。
3. HTTPS 健康接口和登录都正常。
4. `systemctl is-active binhu-certbot-renew.timer` 返回 `active`。
5. 从外部确认 `443` 可连接，`3306` 和 `37125` 不可连接。

续期镜像和 systemd 模板保存在 `nginx/`。如果本地 Certbot 镜像被清理，可以重新构建：

```bash
docker build -f nginx/Dockerfile.certbot -t binhu-certbot:5.4.0 nginx
```

安装或更新定时器前，先比较服务器现有 unit 文件并备份，不能直接覆盖未知配置。

生产 `.env` 还要设置：

```dotenv
BINHU_SESSION_COOKIE_SECURE=true
BINHU_SESSION_COOKIE_SAMESITE=none
BINHU_MULTI_DEVICE_SESSION_ENABLED=true
BINHU_CORS_ALLOWED_ORIGINS=http://tauri.localhost,https://tauri.localhost,binhu://app
```

新建空数据库时，临时设置 `BINHU_BOOTSTRAP_ADMIN_USERNAME` 和
`BINHU_BOOTSTRAP_ADMIN_PASSWORD`。首个超级管理员创建并成功登录后立即删除这两个变量。
已有用户的生产数据库不需要设置。

## 数据库备份与恢复

下面是给负责运维的人使用的备份命令。执行前仍要确认服务器、容器和备份目录是否正确：

```bash
docker exec binhu-mysql sh -c 'mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" \
  --single-transaction --routines --triggers \
  --default-character-set=utf8mb4 \
  --databases OnlineData OnlineDataArchive daily_report' \
  > "/root/binhu_backup_$(date +%Y%m%d_%H%M%S).sql"
```

`--databases` 不能省略。省略后，`mysqldump` 会把第二、第三个数据库名称误当成表名，三库备份会失败。

备份后至少检查：

- 文件存在。
- 文件大小不是 0。
- 文件开头是正常的 SQL 转储内容。
- 重要操作前，备份已经复制到服务器之外的安全位置。

恢复会覆盖数据，不能自动执行。恢复前必须再次备份当前数据，并得到用户对目标服务器、备份文件和覆盖范围的明确确认。

不要使用 `docker compose down -v` 排错或恢复数据，它会删除数据卷。

## 运维中心和每日备份

运维中心已在 2026-07-28 上线。超级管理员可以从侧栏直接打开“运维中心”：

- “运行概况”查看容器、磁盘、MySQL、最近同步、最近备份和 OAuth 过期状态。
- “系统日志”查看后端和 MySQL 日志。这里只能查看，不能输入命令。
- “数据库”查看三个数据库的大小、估算行数、表结构和索引，不能修改数据。
- “备份管理”设置每日备份时间、立即创建备份和下载成功备份。
- “操作记录”查看超管执行过的重要操作。列表默认显示人员姓名和中文操作、目标、结果及详情摘要；展开一行可核对登录账号、原始操作代码、原始目标和原始审计 JSON。在线汇总、走访汇总和下发反馈 XLSX 的导出也会记录安全摘要，不保存工作簿正文、身份证号、手机号或敏感搜索词。
- “导出诊断包”下载脱敏后的状态和近期错误日志，不包含业务数据。

每日备份默认按“系统设置”的时区在凌晨 2 点执行。自动和手动备份都包含
`OnlineData`、`OnlineDataArchive` 和 `daily_report`，保存 7 天，并始终保留最近一份成功备份。

下载整库备份前需要重新输入当前超级管理员密码。运维中心不提供备份删除和数据库恢复。
恢复仍然是高风险操作，必须按照上一节的规则单独确认。

生产 `.env` 必须另外提供两个值：

```dotenv
BINHU_OPS_AGENT_TOKEN=<单独生成的随机内部令牌>
BINHU_BACKUP_DIR=<服务器上的专用备份目录>
```

内部令牌不能和数据库、SSH、管理员或应用密钥共用。备份目录只用于平台备份，不要指向项目根目录或数据卷。

上线时已经完成：

1. `binhu-ops-agent` 没有发布公网端口，只有它挂载 Docker Socket。
2. 普通管理员访问运维接口返回 403；其余角色由同一后端权限规则和自动化测试覆盖。
3. 日志里出现的密码、令牌、Cookie 和 Authorization 已被遮盖。
4. 手动备份成功，下载前会重新验证密码，下载文件能通过 gzip 和 SHA-256 检查。
5. `_backup_schedule`、`_backup_jobs` 和 `_admin_audit_log` 已创建。
6. 日志流、数据库概况、操作记录和诊断包已实际请求成功。

还需要观察下一次真实的凌晨 2 点任务，确认自动备份按时完成且不会重复创建。不要在生产
环境测试恢复、删除、容器重启或故意制造数据库故障。

## 0.12.2 每日人均核查数上线

`0.12.2` 不修改日报物理表，也不重算或覆盖历史业务数据。旧表中的“当日人均核查数”列继续保留供旧版本回退，新接口在读取时统一显示为“每日人均核查数”。多日总汇总增加“在岗人日”，按有同步快照的日期逐日读取出勤和双休日备勤，使用“区间已完成数 ÷ 在岗人日”计算。

上线前运行区间汇总、出勤和前端总计测试，并保留旧程序与旧后端镜像；因为没有数据库结构或数据写入改动，不需要重复备份三个数据库。部署后只读检查健康接口、一个工作日区间、一个跨已排班周末区间和旧单日报，确认新列名、在岗人日、每日人均核查数及近期错误日志正常。不要为验证本版修改排班、请假或腾讯业务数据。

## 0.12.3 手机任务卡片信息补充上线

`0.12.3` 不修改数据库结构、腾讯回写协议或业务数据，只扩展手机任务列表摘要：五类任务显示来源行中的身份证号和拆分后的手机号，全链条额外显示“来源”。上线前运行手机任务摘要测试和前端测试，并保留旧程序与旧后端镜像；无需重复备份三个数据库。部署后只读检查健康接口、带数据的手机任务列表响应和近期错误日志，不为验收修改腾讯业务表。

`0.12.16` 修复全链条来源表新增“登记情况”后造成的物理列错位。程序按真实表头在旧 14 列和新 15 列布局间选择，同步、单元格回写、整行新增及下发发布共用同一布局；“登记情况”不写入业务库。上线不需要数据库迁移或腾讯测试写入，保留旧程序和旧后端镜像即可。部署后等待一次正常同步刷新来源缓存和业务数据，再只读核对创建时间、现住址、核查结果、研判和二次反馈。

`0.12.17` 将全链条“登记情况”从物理占位列升级为正式业务字段。启动时为 `OnlineData.t_fullchain` 和 `OnlineDataArchive.t_fullchain_archive` 增加可空字段，旧程序可忽略该列并继续回退；不修改旧日报快照，读取时按空值兼容。上线前备份 `OnlineData` 和 `OnlineDataArchive` 并保留 `0.12.16` 后端镜像；部署后先核对两张表的字段，再执行或等待一次正常同步，确认当前表和 `_online_source_rows.values_json` 已保存真实登记情况，且创建时间至二次反馈仍未错位。本次不向腾讯业务表制造测试写入。

`0.12.18` 只调整概览明细、审计展示和手机导航，不迁移或改写数据库。结转明细新增“首次下发”，优先读取历史快照中的“下发日期/下发时间”；同步操作记录通过任务编号动态显示 `_sync_log` 终态；管理员和对应管理员权限组账号可以在手机 Dock 配置独立“流口任务”入口。上线只需保留 `0.12.17` 前后端镜像，不额外备份数据库；验证健康版本、同步历史状态、结转日期和管理员手机 Dock 后即可创建标签。

`0.12.19` 调整“下发任务处理”的筛选工具栏和全局下拉框文字垂直对齐，并增加超管专用批次删除。删除只允许从未开始发布且没有发布结果或腾讯来源关联的批次，事务性移除批次和本地任务并记录脱敏审计；已经开始发布的批次必须保留。该版本不迁移数据库结构，也不向腾讯写入数据，上线保留上一版程序和后端镜像即可，不重复备份数据库。部署后只读检查版本、容器、前端资源、删除接口权限和近期错误日志；真实删除由项目管理人针对确认误导入的未发布批次自行验收。

`0.15.1` 在指令核查详情摘要中直接展示当前腾讯来源行的身份证号、手机号、来源、原地址和现住址，并统一“指令核查”导航与个人资料工作构成文案。该版本不迁移数据库结构、不改变腾讯回写协议，生产发布使用 GitHub `Deploy production` 手动工作流，备份范围选择 `none`，由固定发布脚本保留上一版程序和镜像。部署后核对版本、健康状态、指令核查详情只读展示和近期错误日志，不向腾讯业务表写入测试数据。

`0.15.2` 将在线数据汇总、走访汇总和下发任务反馈表的 XLSX 导出写入操作记录，只保存导出类型、日期范围和行数等安全摘要；人员管理在双休日按本周备勤表显示“今日备勤”“休息”或“未排班”，请假和长期离岗继续优先。该版本不迁移数据库结构，也不修改腾讯回写协议，生产发布使用 GitHub `Deploy production` 工作流，备份范围选择 `none`。部署后核对版本、健康状态、人员管理周末状态和近期错误日志；导出记录由项目管理人使用正常业务导出自行验收，不为验证额外导出敏感数据。

`0.15.3` 修复 Univer 公式栏显示前导单引号和拖动横向滚动条导致页面跳顶的问题，并为仪表盘增加按账号隔离的短期会话缓存。该版本只修改前端，不迁移数据库结构、不改变腾讯读取或回写协议；生产发布继续使用 GitHub `Deploy production`，备份范围选择 `none`，固定发布脚本保留上一版程序和镜像。部署后核对版本、可信 HTTPS 健康状态、仪表盘重复打开速度、查询工作表文本显示与横向滚动，以及近期错误日志；不为验收修改腾讯业务数据。

`0.15.4` 继续修复嵌入式 Univer 横向滚动条导致页面跳顶的问题。`0.15.3` 只锁定了内容区的滚动位置，但当前桌面布局实际可能由浏览器文档承担纵向滚动；新版在横向拖动开始、进行及松开后的短暂布局阶段同时保护两处滚动位置。该版本只修改前端交互，不迁移数据库结构、不改变腾讯读取或回写协议；生产发布使用 GitHub `Deploy production`，备份范围选择 `none`，部署后必须在页面存在纵向滚动空间时实际拖动工作表底部横向滚动条验收。

`0.15.5` 重做登录页公安品牌视觉：桌面使用蓝色网格和城市丝绸线稿双栏布局，左侧文案为“数智赋能、守护平安滨湖”，右侧使用警徽和“滨湖公安智慧平台”；手机端收敛为单栏表单。素材进入前端构建包，警徽按实际显示尺寸压缩但保留透明背景。本版只修改前端页面和静态资源，不迁移数据库结构、不改变认证接口；生产发布使用 GitHub `Deploy production`，备份范围选择 `none`。部署后核对版本、登录页桌面和手机布局、错误提示、首页跳转、资源加载及近期错误日志。

`0.15.6` 补齐登录页深色主题：左侧公安蓝品牌画面保持不变，右侧页面背景、品牌标题、登录卡片、输入框、浏览器自动填充和错误提示统一切换为深色配色。本版只修改登录页样式和测试，不迁移数据库结构、不改变认证接口；生产发布使用 GitHub `Deploy production`，备份范围选择 `none`。部署后分别以浅色和深色主题核对桌面、手机登录页以及输入框聚焦、自动填充和错误提示状态。

`0.15.7` 统一平台滚动条视觉：浏览器文档、侧栏、抽屉、弹窗、原生横向列表、Ant Design 虚拟列表和表格粘性滚动条统一使用细圆角滑块，并分别适配浅色和深色主题。本版不改变任何滚动容器或业务逻辑，不迁移数据库结构；生产发布使用 GitHub `Deploy production`，备份范围选择 `none`。部署后重点检查侧栏、主页面、长弹窗、横向表格及 Univer 底部滚动条的拖动、列对齐和页面纵向位置。

## 0.15.8 流口指令核查筛选与优先处理

本版不修改数据库结构、腾讯回写协议或任务详情处理流程。流口任务列表新增社区、核查人多选，支持“社区未填写”和“未分配核查人”空值筛选；更多筛选提供精确任务状态、复核阶段、互斥优先级和更新时间排序。列表默认按“已研判、来源异常、待同步、普通待处理、等待研判、已完成”排序，更新时间排序也把已完成任务固定沉底。数量卡统计当前业务、职责范围、社区/核查人和关键词条件下的全量数据，不只统计当前分页。

敏感关键词改由 `POST /api/mobile-tasks/{parser_type}/search` 正文提交，不进入 URL；筛选选项由
`GET /api/mobile-tasks/{parser_type}/filter-options` 按后端权限和实际任务生成。旧 GET 列表接口继续保留
兼容。上线使用 GitHub `Deploy production`，版本为 `0.15.8`，备份范围选择 `none`；部署前不向腾讯业务表写入测试数据，部署后核对健康版本、任务搜索、筛选选项、手机/电脑布局和近期错误日志。

## 0.13.1 个人资料入口、排版和研判结果展示

`0.13.1` 不修改数据库结构、贡献口径或腾讯回写协议。独立人员目录和导航入口被移除，旧 `/people`
地址转到人员管理；人员管理桌面表格和手机卡片中的姓名进入公开资料详情。资料页改为限制最大宽度的
侧栏加贡献区布局，并统一桌面、平板、手机和深色模式间距。流口任务列表与详情直接显示已有的研判
内容，但保持只读。数据下发新增缺手机号待研判规则：新导入记录不再分配社区，启动兼容处理会把尚未
产生腾讯外部结果的既有空手机号任务重新打开为待研判；补齐手机号前不能审核或发布为社区下发。
上线前备份 `OnlineData`，并保留 `0.13.0` 前端产物和旧后端镜像；验证版本、姓名链接、旧地址跳转、
两类研判展示、空手机号任务状态、登录页、容器和近期日志后创建标签。

## 0.13.0 公开人员主页与工作贡献上线

`0.13.0` 新增兼容表 `_work_activity_events`，并从仍在保留期内的在线回写审计、管理审计和已有工作
日志草稿幂等回填贡献。上线前只读统计三类可回填数量，备份 `OnlineData`，保留 `0.12.19` 前后端
程序和后端镜像；另外两个数据库不受影响。不要为了补齐旧年份而从当前业务结果猜测操作者或时间。

部署后依次检查：表结构与唯一事件键；重复 `event_key` 为零；事件行没有 JSON、业务正文或敏感
字段；核查任务、下发审核和工作日志各完成一次后只增加预期数量；修改核查人、查询、导出和同步不
增加；账号改绑前后的贡献都归入对应人员主页；普通登录用户能查看人员目录和公开主页，但响应不含
用户名、身份证号、手机号、权限及会话信息；登录页错误提示与按钮间距正常；健康接口返回 `0.13.0`。
回退时切回旧程序和镜像即可，新表由旧版本忽略并保留，不能为回退删除贡献事件或恢复整库备份。

## 0.12.4 手机任务结果选项和号码面板修复上线

`0.12.4` 不修改数据库结构或业务数据。腾讯为空白结果单元格返回空下拉项时，后端优先读取同一工作表缓存中的真实选项及 ID，并在写回前使用同一规则补齐元数据；没有缓存时使用对应业务的固定默认结果。多号码改用浏览器和手机系统提供的原生选择控件，选中后再拨号或复制，不再显示网页蒙版。上线前运行在线回写、手机任务和前端测试，保留旧程序与旧后端镜像，不重复备份三个数据库；上线后只读检查选项元数据、前端资源、健康接口和近期错误日志，不为验收修改腾讯业务表。

## 定时同步怎么使用和排查

超级管理员可以在“设置 → 系统设置”中开启、关闭或修改同步间隔。修改间隔或重新开启后，
倒计时会从保存时刻重新开始。常用间隔可以直接选择，自定义值必须在 5 分钟到 7 天之间。

所有登录用户都可以读取同步状态和下一次倒计时；只有拥有 `sync.trigger` 权限的账号可以点击
“立即同步”，后端会拒绝其他账号发起同步，不能只依赖前端按钮隐藏。同步成功后自动生成日报，
页面不再提供单独的“生成日报”按钮。

同步状态分成四步：等待、同步在线数据、生成报表、完成。页面显示的步骤数来自真实任务进度，
不会再显示估算的假进度。自动同步失败或部分失败后，超级管理员可以从页面上的铃铛查看个人提示。

所有登录用户都能打开消息中心。公告对所有账号可见，但每个人的已读状态分别保存；个人提示只对
接收者可见。超级管理员可以在消息中心发布或删除公告。首次升级会自动发布“出勤历史数据说明”，
因此走访汇总页面不再重复显示同一条长期提示。

如果自动同步没有按时开始，按下面的顺序检查：

1. 查看“系统设置”里是否开启，以及下一次执行时间是否合理。
2. 查看最新同步状态，确认没有另一项任务仍处于等待中或运行中。
3. 查看后端日志中是否有 `[SCHEDULER]` 或 `[SYNC]` 报错。
4. 只读检查 `_sync_schedule` 和最新 `_sync_log`，不要直接修改时间或任务状态。
5. 如果刚发生后端重启，确认中断任务已经被标为失败，并且下一次时间已重新安排。

部署包含定时同步改动的版本后，还要额外检查：

1. `_sync_schedule`、`_notifications`、`_announcements`、`_announcement_reads` 和 `_sync_log` 已经存在。
2. 使用普通管理员、组长和组员验证手动同步权限。
3. 实际等待一轮定时同步，确认来源显示为“自动触发”。
4. 确认分汇总表全部成功后，总汇总表才最后更新。
5. 使用普通账号确认能看到公告、看不到他人的个人提示，并确认不同账号的公告已读状态互不影响。
6. 不要在生产环境故意制造失败；失败通知使用自动化测试验证。

## 正式部署入口

正式版本使用 GitHub 仓库中的 `Deploy production` 手动工作流发布，不再从开发电脑逐文件传输。普通 push、
Pull Request 和 `CI` 工作流没有生产 Secret，也不能连接服务器。代码必须先通过 PR 合并到 `main`，然后由
发布人手动填写：

- `ref`：必须是已经合并到 `main` 的完整 40 位小写 Git 提交号。禁止使用 7/8 位短提交号、分支名或标签；短提交号可能无法被 GitHub Actions 的检出步骤解析，导致发布在构建前失败。
- `expected_version`：必须与该提交根目录 `VERSION` 完全一致。
- `backup_scope`：`none`、`online`、`daily` 或 `all`，按本手册“部署服务器前”的风险范围选择。
- `release_scope`：默认 `auto`。生产提交可验证时，纯后端变化发送后端精简包，纯前端变化发送前端精简包；
  前后端同时变化、Compose、Nginx、部署脚本、未知路径或状态不确定时发送完整包。也可以明确选择 `backend`、
  `frontend` 或 `full`，但手动精简范围遇到不属于该范围的运行文件会被拒绝。

网页上可以从 GitHub Actions 手动运行，也可以在已经登录 GitHub CLI 的受控电脑执行：

```powershell
$version = (Get-Content VERSION -Raw).Trim()
$releaseCommit = (git rev-parse origin/main).Trim()
if ($releaseCommit -notmatch '^[0-9a-f]{40}$') { throw '发布提交号必须是完整 40 位小写 Git SHA' }
git merge-base --is-ancestor $releaseCommit origin/main
if ($LASTEXITCODE -ne 0) { throw '发布提交尚未进入 main' }
gh workflow run deploy-production.yml --ref main `
  -f ref=$releaseCommit `
  -f expected_version=$version `
  -f backup_scope=none `
  -f release_scope=auto
```

其中命令行参数 `--ref main` 表示从 `main` 读取工作流定义；表单参数 `-f ref=$releaseCommit` 才是实际发布的代码提交，两者不能混用。触发后应在工作流标题中确认显示的是完整 40 位提交号。

`backup_scope=none` 只适用于纯前端，或不修改数据库结构和数据写入逻辑的普通后端更新。涉及在线同步、导入、日报计算、数据库结构或迁移时，必须按“部署服务器前”的风险表改为 `online`、`daily` 或 `all`，不能因为自动工作流方便就一律填 `none`。

触发后记录工作流地址并等待唯一一次运行结束，不要因构建时间较长重复点击。可以使用：

```powershell
gh run list --workflow deploy-production.yml --limit 1
gh run watch <运行编号> --exit-status
```

只有工作流成功、生产健康接口返回预期版本、容器和近期日志正常后，才创建并推送 `vX.Y.Z` 标签。工作流失败时先查看失败步骤；不要在原因不明时重复发布，也不要改用手工上传掩盖固定入口的问题。

工作流始终运行部署脚本测试；后端或完整发布运行后端测试和 Python 编译，前端或完整发布运行前端测试和生产构建。通过后
生成与发布范围一致的发布包：完整发布包含源码和前端产物，后端发布只包含 `VERSION` 与 `backend/`，前端发布只包含
`VERSION` 与 `frontend/dist`。包保存
30 天供审计，再用专用受限身份发送到服务器。目标提交不是 `main` 的
祖先、版本不一致、并发发布、主机指纹不一致或任一检查失败时都不会切换生产。

### 第一次安装固定入口

部署身份只需在每台生产服务器初始化一次：

1. 在受控电脑生成一把仅供 GitHub Actions 使用的 Ed25519 密钥，不设置为日常 root 登录密钥，也不放进仓库。
2. 使用已经验证的人工运维入口，把公钥和 `deploy/` 下三个服务器脚本复制到临时目录。
3. 以 root 执行 `install-server-deployer.sh <公钥文件> <平台 HTTPS 地址>`。安装器创建锁定密码的 `binhu-deploy` 账号、强制命令、固定 sudo 规则和 root 所有的发布目录，并把正式 HTTPS 地址写入服务器私密配置用于发布后自检。
4. 检查该密钥不能打开交互 Shell、不能端口转发，错误命令会被拒绝；同时确认原人工运维入口仍可用于紧急回退。
5. 在 GitHub 建立 `production` Environment，并保存 `BINHU_DEPLOY_HOST`、`BINHU_DEPLOY_PORT`、`BINHU_DEPLOY_SSH_KEY` 和 `BINHU_DEPLOY_KNOWN_HOSTS` 四个 Secret。主机指纹必须从已经信任的人工 SSH 连接核对，不能只相信临时网络扫描。

GitHub 不保存 root 密钥、服务器 `.env`、数据库密码或腾讯凭据。`binhu-deploy` 的公钥授权行固定带
`restrict` 和服务器强制命令；即使工作流配置写错，也不能把它变成普通远程终端。

`/usr/local/sbin/binhu-deploy` 和强制网关属于服务器安全边界，不会随普通应用发布自动覆盖。以后修改
`deploy/` 下的服务器脚本时，必须先走代码 Review，再由人工 root 入口重新运行安装器并复核权限；不能让
旧发布脚本自动更新它自己。

### 发布、回退和记录

GitHub 在受限命令中同时传入发布包的精确字节数，服务器只读取该长度，避免依赖 SSH 会话关闭来判断文件结束。
服务器随后按固定顺序执行：校验包与提交、检查磁盘、检查 Compose、确认没有运行中的同步或备份、
按范围构建候选后端或复用现有后端镜像、保存对应旧程序和必要的旧镜像、创建并校验所选数据库备份、切换、核验内部健康版本、前端资源和正式
HTTPS 健康接口。公网自检由服务器在固定脚本内执行，避免 GitHub Runner 到国内 443 的链路策略被误判为应用故障。成功记录在
`/var/lib/binhu-deploy/history/`，当前结果在 `current.json`；程序和即时数据库备份保存在项目的
`deploy-backups/automated/` 下。

后端精简发布不会替换现有 `frontend/dist`，但会先替换整个后端目录，避免已删除的旧 Python 文件残留。切换失败会
自动恢复旧程序和旧后端镜像并重建服务。前端精简发布不会构建或替换后端镜像，只备份和替换 `VERSION` 与
`frontend/dist`，随后重建后端和运维代理容器以加载新版本；失败时恢复旧版本号和旧前端。失败记录不会覆盖
`current.json` 中最后一次成功发布。自动流程绝不导入数据库备份，也不撤销腾讯文档等外部
结果。涉及迁移或外部写入时，发布前必须另写业务回退办法；需要恢复数据库时仍按“数据库备份与恢复”一节
重新确认。服务器磁盘上的历史发布目录、即时备份和旧镜像不会被发布脚本自动删除，清理前必须先核对当前与
回退版本、备份保留和目标绝对路径。

只有 GitHub Actions 故障且业务确实不能等待时，才使用 `AGENTS.local.md` 的私密人工入口应急发布。应急发布
仍要使用同一提交、检查、备份和回退规则，并在任务记录中说明绕过原因；不能在生产服务器临时修改业务代码。
真实地址、账号、密码、主机指纹和密钥不能写进仓库脚本、命令示例或共享文档。

一次性迁移、修复或核验代码在任务完成并留下 Git 标签后应从主线删除。再次遇到类似问题时，应按
当时的结构和风险重新编写短期工具，不把历史脚本当作长期运维入口。

## 物理服务器迁移与旧云退役（历史记录）

物理服务器迁移和正式停写切换已完成。本节保留迁移过程和回退设计，供审计和故障复盘使用；其中
WireGuard、旧云网页代理和异地备份脚本均已退役，下面的安装命令不是当前操作入口。生产平台只依赖物理服务器
本地 Docker、MySQL、独立备份盘和 SakuraFRP 公网/SSH 隧道。

### 两台服务器之间的专线（已退役）

迁移期间旧云和物理服务器曾使用只承载两台主机流量的 WireGuard `/30` 网络：旧云为 `10.77.0.1`，物理服务器为
`10.77.0.2`。双方 `AllowedIPs` 只能填写对端 `/32`，不得增加默认路由、局域网网段或 IP 转发。物理服务器
位于 NAT 后方，由它主动连接旧云 UDP `51820`，并使用 25 秒保活。

私钥只能在对应服务器生成并保存到 root 可读文件。取得双方公钥后，以 root 从完整发布目录执行：

```bash
deploy/install-wireguard-link.sh server <旧云私钥文件> <物理服务器公钥>
deploy/install-wireguard-link.sh client <物理服务器私钥文件> <旧云公钥> <旧云公网主机>
```

旧云防火墙只额外开放 WireGuard UDP 端口。安装后核对 `wg show` 最近握手、双方私网 Ping 和内部健康接口；
不得仅凭接口存在就判断专线可用。

### Nginx 维护与生产配置（网页代理已退役）

`deploy/install-nginx-migration-profiles.sh` 只安装配置，不改变当前活动站点。它会把原站点保存为 `legacy`，
并安装 root 专用的 `binhu-nginx-profile` 切换器。配置中的真实站点名称和文件路径只能在服务器私密资料中保存：

```bash
deploy/install-nginx-migration-profiles.sh new <物理服务器活动站点文件>
deploy/install-nginx-migration-profiles.sh old <旧云活动站点文件> <旧云HTTPS名称>
```

物理服务器生产配置当前只提供外部 HTTPS；迁移期间曾额外提供只监听 `10.77.0.2:18080` 的专线入口，旧云
`old-proxy` 曾通过 WireGuard 访问该入口。该网页代理和专线监听已于 2026年8月9日退役，旧配置只作为私密回退材料
保留，未经项目管理人再次明确授权不得启用。切换器只接受固定 profile 名称，每次先执行 `nginx -t`，加载失败时自动恢复上一文件。

正式切换时曾使用 `new-maintenance`、`old-maintenance` 和 `old-proxy` 完成入口过渡。这些旧云 profile 现在只用于
审计和紧急恢复设计参考，不属于日常入口；当前用户统一使用物理服务器的 FRP 可信 HTTPS 地址。

### 每日异地备份（已退役）

迁移期间曾由物理服务器把 02:00 平台备份通过 `binhu-offsite-push.timer` 和 WireGuard 上传到旧云。
2026年8月9日已停止并禁用发送端和接收端，当前不再向旧服务器发送备份。以下命令仅作为历史记录保留：

1. 在物理服务器执行 `deploy/install-offsite-backup.sh sender-init`，只生成专用密钥和服务文件，不启动上传。
2. 把输出的公钥通过已验证的人工运维通道交给旧云，执行
   `deploy/install-offsite-backup.sh receiver <发送端公钥文件>`。
3. 从旧云已经信任的 SSH 主机公钥生成 `[10.77.0.1]:51234` `known_hosts` 条目，不能只信任临时网络扫描；
   异地备份复用旧云现有 SSH 端口，不额外开放公网 22 端口。
4. 在物理服务器执行 `deploy/install-offsite-backup.sh sender-activate <已验证known_hosts文件>`。
5. 手工启动一次 push 和 ingest 服务，核对文件名、大小、gzip、八库标识和 SHA-256 后再等待定时任务。

接收账号使用 `restrict`、来源地址、强制命令和专用拒绝式 Shell，只能上传一个符合命名和 256MB 上限的
备份流。旧云 root 校验后把文件改为 root 所有并移出可写收件箱，归档目录保存 7 天且始终保留最近一次成功
备份。发送端不能读取或删除归档文件。

### 正式切换和自动发布

维护窗口开始后，旧入口先进入维护状态，再等待同步和备份结束，关闭同步、每日备份和在线回写并停止旧后端。
正式切换期间曾同时保留旧云、物理服务器独立备份盘和服务器外部副本。当前发布和恢复只使用物理服务器独立备份盘上的
本地备份；如需再次增加异地副本，必须另行设计新的受控目标和恢复演练。

项目管理人开始登录验收后，才恢复每 10 分钟同步、02:00 每日备份和在线回写。项目管理人使用现有任务验证
一次腾讯回写，Agent 不创建测试数据。新平台或腾讯已经产生新业务写入后，不能直接启动旧后端；要先冻结并
备份新库，再制定反向迁移。

GitHub 自动发布在正式切换同日迁移。新服务器复用现有专用部署公钥，GitHub Environment 更新目标主机、
穿透端口和经可信人工连接核对的主机指纹；服务器公开健康检查使用物理服务器的 FRP 可信 HTTPS 地址。
首次纳管已经从 `main` 发布精确 `0.15.2` 且 `backup_scope=all`，新服务器成为唯一生产数据库节点。旧服务器上的 GitHub 部署授权已经移除，历史平台容器保持停止；旧主机当前仅保留原数据库、卷、程序和历史回退材料。

物理服务器直接入口已经使用 FRP 服务商托管、受浏览器信任的 HTTPS 子域名，GitHub 发布后的公开健康检查
也使用该入口并保持严格 TLS 校验。旧云不再提供网页备用入口；任何发布或诊断脚本都不得加入跳过证书校验的选项。

## 八库分域和新业务数据库

新业务数据库连接已加入配置和 Compose：`PlatformData`、`VisitData`、`DispatchData`、`RegistryData`、`WorkflowData`，连同现有 `OnlineData`、`OnlineDataArchive`、`daily_report` 构成八库目标布局。当前迁移采用分阶段方式；新库缺失时后端会跳过可选连接，避免旧表平台因准备工作未完成而无法启动。

`RegistryData` 和 `WorkflowData` 的表由后端启动兼容初始化。首次在服务器创建数据库时，必须先执行 `backend/init.sql` 中的数据库创建和授权部分，再启动后端；不要在生产库直接运行测试导入或手工修改新业务表。

房屋档案来源导入必须先执行只读预览，再由具备 `registry.import.manage` 权限的账号确认。户号表导入和告知书挂载会写入 `RegistryData`，发布前备份范围使用 `all`。告知书读取复用走访来源平台的服务器端地址和认证配置，仅调用 GET `/api/address/queryHouseCertificate`，不向来源平台写入；请求不附加日期范围，但后端会强制校验 `VISIT_SOURCE_POLICE_NAME=滨湖新城派出所`。审计只记录批次号、数量和结果，禁止记录地址、姓名、身份证号或完整来源正文。

责任告知书图片预览另使用服务器私密配置 `BINHU_CERTIFICATE_IMAGE_BASE_URL` 指向旧平台的只读签名图片目录。不要把旧平台图片主机或文件路径写入前端、普通日志或公开文档。房屋详情按钮只对 `registry.import.manage` 显示；后端再次校验房屋社区范围、图片相对路径和 JPG/PNG 文件头，单张图片上限 10 MB，读取失败不会改变档案或来源数据。

正式迁移按 [八库业务域与新档案规划](database-domain-plan.md) 分阶段执行。2026-08-08 已完成走访、下发、小区地址、平台基础、工作日志和身份证 HMAC 回填；人员标签快照因当前没有标签分配而无可回填记录，Registry/Workflow 新功能仍待后续维护窗口。旧表全部保留，未执行长期双写或删除。

_最后核对：2026-08-11_

## v0.16.0 八库迁移操作顺序

本版本的代码发布和数据库迁移是两件事。先通过 GitHub `Deploy production` 发布兼容代码，`backup_scope=all` 才表示八库完整备份；确认备份清单、SHA-256、磁盘空间和同步/备份空闲后，才进入维护窗口。

迁移窗口只允许按以下顺序执行：`measure` 只读测量 → `visit` → `dispatch` → `platform` → `work_logs` → `registry_addresses` → 身份证 HMAC/标记快照回填 → 最后启用 Registry/Workflow 功能。前六个阶段和 HMAC 回填已完成；后续开启 Registry/Workflow 前仍要单独备份、核对权限和回退条件。

示例（在后端容器或具备生产依赖的维护环境执行，不在本机执行）：

```text
python -m migrations.domain_migration measure --schema OnlineData
python -m migrations.domain_migration migrate --domain visit
python -m migrations.domain_migration migrate --domain visit --apply
python -m migrations.domain_migration verify --domain visit
python -m migrations.domain_migration migrate --domain platform --apply
python -m migrations.domain_migration verify --domain platform
```

Registry/Workflow 开关在全部迁移和权限核验完成前保持关闭。新库产生业务写入后不能自动切回旧表；异常时先冻结入口、备份新库，再制定反向复制方案。附件目录由 `BINHU_WORKFLOW_ATTACHMENT_DIR` 指定，数据库备份 manifest 同时记录八库段摘要和附件归档摘要。

### 2026-08-08 走访域迁移结果

- 维护入口期间停止同步、每日备份和在线回写，完成迁移前八库备份后执行 `migrate --domain visit --apply`。
- `OnlineData._visit_import_batches` 35 行、`t_visit_details` 10,678 行、`_visit_import_issues` 299 行，均已复制到 `VisitData`；三张表的结构、行数和主键边界全部一致，`verify --domain visit` 返回 `consistent=true`。
- `BINHU_VISIT_DOMAIN_ACTIVE=true` 已生效，后端实际 SQL 路由到 `VisitData`；旧 `OnlineData` 走访表保留不删除，作为只读回退材料，不进行长期双写。
- 迁移后八库备份已保存到独立备份盘：`/backup/binhu/migration/0.16.0-visit-migrated-20260808T105005Z-eight-db.sql.gz`，大小 5,116,192 字节，SHA-256 为 `41af35ccd45c61d9b9b007efda456efecbe9da4edf6e84bd0e444e28a68cf9a9`，压缩完整性和八库标识均已核验。
- Nginx 已恢复 `new-production`；同步按 5 分钟、每日备份按 02:00 且保留 7 天、在线回写总开关已恢复。`DISPATCH_DOMAIN_ACTIVE`、`PLATFORM_DOMAIN_ACTIVE`、`DAILY_DOMAIN_ACTIVE`、`REGISTRY_ADDRESS_DOMAIN_ACTIVE` 仍保持关闭。

### 2026-08-08 下发与小区地址域迁移结果

- 第二个维护窗口同时迁移 `dispatch` 和 `registry_addresses` 两个独立路由域；迁移期间维护页生效，同步、每日备份和在线回写关闭，运行中的同步、备份和下发发布任务均为 0。
- `DispatchData` 已创建 `_police_dispatch_batches`、`_police_dispatch_tasks`、`_police_dispatch_publish_results`，源库和目标库均为 0 行；字段、主键范围和索引定义一致。
- `RegistryData` 已接收 `_police_address_entries` 93 行、`_police_address_sources` 93 行、`_police_address_imports` 2 行、`_police_address_import_conflicts` 0 行；结构、数量、主键范围和索引定义均与 `OnlineData` 一致。
- 迁移前八库备份为 `/backup/binhu/migration/0.16.0-phase2-pre-migration-20260808T142953Z-eight-db.sql.gz`，SHA-256 为 `c4e84e652cd3ac8d14e12ce84f5474669312e0dcfde4f4cb1186025211fc785d`。
- 迁移后八库备份为 `/backup/binhu/migration/0.16.0-phase2-dispatch-address-migrated-20260808T143135Z-eight-db.sql.gz`，大小 5,061,835 字节，SHA-256 为 `d62465771c8b3ba43ece62d4a40ab5bdbf906e8513348b9a818be91b268a6725`；gzip、八库标识和 manifest 均已核验。
- `BINHU_DISPATCH_DOMAIN_ACTIVE=true`、`BINHU_REGISTRY_ADDRESS_DOMAIN_ACTIVE=true` 已生效；`VISIT_DOMAIN_ACTIVE=true` 保持不变。平台、日报、Registry/Workflow 功能仍关闭，旧表全部保留且不双写。
- Nginx 已恢复 `new-production`，新直达入口和旧云透明代理入口均返回 `0.16.0`；同步恢复为 5 分钟、每日备份恢复为 02:00/保留 7 天、在线回写恢复开启。恢复后的自动同步任务 `3411` 已成功完成 12/12 步，近期后端和运维代理错误日志为空。

### 2026-08-08 平台基础域迁移结果

- 第三个维护窗口迁移 `platform` 域；维护期间同步、每日备份和在线回写关闭，迁移前八库备份已完成并通过 gzip、八库标识和 SHA-256 校验。
- `PlatformData` 共迁移 24 张平台基础表，源库与目标库的字段结构、行数、单列主键边界和关键表索引定义均一致。关键数量为用户 75、人员 72、用户权限组关系 3。
- `_sync_schedule` 与 `_sync_log` 仍按当前代码白名单保留在 `OnlineData`，不属于本阶段遗漏；同步任务已实际成功完成 12/12 步。
- `BINHU_PLATFORM_DOMAIN_ACTIVE=true` 已生效，后端与运维代理已重建并恢复生产配置；`BINHU_VISIT_DOMAIN_ACTIVE`、`BINHU_DISPATCH_DOMAIN_ACTIVE`、`BINHU_REGISTRY_ADDRESS_DOMAIN_ACTIVE` 继续为真，`BINHU_DAILY_DOMAIN_ACTIVE=false`、`REGISTRY_FEATURE_ENABLED=false`、`WORKFLOW_FEATURE_ENABLED=false` 继续保持关闭。
- 迁移前备份：`/backup/binhu/migration/0.16.0-platform-pre-migration-20260808T155700Z.sql.gz`，SHA-256 为 `8c3db54c52efc07192ef345ea7eaac00578aa7f04d8cfeae21f0007f772aee6a`。
- 迁移后备份：`/backup/binhu/migration/0.16.0-platform-migrated-20260808T160239Z.sql.gz`，SHA-256 为 `98e9c457df9c957354fa9af619baeddab923b40ca3625014fad178e7730636e2`。
- 只读验收：三个生产容器正常，健康接口返回 `0.16.0`，迁移状态表显示 `dispatch`、`platform`、`registry_addresses`、`visit` 均为 `copied`，近期后端和运维代理错误日志无新增错误。

### 2026-08-08 日报工作日志域迁移结果

- 第四个维护窗口先切换 `new-maintenance`，等待自动同步任务 `3428` 成功完成后，关闭同步计划、每日备份计划和在线回写总开关。
- 迁移前八库备份已完成并通过 gzip、八库标识和 SHA-256 校验：`/backup/binhu/migration/0.16.0-worklogs-pre-migration-20260808T163351Z.sql.gz`，SHA-256 为 `2664fbf2d677914b44e7585a79bc66b709286264a8353a3630ccaa00ec8990b4`。
- 执行 `migrate --domain work_logs --apply` 和 `verify --domain work_logs`：`OnlineData._work_log_drafts` 与 `daily_report._work_log_drafts` 均为 3 行，字段结构和主键边界一致，验证结果为 `consistent=true`。
- 迁移后八库备份已完成并校验：`/backup/binhu/migration/0.16.0-worklogs-migrated-20260808T163528Z.sql.gz`，SHA-256 为 `6f36ed8dbaa9a6f5c361103e79127411f86d3454e3e68468a0a81f54b18822c9`。
- 已保存环境回退副本 `/srv/binhu/.env.worklogs-pre-switch-20260808T163552Z`，随后启用 `BINHU_DAILY_DOMAIN_ACTIVE=true` 并重建后端、运维代理。应用实际将 `_work_log_drafts` 路由到 `daily_report._work_log_drafts`。
- 已恢复 `new-production`、5 分钟同步、02:00 每日备份/保留 7 天和在线回写；同步任务 `3429` 已成功完成。当前 `REGISTRY_FEATURE_ENABLED=false`、`WORKFLOW_FEATURE_ENABLED=false`，新档案、人员标签和工单功能仍关闭。
- 三个生产容器正常，健康接口返回 `0.16.0`，近期后端和运维代理错误日志为空。旧 `OnlineData._work_log_drafts` 保留，不删除、不长期双写。

### 2026-08-08 身份 HMAC 与任务标记快照回填结果

- 只读预览确认 `BINHU_REGISTRY_HMAC_KEY` 未配置，当前投影摘要使用应用密钥回退；在线投影共 673 条，其中 339 条有身份证字段，334 条没有可用身份证字段，格式异常为 0。
- 在维护状态下生成专用 HMAC v1 密钥，保存到服务器私密配置，并以 root-only 文件备份；密钥值不进入数据库、日志、Git 或发布包。
- 339 条已有身份证摘要全部按专用密钥重算，正式回填 339 条；随后复核现有摘要与当前值 0 条不一致，334 条无身份证字段继续为空。
- `RegistryData.watch_people`、`watch_assignments` 和 `online_task_watch_snapshots` 均为 0 条，没有执行虚构标记或测试任务写入。
- HMAC 回填后的八库备份：`/backup/binhu/migration/0.16.0-hmac-backfilled-20260808T174803Z.sql.gz`，SHA-256 为 `b283849786e3837ae611747afa647d0025c6b1ad776433b4fb6368b8888549a6`。
- 重建后第一次同步 `3442` 因后端重建被取消，未产生部分业务写入；下一次同步 `3443` 成功完成 12/12 步。当前健康接口、容器、同步、每日备份和在线回写均已恢复，Registry/Workflow 功能仍关闭。

### 服务器显示时间 2026-08-09 的 Registry/Workflow 开放结果

- 按项目管理人明确授权，服务器保持现有系统时间，不执行回拨；本次操作的逻辑迁移窗口记录为服务器显示的 `2026-08-09`，不代表修改了系统时钟。
- 维护期间先切换 `new-maintenance`，关闭同步计划、每日备份计划和在线回写；创建并校验八库备份后，启用 `BINHU_REGISTRY_FEATURE_ENABLED=true` 与 `BINHU_WORKFLOW_FEATURE_ENABLED=true`，重建 `binhu-backend`、`binhu-ops-agent`，再恢复 `new-production`。
- 生产验收：健康接口返回 `0.16.0`；三个容器正常；RegistryData 26 张表、WorkflowData 14 张表；辖区档案、人员标签、任务快照和工单均为 0 行；照片调取流程 1 个已发布版本/1 个基础管控处理节点，请假流程保持未发布。
- 权限核验：社区民警使用 `community_registry_viewer` 且仅有 `own_department` 只读档案权限；内勤岗位可维护全所档案和处理工单；超级管理员拥有 `workflow.config.manage`；功能权限没有因开关开启而扩大。
- 备份副本：`/backup/binhu/migration/0.16.0-registry-workflow-pre-switch-20260808T192125Z.sql.gz`；SHA-256 为 `c942942ea0af8768f9fbdb25e3c3c389e22fe373ccb90d7a8a9c629882c9fc68`。同名 manifest 和附件归档一并保留；附件目录为空。
- 恢复后同步计划为 5 分钟、每日备份为 02:00/保留 7 天，在线回写维持切换前的关闭状态；最近同步任务 `3459` 成功，后端与运维代理近期错误计数为 0。旧表、旧备份和迁移副本均未删除。

## v0.17.0 超级管理员预约维护

- 入口：系统设置中的“平台维护模式”。开始时间为空表示立即维护，结束时间为空表示持续到超级管理员手动关闭；填写结束时间后会自动恢复。
- 保存时页面按系统设置时区显示，后端统一换算并保存 UTC。不要为了配合预约维护修改宿主机系统时钟。
- 维护期间普通用户无法新登录，已有普通会话访问业务接口也会收到 503 并回到登录页；超级管理员仍可登录、关闭或调整维护配置。
- 登录页公开接口只返回非敏感维护状态。发布后可在未启用维护时直接检查 `/api/maintenance/status`，不需要创建测试业务数据。
- 应用级维护不能阻止健康检查、静态登录页和超级管理员请求。数据库迁移、容器重建、Compose 切换或需要完全冻结入口时，继续使用 Nginx `new-maintenance`。
- 回退时恢复上一版程序即可；旧版本会忽略新增的 `_system_config` 键，不需要恢复数据库备份。

### 0.17.1 维护模式交互修复

- 系统设置中的维护状态改为“关闭维护、立即维护、预约维护”三种明确选项；只有选择预约维护时才显示开始和结束时间。
- 关闭或立即维护会清除预约时间，避免保存时误把旧预约时间带入新状态；预约维护必须填写开始时间，结束时间可选。
- 登录页维护提示拆分为维护说明和预计恢复时间，适配窄屏与深色模式，不再展示角色说明文字。

### 0.17.2 照片调取批次与维护页间距

- 照片调取工单可以从流口任务详情直接发起，自动保存对象姓名、身份证摘要和任务关联信息；没有身份证号的任务不能发起可自动匹配的照片申请。
- 基础管控在数据上传中心上传 ZIP，文件名使用 `姓名_身份证号.jpg`。上传先生成预览批次，确认后才把照片保存为工单附件、完成处理中工单并通知申请人。
- 同一身份证号存在多个处理中照片工单时，照片全部挂载；姓名只用于人工核对提示。重复 ZIP、重复照片、无对应工单、格式错误和超限内容均保持幂等或进入异常明细，不自动改动其他工单。
- ZIP 暂存目录受保护，批次接口和普通日志不返回完整身份证号；工单详情沿用现有附件权限。生产发布涉及 `WorkflowData` 新表和附件写入逻辑，备份范围必须使用 `all`，真实照片批次由项目管理人验收，Agent 不上传真实照片。
- 系统设置的维护模式卡片使用专用间距样式，只收紧本卡片，不改变全局卡片、分页器和下拉框布局。

### 0.17.3 Univer 长数字自动回写防护

- Query 工作表不再在编辑结束后扫描整张表并把全部差异视为用户修改；单元格编辑、粘贴和清空只处理本次明确涉及的区域。
- 身份证号、长手机号和 `7.30` 这类文本若被 Univer 内部转换为数字或丢失尾零，前端立即恢复原值并停止提交；后端对没有明确文本编辑标记的同类精度变化再次拒绝，拒绝前不创建成功回写审计。
- 相同值提交在读取腾讯当前物理行后直接拒绝，不发送腾讯更新请求。真实人工编辑仍按物理文本/下拉类型、行哈希、工作表锁和写后回读流程处理。
- 本版不迁移数据库结构。生产发布使用 GitHub `Deploy production`，备份范围至少选择 `online`；部署和只读检查期间保持在线回写关闭，由项目管理人使用现有任务验收后再单独决定是否开启。

### 0.17.4 仪表盘、手机 Dock 与照片文件名修复

- 桌面仪表盘卡片改为双列瀑布流，左右卡片独立向下排列；手机端保持单列，避免高度不一致时留下大块空白。
- 手机 Dock 移除单独“首页”按钮，只展示首页之外最多四个分类；点击手机顶部“滨湖智慧平台”返回仪表盘。电脑侧栏的仪表盘入口保持不变。
- 照片批次兼容 UTF-8 和 Windows 未标记 GBK 的 ZIP 文件名，历史预览批次读取和确认时也会修复旧乱码。受权限保护的批次结果页显示完整文件名、姓名和身份证号，普通日志、通知和操作记录仍不保存这些内容。
- 本版不修改数据库结构，但调整照片批次确认的兼容写入定位；当前发布工作流没有单独的 `workflow` 备份范围，因此生产发布使用 `backup_scope=all`。上线后检查桌面仪表盘、手机四分类 Dock、顶部品牌返回首页，以及一份由项目管理人提供的真实照片 ZIP 预览。Agent 不确认导入、不完成真实工单。

### 0.17.5 照片批次文件持久化修复

- 照片预览 ZIP 不再保存在后端容器自身的临时文件系统。Compose 把服务器 `${BINHU_WORKFLOW_PHOTO_IMPORT_DIR:-/data/binhu/workflow-photo-imports}` 挂载到容器 `/app/workflow-photo-imports`，容器重建和普通版本发布不会再丢失等待确认的批次文件。
- 如果数据库已有同一 ZIP 的预览记录、但原文件因旧版本容器重建而丢失，基础管控重新上传完全相同的 ZIP 时会锁定原批次并恢复文件，不新建重复批次，也不改写原预览明细。
- 点击确认时如果原文件仍不存在，页面明确提示“重新上传同一个 ZIP 恢复后再确认”，不再只显示难以处理的文件不存在错误。
- 本版不修改数据库结构和腾讯回写逻辑。发布使用 `backup_scope=none`，保留 `0.17.4` 镜像；服务器切换后必须检查真实绑定挂载、目录权限、健康接口和近期日志。现有失效批次由项目管理人重新上传原 ZIP 恢复，Agent 不接触真实照片内容。

### 0.17.6 工作每日明细与走访人数

- “文件生成”页面以两个独立 Panel 区分工作每日明细 XLSX 与工作日志 PDF。每日明细无需创建草稿，按所选业务日期读取实时在用人员名册、出租房/自购房走访人员汇总和在线日报人员快照，只填写入户与指令核查板块。出租房、自购房目标数默认 10、15，导出成功后按账号记住。
- 发布包只包含脱敏结构模板，不包含项目管理人提供的原始工作簿、样例姓名、数字、备注或背景色。导出操作记录仅保存业务日期、人员行数、目标数和文件格式。
- 走访社区汇总在“在岗人日”前增加“网格员人数”，使用区间结束日的组长、组员有效社区名册；页面筛选总计和 XLSX 均按当前可见行重算该列。
- 本版不新增业务表或不可空字段，但改变日报与走访统计读取和 XLSX 生成。生产发布使用 GitHub `Deploy production`，版本填写 `0.17.6`，`backup_scope=all`。上线后检查健康版本、每日明细文件版式、社区汇总列顺序、导出操作记录和近期日志；真实业务日期的文件由项目管理人下载验收，Agent 不制造生产走访或核查数据。

### 0.17.7 流口任务卡片与 Univer 交互修复

- 流口任务列表不再把宽屏强行压成 7～8 个窄卡片，而是按不小于约 236px 的可读宽度自适应排列，常见宽屏约六列。姓名、主状态、复核标记、社区归属和来源分层展示，身份证号与手机号使用横向等宽数字，避免逐字折行。
- 在线数据查询在点击、编辑、框选和拖动 Univer 工作表时同时保持主内容区与浏览器文档纵向位置，并覆盖松开指针后的延迟聚焦和布局更新，避免页面跳回顶部。
- 单元格成功写回后只重绘受影响行并更新待同步状态，不再重绘整张工作表，也不因关键词或列筛选自动重新查询；写回失败或版本冲突仍重新加载真实来源以保证安全。
- 本版不修改数据库结构、同步计算或腾讯写回协议。生产发布使用 GitHub `Deploy production`，版本填写 `0.17.7`，`backup_scope=none`；回退使用上一版程序和前端产物即可。上线后检查健康版本、任务卡片宽度、页面中部点击表格不跳顶、单元格保存不闪动和近期启动日志。

### 0.17.8 流口任务分配、来源标签与地址聚类

- 批量分配从“仅组长显示”扩展为组长、片长、基础管控、中队长、所队领导和管理员账号按实际 `online.raw.edit` 范围使用。组长仍限本人社区；上级岗位不能借全所查看范围突破其编辑范围。前端选择第一条后锁定同一社区，后端按正式社区和别名再次检查任务范围，并且只接受该社区在岗组员。
- 全链条来源在列表和详情中按空白及常用分隔符拆成去重标签云，不改写数据库或腾讯原始来源。默认优先级排序在每个优先级桶内按去空白后的业务地址聚类，同地址相邻、空地址靠后，显式更新时间排序保持原语义。
- 疑似未注销模型三在流口任务详情中开放“备注”文本编辑；它沿用现有工作表锁、来源版本、行哈希、批量回读和审计，不参与任务完成状态或统计口径。
- 本版不修改数据库结构，但改变在线任务回写字段和批量分配权限。生产发布使用 GitHub `Deploy production`，版本填写 `0.17.8`，`backup_scope=online`；回退保留上一版程序和镜像，不自动恢复数据库。上线后只读检查岗位入口、来源标签和地址排序，真实批量分配与模型三备注由项目管理人使用现有任务验收。

### 0.17.9 人员电话可见范围修复

- 能进入人员管理的账号在桌面表格和手机卡片中都可以看到人员电话，并可以使用电话搜索人员；不再要求额外的 `personnel.sensitive.view`。
- 备注、请假原因、完整出勤记录、人员 CSV 导出和身份证号权限不随本次调整扩大；公开个人主页继续不返回手机号。
- 本版自身不修改数据库结构、同步或腾讯回写逻辑；但生产服务器尚未切换到 `0.17.8`，因此本次会连同流口任务回写字段和批量分配修复一起上线。生产发布使用 GitHub `Deploy production`，版本填写 `0.17.9`，`backup_scope=online`；此前 `0.17.8` 发布因同步或备份任务处于活动状态安全退出，服务器未切换程序。

### 0.17.10 流口核查结果保存与详情宽度修复

- 生产只读审计确认，2026-08-10 多次“核查结果”回写均进入失败状态：腾讯来源行是下拉类型，但区域更新接口不支持写入 `cellValue.select`，请求后的回读结果为空，前端按失败规则重新加载后表现为刚选的“已登记”消失。
- 新版保留下拉选项合法性校验，但实际按腾讯官方支持的文本值写入；写后仍读取同一物理行并逐字段确认，失败不会伪装成成功。详情页保存响应还会把选项 ID 还原为显示文字，避免不完整响应覆盖当前表单。
- 流口任务详情页只在桌面端收窄到 1240px，手机端继续使用满宽布局，任务列表的多列卡片宽度不受影响。
- 本版改变腾讯在线回写协议，不改数据库结构。生产发布使用 GitHub `Deploy production`，版本填写 `0.17.10`，`backup_scope=online`；上线后检查健康版本、近期错误日志和失败审计不再增长，真实“已登记”“离苏”及模型三结果由项目管理人使用现有任务验收。

### 0.17.22 流口平均分配、工作表文本保护与卡片日期

- 流口任务的选择模式增加吸顶操作栏，滚动时持续显示已选数量；同一社区任务可以继续指定一名组员，也可以在确认预览后按连续区段尽量平均分给全部在岗组员。平均分配只由用户主动触发，后台不会自动均分。
- Univer 工作表补齐内部剪贴板回退，社区和核查人可以复制粘贴；日期和时间列在前端保留编辑原文，后端即使读到腾讯数值型元数据也固定按文本写回，防止新的 `8.10` 被缩成 `8.1`。腾讯已经只返回 `8.1` 的历史值无法据此判断原文，不自动补零。
- 流口卡片只展示来源行的截止日期并格式化为 `MM-dd`，不再用首次下发日期覆盖；身份证号、手机号和地址使用统一的行高、间距和字重。
- 本版不修改数据库结构，但改变腾讯日期写回和任务批量分配行为。正式发布应使用 `backup_scope=online`，上线前先做只读来源核对；真实复制粘贴、日期保存和平均分配由项目管理人使用现有任务验收。验收前不创建 `v0.17.22` 标签。

### 0.18.0 调照片名单工单化与双向同步

1. 使用 GitHub `Deploy production` 发布已经进入 `main` 的 `0.18.0`，`backup_scope=all`。本版会在 `WorkflowData` 增加兼容表和可空字段，旧数据不删除。
2. 部署后先确认健康接口版本、工单中心和工单流程配置页；此时调照片名单读写开关默认关闭，不会访问或修改腾讯名单。
3. 超级管理员在“工单流程配置 → 调照片名单”保存腾讯地址和表头行，仍保持读写关闭，点击“只读预览”。核对申请数、批次边界、最后边界后待办、异常身份证、重复组和申请人映射。
4. 只有预览结果与在线表一致后，才在维护窗口点击“确认正式导入”。导入令牌绑定当次腾讯内容，期间表格变化会拒绝执行并要求重新预览。
5. 导入完成后核对工单总数、基础管控待领取队列、历史完成分类、异常列表和近期日志，再先开启读取、执行一次完整同步；确认没有重复工单后再开启名单写入。
6. 名单写入同时受名单写入开关和全局在线回写开关控制。关闭任一开关只积压 outbox，不撤销本地工单。G 列失败也不得恢复数据库或删除照片。
7. 使用自然产生的一条真实申请完成“腾讯新增 → 待领取 → ZIP 照片 → 工单完成 → G 列标记”验收。验收前不创建 `v0.18.0` 标签。

回退：兼容代码失败时关闭照片名单读写开关并恢复上一版程序/镜像；新增表和可空字段可保留。若已经正式导入历史工单或产生新工单，不得自动导入数据库备份或删除这些记录，应先冻结同步、备份八库并制定数据保留方案。

### 0.18.1 腾讯接口传输超时修复

腾讯文档偶发返回 HTTP 200 但业务码 `400010`，并在错误信息中包含 `ReadFrame`、`transport` 或 `i/o timeout`。这表示上游 TCP 读取超时，不表示表格被清空，也不应触发历史数据归档。

- 客户端对明确的传输型 `400010` 最多重试 3 次，并使用递增等待；参数、权限和其他业务错误不重试。
- 预览失败时保持已有预览结果，不执行历史导入；只有预览统计稳定且经人工核对后才允许正式导入。
- 发布后先保持调照片名单读取、写回开关关闭，重新执行只读预览，比较行数、批次边界和异常数量后再决定下一步。

### 0.19.0 Windows、Android 客户端兼容协议

- 原生客户端通过 `X-Binhu-Client-Platform` 和 `X-Binhu-Client-Version` 报告平台与 SemVer，启动时调用 `/api/app/bootstrap` 取得服务端版本、最低版本、维护状态、业务时间、账号权限和平台功能开关。
- 部署变量为 `BINHU_WINDOWS_MIN_SUPPORTED_VERSION`、`BINHU_ANDROID_MIN_SUPPORTED_VERSION`、`BINHU_CLIENT_WRITE_VERSION_ENFORCEMENT_ENABLED` 和 `BINHU_CLIENT_WRITE_IDENTIFICATION_REQUIRED`。首次上线两端最低版本保持 `0.0.0`、身份必填保持关闭，确认网页和正式原生客户端都已经发送请求头后再提高最低版本并开启身份必填。
- 提高最低版本后，已声明 Windows 或 Android、但版本缺失、非法或过旧的客户端写请求返回 `426 client_upgrade_required`。这项检查只处理接口兼容性，不能替代登录、权限、数据范围和业务版本冲突校验。
- 本版不修改数据库结构和业务数据，正式发布使用 `backup_scope=none`；上线后检查健康版本、匿名及登录 bootstrap、维护状态、网页端正常写入和一组模拟旧客户端 `426`。回退时恢复上一版程序即可；紧急情况下可以先关闭写版本检查，但不得借此绕过业务权限。

### 0.19.1 照片调取工单创建修复

- 修复照片工单详情写入语句少一个参数占位符导致的 HTTP 500。事务在错误时会完整回滚，因此旧故障不会留下半截工单或腾讯名单写回队列。
- 增加照片快捷申请完整创建路径测试，直接校验 SQL 占位符与参数数量一致。本版不修改数据库结构和业务规则，正式发布使用 `backup_scope=none`；回退时恢复 `0.19.0` 程序和镜像即可。

### 0.19.7 腾讯批次结单后的照片补挂

- 本版不改数据库结构，但会重新计算既有照片导入批次并写入工单附件，正式发布使用 `backup_scope=all`。
- 对因腾讯时间分隔行先行结单而全部未匹配的 ZIP，基础管控重新上传完全相同的 ZIP；平台会复用原批次并返回新的预览，不会创建第二个批次。
- 确认前核对匹配、未匹配和姓名警告数量；确认后检查目标工单附件、`photo_import_complete` 事件、申请人通知和“已处理”列表。未匹配照片继续保留在批次明细，不猜测绑定。
- 补挂范围只包括最近一次腾讯批次、七天内自动结单且没有附件的工单，避免一万多条历史完成工单被新 ZIP 误命中。
- 若确认过程失败，数据库事务回滚并清理本轮新保存的附件文件；原工单完成状态和原批次记录保持不变。

### 0.19.9 小区管理、下发预览与工单审计

- 小区管理按功能权限和数据范围开放：组长、组员只能增删改查及导出本人社区的小区，管理员和原全所管理岗位继续管理全部社区。
- 小区删除会在事务中清理地址来源关联并删除主记录；导入批次和操作审计继续保留。生产发布前备份 `RegistryData`。
- 已处理数据直接下发调整为“上传预览、确认导入”两步，确认时重新校验文件摘要和预览令牌；生产发布前备份 `DispatchData`。
- 工单领取、处理决定和补充材料重新入队补充安全操作记录，不记录备注正文或敏感业务内容。

### 0.19.11 已注销对象重新下发与全链条发布兼容

- 基础管控上传的已处理数据中，登记情况为“流口已注销”“已注销”或“注销”的记录不再自动归入无需登记，而是按文件社区进入正常下发流程，由网格员重新核查；原始数据人工审核仍保留“无需登记”动作。
- 发布到新版 15 列全链条表时写入原始登记情况；旧 14 列表继续按原布局发布。写后回读、相同业务主键检查和同步对账只比较腾讯物理表实际存在的列，旧表不会因为缺少登记情况而误报冲突。
- 本版不修改数据库结构。正式发布使用 `release_scope=auto`、`backup_scope=all`；上线后只读检查健康版本、下发预览口径和近期发布错误，不制造测试下发或测试腾讯行。若异常，恢复 `0.19.10` 程序和镜像；已产生的真实腾讯写入不得通过数据库回滚撤销。

### 0.19.8 工单入口与附件权限修复

- 程序启动时为旧 `flow_post` 权限组补充 `workflow.attachment.view`，为旧 `internal_business` 权限组补充 `workflow.ticket.create` 和 `workflow.ticket.view`；中队长重新登录后应看到工单中心。
- 组员只能预览、下载其本来就有权查看的工单附件。上传和删除接口改为要求工单处理或管理权限，不能通过直接调用接口绕过页面按钮。
- 本版不修改数据库结构和业务数据，只更新系统权限组 JSON；发布使用 `backup_scope=all`。上线后检查中队长导航、组员附件预览/下载、组员无上传/删除按钮及近期 403/500 日志，不制造测试工单或测试附件。

### 0.19.12 任务入口拆分与人工复核归队

- “工单中心”移入工作台；任务处理下增加独立“研判”和“调照片”入口，并保留原“下发任务处理”。旧手机 Dock 会把原工单入口迁到工作台，并为已有下发入口补齐研判入口；具备照片处理权限的基础管控同时补齐调照片入口。
- 已处理文件确认导入后，人工复核记录仍保存在原下发批次；页面直接打开该批次的待审核任务，整批未完成审核时发布接口继续拒绝发布。该版本不迁移数据库、不改变腾讯读写协议，发布使用 `backup_scope=none`、`release_scope=auto`。
- 上线后分别以基础管控和普通工单发起人检查导航：基础管控可见下发任务处理、研判、调照片和工作台工单中心；普通发起人只按权限看到工单中心。再检查旧 Dock 配置、健康接口版本和近期 403/500 日志，不导入测试文件、不领取真实照片工单、不发布腾讯任务。

### 0.19.13 统一研判工作台口径修正

- “研判”不再等同于下发文件人工复核。页面默认打开“网格核查研判”，跨五类流口业务筛选核查结果为“无法核实”且研判字段为空的数据；可切换到“下发数据复核”，处理原下发批次中的人工判断记录。
- 网格核查研判复用流口任务详情、编辑权限、来源版本和腾讯回写校验；下发数据复核继续使用原批次审核和发布约束。该版本不修改数据库结构或腾讯写回协议，发布使用 `backup_scope=none`、`release_scope=auto`。
- 上线后以基础管控账号检查两个页签、五类业务切换、等待研判数量、任务详情研判字段和返回列表刷新；不人为修改真实任务做自动验收，最终保存由业务人员使用自然产生的“无法核实”数据确认。

### 0.19.14 研判权限、专用保存与统一列表工具栏

- 新增 `online.task.manage`，默认授予片长、基础管控、中队长、社区民警、所队领导、管理员和超级管理员。该权限启动时幂等补入 `PlatformData` 权限组，只在流口任务工作台提供全所查看、编辑现有任务和分配核查人，不开放 Univer 或原始行增删。
- 网格核查研判可切换待研判/已研判，筛选数量按当前状态重算；研判详情和保存使用专用接口，普通详情不暴露输入框，保存仍执行腾讯来源版本、行哈希、回写开关和并发校验。
- 任务、工单和基础资料列表统一为即时筛选工具栏，关键词防抖并阻止旧响应覆盖新结果；任务详情的手机号和新旧地址可直接复制，拨打电话和调取照片使用胶囊按钮。
- 发布使用 `backup_scope=all`、`release_scope=auto`。部署前等待现有同步任务结束；上线后核对权限组写入、研判两种状态、社区民警任务能力、普通组员隔离、调照片工具栏、健康接口和近期 403/500 日志。验收前不创建标签。
- 腾讯名单追加位置按 A–G 实际非空尾行计算，不再使用工作表容量。上线后执行一次完整同步，使被人工移动的现有平台申请按唯一摘要从旧物理行自动重定位；核对工单和腾讯正文均未重复创建。
- “立即同步”完整读取可能超过三十秒，前端最长等待五分钟。按钮执行时先清空到期的待写回队列，再读取全表；页面超时不能作为后端同步失败或腾讯未写入的判断依据。
- 数据下发增加“已处理数据直接下发”模式，并为批次增加兼容字段 `import_mode`。启动时幂等补列；自动确认只处理字段齐全、身份证格式正确、社区与登记状态可识别且同批不重复的记录，其他行仍需人工审核。最终发布继续使用现有按钮和腾讯安全写回流程。上线后只用自然业务文件验收导入统计，不为测试向腾讯发布数据；回退旧程序时新增字段可保留。

### 0.19.17 已处理下发整批发布与卡片统一

- 已处理直发批次允许先发布已审核通过的任务，待复核、重复和异常记录留在原批次继续处理；原始审核批次仍要求全批完成后再发布。
- 整批发布入口同时出现在待发布工作台、批次详情和数据上传中心批次列表，按钮直接显示可发布数量；进入单条已审核任务时只提示使用整批发布，不再显示容易误解的逐条保存按钮。
- 下发任务卡片与流口指令核查卡片统一信息层级和复制交互，平台建议放在研判结果对应位置。
- 发布涉及下发写入条件和前后端页面，使用 `backup_scope=none`、`release_scope=auto`；上线后核对版本、待发布数量、异常记录隔离、整批发布按钮、不同状态卡片、深浅色模式和近期 409/500 日志。真实腾讯写入由业务自然批次验收，不制造测试行；验收前不创建 `v0.19.17` 标签。

### 0.19.18 修复下发任务详情运行时崩溃

- 修复下发任务详情仍引用已移除的 `CopyButton`，导致待发布页面打开任务后出现 `ReferenceError` 的问题；详情中的身份证号、手机号和原地址统一复用现有点击复制组件。
- 增加静态回归检查，禁止该页面再次出现未定义的旧组件引用。本版仅修改前端和版本号，生产发布使用 `backup_scope=none`、`release_scope=auto`，上线后检查待发布列表、任务详情和三类字段复制，不制造或发布腾讯测试数据。

### 0.19.19 修复 Univer 长数字显示精度损失

- 在线数据查询把身份证、证件号、手机号和电话类列显式载入为精确文本单元格，关闭强制文本提示和前导标记显示，避免18位身份证经过 JavaScript 数字路径后尾数变成 `0`。
- 单元格保存失败恢复原值时继续沿用列类型，防止恢复动作再次触发精度损失。腾讯回写端即使收到错误的数值型元数据，也把身份证和电话类字段固定写成文本，避免显示错误经人工编辑后反写腾讯。
- 本版不修改生产数据库中的身份证数据，也不自动恢复历史写回事故。发布前只读核对来源行、来源投影和业务表保存的字符串；发布使用 `backup_scope=online`、`release_scope=auto`。上线后强制刷新在线数据查询，核对已知行尾号与数据库原值一致，再检查近期回写错误日志；验收前不创建标签。

### 0.19.20 下发页面职责与批次详情入口调整

- 数据上传中心只保留文件上传、已处理数据预览、确认导入、查看处理进度和反馈下载，移除历史批次列表中的整批发布入口；上传完成后仍直接进入对应批次的待审核或待发布工作台。
- 下发任务处理在当前批次选择器旁增加明确的“批次详情”入口。批次详情保留历史倒查、复盘、反馈导出、发布异常与对账处理能力；日常审核和整批发布仍集中在任务工作台。
- 本版仅调整前端导航与文案，不修改数据库或腾讯回写协议，发布使用 `backup_scope=none`、`release_scope=auto`。上线后检查数据上传中心无发布按钮、工作台批次详情入口、历史批次切换、整批发布和反馈下载；验收前不创建标签。

### 0.19.21 副开发 PR 整合：照片标记、附件名称与个人头像

- 照片 ZIP 正式导入后，照片附件以“姓名_身份证号.扩展名”保存和下载；历史平台生成名称在列表与下载时动态转换，不修改既有文件路径。普通工单附件保持原名。
- 流口任务列表批量查询当前账号有权查看、已完成且确有图片附件的快捷调照片结果，并显示“已调照片”标签；权限判断与任务详情附件一致，不按身份证号跨任务串联。
- 下发任务工作台顶部改用主题变量，修复浅色模式文字和背景对比度。个人中心增加头像上传，支持 JPG、PNG、WebP、HEIC，最大 5MB；用户表增加可空头像存储键和 MIME 字段。
- 头像目录固定挂载到 `${BINHU_USER_AVATAR_DIR:-/data/binhu/user-avatars}`，容器重建后继续保留。数据库更新失败时删除新文件，替换成功后安全清理旧头像；头像接口要求登录并禁止浏览器缓存。
- 发布会幂等增加 `PlatformData._users` 的两个可空字段并新增持久化目录，使用 `backup_scope=all`、`release_scope=auto`。上线后检查八库备份、用户字段、头像挂载、深浅色下发工作台、已调照片标签、附件下载名称和近期错误日志；不上传测试头像、不下载真实照片，验收前不创建标签。

### 0.19.23 在线数据查询全量排序

- 在线数据查询的桌面 Univer 工具栏增加排序字段、升序/降序和清除排序；字段来自当前业务的实际列。
- 排序通过现有后端 `sort_by`、`sort_order` 对权限过滤、关键词和表头筛选后的完整结果执行，再按页连续加载；不在浏览器内重排当前片段，腾讯来源行号、版本和写回关系保持不变。
- 切换业务类型或当前/归档数据时清除排序；未保存的新增草稿继续独立保留在真实数据末尾。发布仅涉及前端接入既有接口，使用 `backup_scope=none`、`release_scope=auto`；上线后核对全量排序、筛选组合、长数字文本和编辑写回，验收前不创建标签。

### 0.19.24 下发任务多选发布

- 移除工作台和批次详情的整批发布入口；数据上传中心继续只负责上传、预览和导入。发布统一在下发任务工作台进入“选择发布”模式后完成。
- 选择模式支持逐卡多选、高亮、持续显示已选数量，以及覆盖当前筛选所有分页的“全选当前筛选”。全选最多返回 5000 条真正可发布任务，超过时要求先缩小筛选范围。
- 正式发布只处理请求正文中的任务 ID。后端在事务和行锁内重新核对批次、最终动作、待发布状态、可重试状态、手机号及完整重复人员组；混入跨批次任务或任一状态已变化时整次拒绝。
- 本版不修改数据库结构，但会影响腾讯下发写入路径；发布使用 `backup_scope=all`、`release_scope=auto`。上线后先只读核对待发布筛选和全选数量，再由真实业务人员选择自然任务验收，不制造腾讯测试行；验收前不创建 `v0.19.24` 标签。

### 0.19.22 全链条“待登记”结果适配

- 全链条任务和在线表格把“待登记”作为正式核查结果；旧腾讯下拉缓存缺少该项时，平台保留既有选项 ID，并把业务缺失项同时补入页面选项和写回校验列表，避免再次出现“无效的下拉选项”。
- “待登记”按已完成任务统计，并纳入全链条日报的结果分类和核查见底口径；不增加新的日报列，也不修改其他业务类型的结果选项。
- 本版不新增数据库字段，但会影响同步写回和日报统计，发布使用 `backup_scope=all`、`release_scope=auto`。上线后检查全链条任务状态、结果保存、日报分类、健康接口和近期错误日志；不制造测试任务或腾讯写入，验收前不创建标签。

### 0.19.16 快捷调照片结果回到核查任务

- 本版不新增表或字段，也不复制照片文件。流口任务详情只投影通过快捷入口创建、精确关联当前任务、已经完成且含有效图片附件的照片工单。
- 图片预览和下载继续使用工单附件接口。上线后以真实已完成快捷工单检查：申请人打开对应任务可见照片并可下载；相同身份证的其他任务、手动工单和腾讯历史工单不应出现。
- “照片调取完成”个人提示点击后应先标记已读，再进入原核查任务；已读提示仍可再次点击。通知路径不得包含姓名、身份证号、手机号或地址。
- 发布不改数据库结构或数据写入逻辑，使用 `backup_scope=none`、`release_scope=auto`。上线后核对健康版本、任务详情附件、消息跳转及近期 403/500 日志；验收前不创建 `v0.19.16` 标签。

### 0.19.6 调照片名单完整核对重定位

- 发布前备份范围使用 `all`。本版不改表结构，但完整核对会在事务内更新 `WorkflowData.photo_sheet_rows` 的物理行定位与同步状态。
- 部署后等待或手动触发一次完整核对，确认运行成功、`last_full_sync_date` 更新为当天、没有新增重复工单，且五分钟增量同步继续推进游标。
- 若完整核对失败，事务会恢复原映射；保留增量同步，不要删除旧映射或重复执行历史导入。先检查同步运行的安全错误摘要和冲突列表。
- 回退程序不会自动反向修改已经成功重定位的行号；由于工单和腾讯正文均未删除或覆盖，可保留新映射并回退镜像。需要人工调整映射时必须先备份 `WorkflowData`。

### 0.19.5 调照片名单同步恢复与即时写回

- 本版不新增表或字段，但会创建照片工单、来源行映射和腾讯写回记录，正式发布使用 `backup_scope=all`，发布范围可用 `auto`。
- 发布前确认调照片名单读写开关和全局在线回写开关保持现有值，不通过关闭回写来部署。暂停普通在线数据自动同步只用于避免部署切换与长任务重叠；不修改真实腾讯行做测试。
- 上线后先只读核对健康版本、`photo_sheet_sources.last_cursor_row`、腾讯当前总行数、最近 `photo_sheet_sync_runs` 和现有 outbox 状态。随后等待或手动触发一次照片名单增量同步，确认游标推进、腾讯自然新增行形成工单且没有重复映射。
- 平台向腾讯的即时追加使用下一条自然产生的照片申请验收：本地工单应立即创建，腾讯暂时失败时只显示待同步并保留重试，不得删除或重新提交工单。每日完整核对即使超时也不应阻止之后的增量运行记录出现。
- 回退程序时恢复 `0.19.4` 镜像即可，已经创建的自然工单、映射和腾讯行不得删除或自动回滚；如发现重复外部行，先关闭照片名单写入、备份八库并按冲突记录人工核对。

### 0.19.2 发布包传输优化

- 发布工作流新增 `release_scope=auto/backend/full`。纯后端安全路径自动使用只含 `VERSION` 和 `backend/` 的精简包，
  遇到前端、Compose、Nginx、部署脚本或无法确认生产祖先提交时自动回退完整发布；手动指定后端范围遇到不安全路径会拒绝。
- 服务器固定入口增加只读 `status`，发布日志记录接收、候选镜像、回退归档和发布范围耗时；后端发布保留现有前端静态文件。
- 本版首次发布因同时修改发布入口和前端版本显示，必须走完整包。发布前需由 root 重新安装 `deploy/install-server-deployer.sh` 更新固定网关，
  再使用 GitHub `Deploy production`，`backup_scope=none`；后续纯后端修复可使用自动精简包。数据库不迁移、不写入腾讯数据。

### 0.19.3 前端精简发布

- 发布范围扩展为 `auto/backend/frontend/full`。生产祖先提交可验证且仅有前端运行代码变化时，自动生成只含 `VERSION` 和
  `frontend/dist` 的精简包；前后端同时变化或涉及基础设施、部署脚本和未知路径时仍安全回退完整发布。
- 前端精简发布跳过后端依赖安装、后端全量测试和后端镜像构建，复用当前 `binhu-backend:latest`；服务器只归档并替换版本号和
  前端产物，再重建后端与运维代理容器。健康检查失败会恢复旧版本号和旧前端，不自动恢复数据库。
- 本版自身修改了固定发布网关和服务器脚本，首次发布必须先由 root 重新安装固定入口，再通过 GitHub `Deploy production`
  使用 `release_scope=auto`、`backup_scope=none` 发布；由于部署脚本属于完整范围，本次会走完整包。后续真实纯前端版本再记录精简包大小和耗时。

### 0.19.4 工单恢复待领取与页面间距优化

- 管理员、超级管理员及已有工单管理权限的账号，可以把误退回到“待补充”的工单恢复为“待领取”。恢复时必须填写原因，系统会清空当前处理人、释放旧领取记录、恢复原岗位队列和处理时限，并保留退回与恢复事件。
- 普通退回补充也会同步释放当前领取记录，避免工单后来恢复队列后仍被旧领取关系占用。状态更新继续使用行锁和版本号校验，并通知申请人及恢复后的岗位队列。
- 工单中心与系统设置采用副开发提交的分节与间距优化。本版不修改数据库结构；由于会写入 `WorkflowData` 工单状态，正式发布使用 `backup_scope=all`、`release_scope=auto`。上线后打开现有待补充工单，由管理员通过页面执行恢复，不直接修改生产数据库。
### 0.19.25 走访与星级只读获取

- 生产服务器通过环境变量配置全民防地址及只读账号，认证信息不得写入仓库、普通日志、操作记录或 PR。
- 数据上传中心的“立即获取并预览”只生成预览；`VISIT_SOURCE_AUTO_CONFIRM=false` 时必须人工确认才会替换当前数据。
- 接口异常、认证失败、业务错误、空响应、重复分页或数量保护触发时保留旧快照，可继续使用原 XLSX 手动上传兜底。
- 首次发布涉及 VisitData 来源运行记录及快照元数据，使用 `backup_scope=all`、`release_scope=auto`；上线后先各执行一次最小日期范围预览，核对派出所范围、数量和字段，不自动确认。
### 0.19.26 走访与星级联合预览修复

- 修复“同一次预览的走访全部为新增时，星级只匹配当前数据库而显示全部未匹配”的口径错误；数据源仍为新星级评分管理。
- 发布不修改数据库结构，使用 `backup_scope=none / release_scope=auto`。上线后保留旧待确认运行，不自动确认；重新生成同日期范围预览，核对星级匹配数后再由用户决定是否替换。

### 0.19.27 照片批次上传限制修复

- 照片批次预览接口单独允许最多 201MB 的 HTTP 请求体，其中后端继续严格限制 ZIP 文件本身不超过 200MB；上传、代理和前端等待时间统一为 300 秒。
- 发布后把仓库中的 `nginx/migration/new-app-locations.conf` 安全更新到生产 `/etc/nginx/snippets/binhu-app-locations.conf`，执行 `nginx -t` 成功后再 reload，并保留更新前配置副本。
- 使用用户原 ZIP 重新预览前只做本地结构检查，不制造照片工单、不确认批次。该版本不修改数据库结构，使用 `backup_scope=none / release_scope=auto`。

### 0.19.28 流口任务视图与批量分配稳定性

- 流口任务电脑端新增账号级卡片/双层表格视图，在线数据查询只向管理员和超级管理员开放。
- 下发批量分配按每批 20 条执行，支持中断续传，并将成功、跳过、失败统计保持互斥。
- 本版为 `PlatformData._users` 幂等增加可空的 `task_display_mode` 字段，发布使用 `backup_scope=all / release_scope=auto`。
- 修复照片批次预览成功但确认阶段生成附件名时报错的问题；确认接口前端和 Nginx 超时统一为 300 秒。

### 0.19.30 流口任务表格直接编辑

- 表格视图一次批量读取当前页的字段权限、下拉选项和来源版本，第二行直接显示可填写控件；用户不再需要逐条点击“编辑本行”。
- 每条任务仍独立保存并执行既有腾讯来源版本、字段权限和社区范围校验；多来源或冲突任务只允许进入详情处理。
- 本版不修改数据库结构和腾讯写回协议，发布使用 `backup_scope=none / release_scope=auto`。上线后检查普通核查页、研判页、组员与组长字段差异、任务间分隔、单条保存及近期 403/409/500 日志；不为验收修改真实腾讯任务。

### 0.19.34 流口任务视图互斥修复

- 修正表格视图下仍继续显示卡片列表的问题。根因是通用卡片列表的 `display: grid` 覆盖了响应式隐藏工具类。
- 改用专用桌面隐藏样式：电脑端表格与卡片二选一，手机端继续保留卡片。
- 不修改接口、数据库、权限或腾讯回写逻辑。

### 0.19.33 流口任务来源标签与手机下拉修复

- 电脑端流口任务表格将来源按现有解析规则拆分为标签云，与卡片视图保持一致。
- 合并副开发 PR #225：手机端枚举下拉关闭搜索输入，点击核查结果等选项时不再唤起系统输入法；桌面端继续支持搜索。
- 不修改接口、数据库、权限或腾讯回写逻辑。

### 0.19.32 流口任务卡片边框对齐修复

- 修正横向滚动表格中 Ant Design 展开行固定容器自带内边距造成的错位；上下两层卡片现在使用相同左右边界。
- 仅调整电脑端流口任务表格样式，不修改接口、数据库、权限、腾讯回写或手机端卡片。

### 0.20.1 房屋档案大批量导入稳定性

- 户号表预览原先在解析约 51,936 行后逐条写入来源记录，浏览器会先触发 30 秒超时；告知书还需分页读取约 18,343 条，随后同样逐条写入，因此表现为长期转圈或代理超时。
- 新版将来源记录、问题记录、新建房屋、初始地址版本、来源关联和告知书挂载改为每批 500 条执行；户号表来源引用包含工作表名称和物理行号，避免多工作表文件的同行号互相影响。
- 前端及 Nginx 对 `/api/registry/imports/households/` 和 `/api/registry/imports/certificates/` 单独放宽为 300 秒，其他接口不变。真实文件只读解析仍应核对约 51,936 行、51,905 条正常、31 条问题、10 组重复和 44 条其他类型。
- 本版改变 `RegistryData` 导入写入路径但不修改表结构，发布使用 `backup_scope=all`、`release_scope=full`。上线后先重新执行户号表预览，确认数量一致后由项目管理人点击正式导入；完成房屋档案后再读取并挂载告知书。不要为验收制造业务数据，也不要在页面等待期间重复点击。

### 0.20.7 全民防辖区编码口径修正

- 生产单条只读预演已成功完成 TCP 登录、机构身份、唯一模型三任务和人员资料查询，但旧校验错误地要求任务 `xfsq` 与人员 `communityCode` 完全相等，因两套编码分别为 12 位和 10 位而被误判为辖区不一致。
- 新版改为使用登录机构和任务派出所执行硬范围校验，同时继续校验身份证号、姓名和任务唯一性；两个社区编码在受限页面分别显示并提示人工核对。
- 本版不修改数据库结构，也不调用全民防写接口。发布使用 `backup_scope=none / release_scope=auto`；上线后仍只用原单条授权任务复验照片读取，不执行登记或反馈。

### 0.21.1 全民防单入口与只读错误定位

- 本版只保留任务详情中的“全民防登记”入口，按钮和确认操作使用中性主色；后端仍自动执行完整登记前读取、来源复核、一次性准备和二次确认，不减少任何防重复或失败冻结保护。
- 只读 HTTP 错误增加步骤与状态码。审计只保存 `error_step`、`upstream_http_status`、内部来源 ID 和耗时，不保存上游正文或人员敏感信息。
- 2026-08-16 生产只读诊断确认失败样本为 `queryYysList=200`、`queryPeopleBySfzh=200`、`queryLocalPhoto=500`，诊断在照片步骤停止，未访问 `uploadPhoto`、`saveLocalPhoto`、`addPeople` 或 `fnmxCheck`。
- 本版不修改数据库结构和外部写入合同，发布使用 `backup_scope=none / release_scope=auto`。上线后用同一失败任务确认页面能明确提示居住证照片查询 HTTP 500；不得为了验收继续执行登记或制造测试数据。

### 0.21.10 全民防登记权限组控制

- 新增 `qmf.registration.execute` 权限，状态查询、准备、执行、运行恢复、腾讯标记重试和兼容预演接口统一使用该权限；验收后默认由基础管控、中队长、所队领导、管理员和超级管理员拥有。
- 超级管理员可在通用权限组页面把该权限明确授予其他账号，不再需要修改固定用户名或服务器环境变量。取消授权后，任务详情立即隐藏入口，后端接口继续返回 403。
- 本版不修改数据库表结构、全民防接口合同或外部写入顺序，状态预检测与执行前复核继续生效。后续发布建议使用 `backup_scope=none / release_scope=auto`，上线后先核对默认权限组和无权限账号隔离，再用单条任务验证只读状态，不为权限验收执行真实登记。

### 0.21.0 全民防模型三单条真实登记封闭测试

- 本版在只读预演基础上增加一次性准备记录、固定九步执行、失败冻结、最终状态复核和腾讯备注标记；初版采用单一测试账号，v0.21.10 起改为 `qmf.registration.execute` 权限控制，验收后默认由基础管控、中队长、所队领导、管理员和超级管理员拥有。
- 启动时在 `PlatformData` 幂等创建 `_qmf_registration_runs`。旧程序会忽略该表；程序回退时保留运行记录，不删除表，不把数据库回退解释为撤销已经发生的全民防外部写入。
- 本版已经合并并部署到生产。2026-08-16 首次单条“在吴”真实登记经平台运行表、全民防 APP 最终状态和腾讯备注共同确认成功；九步均成功且备注只追加一次。
- 首次真实验收只允许项目管理人选择一条明确授权的自然“在吴”任务，在准备页逐项核对并通过最终二次确认。执行期间不得刷新配置、并行预演、启动第二条任务或在旧客户端重复反馈。
- 任一步显示失败或不确定时立即停止后续验收，到全民防人工核对任务、人员和照片状态；不要再次执行、修改运行表或用数据库回退补偿外部写入。成功后再确认腾讯备注只追加一次，标记失败仅使用独立重试入口。
- 当前未创建 `v0.21.0` 标签。回退只恢复上一版程序和镜像；已经发生的全民防或腾讯写入必须按人工业务核对处理，不能自动撤销。

### 0.20.6 告知书后台读取与固定发布耗时拆分

- “读取告知书”改为持久化后台任务。页面立即得到任务编号，并显示当前页、已读取、通过范围校验和排除数量；刷新或离开页面不会中断服务器任务。
- 每页成功后保存断点。失败后“继续读取”会重新校验最后一页摘要，一致才继续；断点内容变化时只能“重新读取”，避免把两个时期的分页拼接。成功生成正式预览后删除临时分页正文。
- 启动时会把服务重启前遗留的等待中或运行中任务改成“已中断、可继续”，不会永久显示为运行中。旧版同步预览接口暂时保留兼容，但新页面不再使用五分钟长连接读取告知书。
- 固定服务器部署日志和历史记录新增九段耗时；GitHub 工作流另外输出发布包字节数及整个固定网关阶段耗时。本次只增加观察信息，不取消包校验、回退归档、数据库备份、候选镜像或健康检查。
- 本版新增 `RegistryData.registry_certificate_source_runs` 和 `registry_certificate_source_pages` 两张兼容表。旧程序会忽略它们；回退程序时保留表和失败断点，不删除正式预览批次。
- 因为同时修改应用、数据库兼容结构和固定服务器部署脚本，正式发布使用 `backup_scope=all / release_scope=full`。合并后必须先由 root 重新运行 `deploy/install-server-deployer.sh` 更新固定入口，再触发 GitHub `Deploy production`；否则 GitHub 仍会调用服务器上的旧部署脚本，看不到新增计时。
- 上线后先检查健康版本和两张新表，再发起一次真实只读告知书任务，观察分页进度、页面刷新恢复和最终预览；不要重复启动或确认挂载。随后核对 GitHub 日志及 `/var/lib/binhu-deploy/history/` 中各阶段耗时字段。验收前不创建版本标签。

### 0.19.31 流口任务表格卡片化

- 电脑端双层表格把每条任务调整为 12px 圆角分组卡片，移除粗横线和左侧竖线，增加柔和边框、轻阴影、组间留白及未保存/多选状态高亮。
- 本版只修改前端样式和版本记录，不改变接口、数据库、权限或腾讯写回协议。发布使用 `backup_scope=none / release_scope=auto`；上线后检查浅色、深色、长地址、状态标签、横向滚动和保存状态，验收前不创建标签。
