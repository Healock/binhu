# 四份中途任务接续导入台账

本台账记录一次性维护导入的代码和证据位置，不把工作簿正文或敏感字段写入 Git。所有 `8.31`、`9.2`、`9.5` 日期按 2026 年解释；文件中的截止日期原样保留，不因导入日期重新计算。文件核查人按第一核查人处理，文件社区作为当前社区；已有可靠第一社区记录时不覆盖。

## 运行与预览

- 运行编号：由服务器执行时生成，必须使用新的 `continuation-import-<timestamp>` 值。
- 工具：`python -m migrations.continuation_import`，默认只读；生产写入必须显式 `--apply`。
- 来源：`source_kind=one_time_continuation_import`，`source_ref=continuation:<run_id>:<file_sha256>:<sheet>:<physical_row>`。
- 目标：本地业务表、来源行、来源记录、投影和在线汇总更新输入；不创建任务发布批次，不访问腾讯文档。

## 文件摘要（不含正文）

| 业务类型 | 文件 SHA-256 | 预览有效行 | 需人工处理 |
| --- | --- | ---: | ---: |
| 疑似未注销模型三 | `3dfcd519fe3388d9d14887c713b75dd3f35b3b3bcf69cb946b09a59164dc46d1` | 294 | 1 |
| 全链条 | `a1d69b3c3686cd366184f44b37429e458d72e3a541ff68f5a14bc1835a0169b5` | 794 | 0 |
| 出租房屋核查 | `9fde2a983a17611857bbed317913dfa9bb66f9455279c059ecb1cba29a94ea7e` | 7 | 0 |
| 疑似返苏 | `4362966a36fae0bdcfd20dbe8496f5c9246fdec29e6171fdc93d8d1bf68dea4d` | 70 | 0 |

预览有效行因业务主键或社区缺失而少于文件业务行的记录必须进入服务器证据目录的异常清单；不得猜测身份或社区后写入正常任务。生产导入前须完成八库备份并复核预览哈希，任何事务错误整体回滚。

## 状态与后续验收

文件已有核查结果、研判和截止日期进入标准字段；自由文本研判不伪造结构化阶段。模型三不创建无法核实研判流程，其余三类按现有流程接管。导入后核对业务表、来源、投影数量，汇总更新队列排空，页面可回看已完成并继续处理未完成任务。真实 MySQL 预览/导入证据由服务器运行后补录。
