# 全链条与疑似返苏当前任务清理（2026-09-08）

用户在模型三清理完成后明确要求全链条和疑似返苏也清理。本次覆盖两类业务全部当前任务，不限定核查结果，保留既有归档、业务时间、历史审计、研判正文和其他业务。没有处理照片工单，没有改变运行代码或发布版本。

## 执行与证据

正式环境身份、Compose 项目 binhu、本地数据源启用和腾讯禁用均已核实。实时业务数量为全链条 925、疑似返苏 206；截图中的 920 不能替代完整库内核对。两类业务表、来源、active 本地登记逐条任务 ID、业务键及来源标识一致。

运行 `current-flow-cleanup-20260908a` 在严格归档插入时发现疑似返苏当前表与归档表的字段别名差异，整个事务回滚，Backend 恢复。回滚后数量仍为 925/206，归档仍为 7,065/310。错误为 MySQL 1054，不跳过字段或忽略错误。

依据 `SuspectReturnParser.DATABASE_COLUMN_ALIASES` 使用“身份证号码→身份证号”“二次核查结果→二次反馈”的既有映射。新运行 `current-flow-cleanup-20260908b` 重新停止 Backend、备份八库并验证 gzip、dump 完成标记与 SHA-256，再执行同一事务。逐字段归档内容摘要与原始业务行一致才允许清除当前业务。

| 对象 | 清理前 | 提交并重启后 |
| --- | ---: | ---: |
| 全链条当前业务/来源/投影/active 登记（各自） | 925 | 0 |
| 疑似返苏当前业务/来源/投影/active 登记（各自） | 206 | 0 |
| 全链条活动任务图节点 | 16 | 0 |
| 全链条活动研判流程 | 8 | 0 |
| 全链条归档 | 7,065 | 7,990 |
| 疑似返苏归档 | 310 | 516 |
| 模型三当前任务 | 0 | 0 |

当前来源和本地登记标记 archived 并递增 revision；保留来源内容。任务图节点标记 source_missing、设置归档时间并写维护事件，活动依赖取消，不伪造业务完成。8 条流程标记 archived 并递增 flow_version，保留旧状态的维护事件及历史研判正文，不伪造研判成功。既有归档和其他七类业务内容摘要前后保持一致。

同一事务写入 1,131 条带新 revision 的汇总更新输入，由现有 worker 处理来源移除；遵循现有日报规则保留已经计入的工作量，不清空历史日报。安全审计 ID 480，操作者为 authorized_maintenance，不冒充用户账号。

两次运行证据分别位于受限服务器目录 `/srv/binhu-release-evidence-current-flow-cleanup-20260908a` 与 `/srv/binhu-release-evidence-current-flow-cleanup-20260908b`。包含八库备份、脚本与哈希、前后摘要、受保护目标清单和执行日志；失败证据未覆盖。成功运行另保存 `post-restart-health.json`、`post-restart-counts.tsv` 及 SHA-256。任何正文和凭据不进入 Git。

## 验收与回退

Backend 已恢复，健康接口返回 0.28.11。重启后两类业务及关联当前计数均为 0，模型三仍为 0。这是真实授权维护，不是生产造数测试；无需重新部署、创建标签或修改数据库结构。

程序回退不会撤销数据维护。若需恢复，须另行授权、冻结相关写入，按备份和目标清单核对当前业务、来源、流程、任务图与汇总版本后恢复，不整库覆盖后续操作。新任务可通过正常导入继续创建。
