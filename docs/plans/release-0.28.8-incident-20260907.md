# 0.28.8 影子验证中止记录

日期：2026-09-07。

## 当前状态

发布分支为 `release/v0.28.8`，起点 `b5691e3f`。实现仍在工作区，尚未提交、合并、部署或创建发布标签。本次不得声明真实 MySQL 验证通过。

## 验证经过和证据损失

隔离项目为 `/srv/binhu-release-shadow-rls-20260907-a`，Compose 项目同名。MySQL 容器曾健康，未发布宿主端口。

1. runner 补充 `PYTHONPATH=/app` 后进入真实初始化，在 `run_local_source_migration → rebuild_projection → _address_match_entries` 失败：Registry 影子库缺少 `_police_address_entries`。
2. 将该项目的 `REGISTRY_ADDRESS_DOMAIN_ACTIVE` 关闭后重跑，初始化在重建投影时再次失败：Dispatch 影子库缺少 `_police_dispatch_publish_results`。
3. 主 Agent 错误执行清理，将日志复制到**待删除目录内部**的 `artifacts/failed-attempts`，随后运行该项目的 `docker compose -f compose.json down -v --remove-orphans` 并删除整个项目目录。日志、身份文件及隔离数据库卷因此丢失，不能恢复为完整验收证据。
4. 命令重新创建了空项目目录，随后试图从未经核实的 `/srv/binhu-release-shadow-template/backend` 复制源码，因目录不存在失败。

工具输出确认当前项目的 MySQL 容器、数据卷和隔离网络已删除，并报告模板复制失败。目录目前应为空目录，但尚未再次现场核实。没有执行针对正式目录或既有压测项目的删除命令；没有完成生产健康复查，不能由此推断生产健康状态。

可保留的证据仅为会话中的命令输出与以上安全摘要，**完整失败日志已经丢失**。不得把重建后的证据冒充本次原始记录。

## 停止条件与恢复要求

2026-09-07 用户已明确授权“作为经验记录下来……重新验证并继续”。恢复工作使用新运行号 `rls-20260907-b`，保留此次事故原文，不覆盖或伪造已丢失的证据。

依据用户“发现数据丢失或不可逆的状态污染立即停止并等待人工”的条件，已停止服务器操作。此次证据损失由主 Agent 的清理操作造成。

恢复执行前需用户明确指示。恢复时使用全新运行号，在任何删除操作前先把证据复制到独立留存位置并核对完整性；先复核初始化依赖顺序，不以关闭分域开关替代生产配置验证，不使用未经检查的模板目录。当前不能提交为已通过验收或触发生产发布。

## 其他待处理项

- 在线汇总仍需核实同日工作量上限、首次日报表生成、分域表路由、归档无当日流水、提交前 revision 校验及并发刷新；不能仅凭模拟测试认定完成。
- 后续继续真实汇总恒等式、WS/SSE、权限、页面与回滚验证。
- XLSX 清理和导入继续等待文件及维护窗口；dev 架构线不启用。
