# 场所码云端单窗口切换运行手册

版本：2026-09-15

本手册用于候选环境验收和正式切换。生产开关、二维码轮换、数据库写入和 Nginx 路由变更必须在前置核对完成并获得维护窗口批准后执行。云端不连接滨湖业务数据库，`RegistryData` 仍是场所和登记记录的唯一正式来源。

## 当前状态

- 最新主线已包含云端 Receiver、mTLS、Ed25519 签名、加密暂存、租约拉取、ACK、幂等和不确定状态保护。
- 自动化场所码专项、后端、前端和生产构建验证已完成；候选环境已完成一次仅使用虚构资料的端到端闭环，真实生产切换仍需现场核对和维护窗口。
- 生产初始开关必须保持：

```text
VENUE_CLOUD_SYNC_ENABLED=false
VENUE_CLOUD_PULL_ENABLED=false
VENUE_LOCAL_PUBLIC_ENTRY_ENABLED=true
```

### 候选环境核对记录（2026-09-14）

候选服务器的固定只读目标已经在本机受保护配置中登记，未写入仓库、PR
或测试夹具。候选 Compose 项目 `binhu-venue` 的 Receiver、Ingress 和专用
MySQL 均为 healthy，`/health/ready` 返回 200，Compose 配置只读解析通过。
当前候选运行提交为 `7390a10f3171670883b3377e40a3f79783d7861a`，本地内容寻址
镜像 ID 已记入服务器台账；Registry digest 尚未提供，不能把它当作正式制品
晋级证明。

候选 Nginx 语法、`/venue/`、`/api/public/` 和 mTLS 保护的
`/api/internal/` 路由已核对，既有 `/updates/` 路由保持不变。证书续签计时器
最近一次执行成功，候选台账记录的证书到期时间为 `2026-09-18T19:29:19Z`，
应在正式切换前再次确认续签结果。

候选云端 MySQL 只读统计为：启用场所 1 个，提交总数 0，`pending`、
`uncertain`、过期和 delivery event 均为 0。候选备份目录的数据库、照片、
Compose、Nginx、环境快照和台账文件已使用 `SHA256SUMS` 完成校验，服务器台账
记录备份引用和校验结果。台账只保存状态、数量、时间和哈希，不保存令牌、密钥
正文、照片或登记正文。

### 候选虚构闭环记录（2026-09-14）

候选环境使用临时测试场所、短时表单令牌、候选专用密钥和虚构姓名、证件号、手机号、地址及测试图片，完成了配置同步、公开登记页打开、一次性令牌发放、加密暂存、主动拉取、签名校验、正文和照片解密、本地临时库落一条、ACK `accepted` 以及相同 `submission_id` 幂等重投。重复投递后候选本地仍只有一条记录。

闭环完成后已删除测试场所、提交、表单令牌、内部测试请求、临时照片、临时 SQLite 和测试密钥目录；候选计数恢复为启用场所 1 个、提交 0 条、`pending` 0、`uncertain` 0、过期 0 和 delivery event 0。候选 Receiver、Ingress、MySQL 均健康，`/health/ready` 返回 200。脱敏报告和服务器台账位于候选受保护目录，不提交令牌、私钥、登记正文或照片。

原始闭环脚本没有保存每个步骤的独立时间戳，报告对提交、拉取、解密、落库和 ACK 时间明确标记为未记录，不能据此臆造时间。后续正式验收必须使用带时间戳的拉取端记录补齐这项证据。

### 生产只读核对记录（2026-09-15）

生产当前没有运行场所码 Receiver、Ingress 或专用 Compose MySQL；生产目录中仅保留部署源码。生产后端运行环境明确为：

```text
VENUE_CLOUD_SYNC_ENABLED=false
VENUE_CLOUD_PULL_ENABLED=false
VENUE_LOCAL_PUBLIC_ENTRY_ENABLED=true
```

生产容器中尚未挂载场所码 mTLS、签名、公钥或解密目录，`VENUE_CLOUD_BASE_URL` 和 `VENUE_PUBLIC_BASE_URL` 也未配置。生产版本为 `0.28.26`。生产 Nginx 虽然存在 `/etc/nginx/ssl/binhu/fullchain.pem`，但只读核对显示证书主题为 `CN=binhu-staging`，到期时间为 `2026-09-05T20:44:33Z`，当前已过期且身份不匹配，不能作为场所码公开登记入口证书。正式证书或受信任的 IP 证书、密钥分发、正式场所清单、现场打印安排、RegistryData 备份和云端配置同步都仍属于上线前置项，不能进入二维码替换或开关切换。

以上记录只说明候选闭环已用虚构数据验证，不能代表生产切换授权或生产上线完成。生产同步/主动拉取必须继续关闭，本地匿名入口继续开启。

本节记录的是候选观察证据，不代表生产切换授权或生产上线完成。

### 生产云端接收和主动拉取状态（2026-09-15）

在生产维护窗口完成了不产生登记正文的链路准备和空队列验收：云服务器
`47.100.44.36` 的 Receiver 已运行主线提交 `229f21a02abac6803910062751a8146d5510a8c8`，
镜像内容地址为 `sha256:7461c3deaf79c59304a185cc9a5046ee54814df855fd87718635170d7ff88e4f`，
Receiver、Ingress 和容器 MySQL 均为 healthy。生产物理服务器已配置新的 mTLS、Ed25519
和 RSA 密钥材料；密钥正文不进入仓库、日志或本手册，只在服务器受保护目录保存。

本地 `RegistryData` 备份和 token 清单保存在服务器受保护目录，清单权限为 `0600`，
备份 SHA-256 记录在生产部署台账。3 个 active 场所已经完成云端配置同步，云端 active
场所数为 3，`pending` 和 `uncertain` 均为 0；从物理服务器发起的 mTLS、请求签名、
响应验签状态查询成功，空队列长轮询正常返回 timeout。新 token 对应的 3 个公网页面均
返回 HTTP 200。

当前仍保持 `VENUE_LOCAL_PUBLIC_ENTRY_ENABLED=true`，正式二维码打印/替换和旧码回收
尚未开始；因此本节不表示已经完成单窗口切换。完成现场替换、业务方使用虚构或正式维护
窗口完成单场所闭环后，才可以关闭本地入口并验证旧码返回 HTTP 410。云端证书当前由
已有续签计时器管理，到期时间和续签结果必须在后续观察中持续核对。

## 一、只读前置核对

使用登记的持久 SSH MCP 候选目标，复用同一会话完成以下检查；不得把候选地址、端口、用户或密钥通过命令参数传入。MCP 不可用时，只允许使用一次受控只读连接并记录原因。

检查项目：

- 域名解析、HTTPS 证书有效期和现有续签计时器；
- mTLS CA、客户端证书、Ed25519 公钥/私钥和 RSA 公钥/私钥的存在、类型、权限和 key id；
- `/srv/binhu-venue`、`/etc/binhu-venue`、独立 Compose 项目和专用 Docker 网络；
- Receiver、Ingress、MySQL 健康状态和不可变镜像提交号；
- Receiver `/health/ready`、Nginx `/venue/` 路由和既有 `/updates/` 路由；
- 日志脱敏、照片目录权限、数据库目录权限和磁盘空间；
- `pending`、`uncertain`、过期和失败数量；
- 回滚镜像、Nginx 备份和部署台账；
- 试点场所、三批清单、打印负责人、替换顺序和旧码回收方式。

只记录状态、数量、时间、摘要和哈希，不记录密钥正文、证书私钥、令牌、登记正文、照片或完整地址。

## 二、备份和台账

生产开关变化前备份：

1. 滨湖 `RegistryData`；
2. 云端场所码 MySQL；
3. 云端照片目录；
4. 当前和上一 Receiver 镜像引用；
5. Nginx 配置和 Compose 环境快照；
6. 场所 token/config revision 对账结果。

每份备份记录 UTC 时间、路径、字节数、SHA-256、数据范围和恢复引用。建议使用 `deploy/venue-cloud/deployment-ledger.example.json` 作为字段模板；真实台账保存在服务器受保护目录，不提交 Git。

## 三、云端配置同步与单场所闭环

先执行只读 measure，确认启用场所数量和待轮换数量，再在批准的维护窗口执行 apply，最后执行 verify。新 token 输出文件权限必须是 `0600`，在单场所验收前不得打印或发放。

单场所闭环必须使用虚构登记资料和测试照片完成：

1. 本地场所配置同步到云端；
2. 新二维码打开云端页面并发放一次性 token；
3. 提交后云端加密保存；
4. 滨湖主动拉取、验签、解密并在本地事务写入一条记录；
5. ACK 后云端状态为 `accepted`；
6. 重复 `submission_id` 不产生第二条本地记录；
7. mTLS、伪造签名、过期 nonce、损坏密文、租约过期、ACK 不完整和响应丢失进入明确失败或 `uncertain`，不得自动重放。

闭环任一项失败，停止切换，不进入第一批。

## 四、三批切换和入口关闭

第一批为 5–10 个场所，观察 1–2 小时；第二批为 20–30 个场所，观察 2–4 小时；第三批为剩余全部场所。每批记录开始/结束时间、场所数量、同步结果、拉取结果、accepted/rejected/uncertain/expired 数量和回滚状态。

全部批次通过后，确认主动拉取稳定运行，再关闭 `VENUE_LOCAL_PUBLIC_ENTRY_ENABLED`。随后必须验证：新二维码继续打开云端页面，旧二维码返回 HTTP 410，虚构登记在本地只落一条记录。

## 五、回滚和 72 小时观察

出现生产异常、数据不一致、重复落库、版本漂移、签名/mTLS/解密失败、`uncertain` 无法解释增长、日志泄露正文或云端组件不健康时，立即停止后续批次：关闭主动拉取、保留云端队列、回退 Receiver 镜像或配置，并以 `cloud_submission_id` 对账。数据库不自动恢复，本地入口是否恢复必须单独确认。

入口关闭后连续观察 72 小时，至少检查 Receiver/Ingress/MySQL、pending/uncertain/expired、租约和 ACK、重复登记、场所版本漂移、日志脱敏、证书续签、磁盘和照片清理。观察期内保留回滚材料。

## 禁止事项

- 不恢复腾讯表、腾讯 OAuth、腾讯写回或腾讯物理行号；
- 不让云端连接滨湖业务数据库；
- 不把真实登记资料用于候选测试；
- 不在未完成前置核对时启用开关、打印新码、替换旧码或部署正式链路；
- 不自动恢复数据库、不自动重放 `uncertain`、不删除云端队列作为“清理”。
