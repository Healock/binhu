# 走访与星级自动获取待确认项

以下信息在 API 清单中未提供，当前代码使用可配置字段映射和脱敏模拟响应；真实配置未补齐时不会访问生产平台。

1. 两个明细接口的真实 JSON 响应结构、分页总数字段和业务日期字段。
2. 走访/星级字段与现有 XLSX 列的正式映射，尤其是进入方式、操作人账号、星级文本和隐患详情。
3. `deptCode=320584710000` 是否足以限定滨湖新城派出所，是否还必须发送 `pcsdm`。
4. 服务端认证方式、Token/Cookie 有效期和公共请求头；只需配置到服务器安全环境变量。
5. 空数据、权限不足、限流和数量异常的正式错误码。
6. 自动任务的建议执行时间、允许的最大记录数和数量波动阈值。

请补充脱敏响应或正式接口契约后，再开启 `VISIT_SOURCE_BASE_URL` 和认证配置；不要把认证值、原始响应或敏感字段写入日志、前端或公开 PR 正文。
