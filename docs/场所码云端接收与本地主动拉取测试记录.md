# 场所码云端接收与本地主动拉取测试记录

日期：2026-09-02
分支：`codex/venue-cloud-long-poll`
版本文件：`0.28.1`（未修改）

## 已完成

- 云端接收服务、移动登记页、加密暂存、幂等提交、租约拉取和确认接口已实现。
- 滨湖后端场所 Outbox、主动拉取 Worker、解密落库和不确定状态保护已实现。
- 管理页面已增加云端同步状态、待确认和队列提示。
- 云端部署单元、受限 SSH 发布网关、Compose 和 GitHub Actions 手动发布工作流已加入。
- 登记页为不支持 `crypto.randomUUID()` 的旧 WebView 增加标准 UUID 备用生成逻辑。
- 每日场所对账包含 `deleted` 状态，能够发现并修复删除 Outbox 丢失造成的云端漂移。
- 主业务 Worker 已增加受 mTLS 与 Ed25519 保护的 20 秒长轮询信号；信号响应经过严格结构校验，唤醒后拉取到队列为空，并保留每 5 分钟一次的兜底拉取。
- 云端提交只在数据库事务成功后发出进程内通知；通知丢失、Receiver 重启和多实例未共享通知时，等待超时后的数据库复查仍可发现队列。

## 本机验证

| 检查 | 结果 |
| --- | --- |
| 场所码、云端专项测试 | 46 passed |
| 部署合同测试 | 50 tests passed, 1 skipped |
| 后端完整测试 | 841 tests passed |
| 前端测试 | 293 passed |
| 前端生产构建 | 通过；仅有既有的大分块提示 |
| Python 编译与 OpenAPI 解析 | 通过 |
| 真实 MySQL 8.0.46 闭环 | 通过 |
| 临时数据库隔离 | 独立数据目录、`127.0.0.1:43306`，测试后已停止 |

真实 MySQL 闭环已使用虚构场所、虚构人员和测试图片验证：

- 场所配置同步和一次性表单令牌。
- AES-256-GCM 加密提交及照片密文落盘。
- 相同 `submission_id`、相同内容的重复提交幂等。
- 拉取租约、租约续期、`retry_later` 后重新领取。
- `accepted` 正常完成，`uncertain` 停止自动重放。
- 队列状态最终为待处理 0、不确定 1、有效场所 1。

真实数据库验证发现并修复了启动配置校验问题：`ENCRYPTION_PUBLIC_KEY_DIR` 原先被错误地按文件检查，正式容器会误报密钥目录缺失；现已改为目录检查并补回归测试。

## 只读环境检查

- 主业务服务器受限只读账号此前已验证可连接；本 PR 不记录或依赖其具体生产版本。
- 云服务器 443、SSH 端口可达，接收服务内部端口未暴露公网。
- 已通过受限 `binhu-venue-audit` 账号完成云服务器只读审计：系统为 Alibaba Cloud Linux 3.2104 U12，Docker 与 Nginx 均正常运行，根分区剩余约 24 GB。
- Docker 场所码 MySQL 不映射宿主机端口；宿主机既有 MySQL 仍监听 `*:3306`，其公网安全组入口已由项目方关闭，`firewalld` 保持未启用。
- Nginx 配置语法通过，当前没有重复 IP 站点告警；场所码路由与既有 `/updates/` 路由共用同一 HTTPS 站点。
- `/srv/binhu-venue` 和 `/etc/binhu-venue` 已作为独立候选环境部署路径，不与客户端更新目录或既有数据库共用数据目录。
- 现有 Win7、Win10/11 更新清单均返回 200，`Cache-Control: no-store`，未受本次开发影响。
- 公网 IP 证书可正常校验，SAN 包含服务器 IP，当前证书有效期至 2026-09-04 16:52:38 UTC。
- 云服务器 SSH 主机身份与既有指纹匹配，受限审计账号已可正常执行只读报告。

## 云服务器候选环境验收（2026-09-01）

- Docker Engine `29.7.2`、Compose `5.5.0` 已安装；镜像从可访问的 ECR Docker Official Images 按摘要固定拉取。
- 候选 Receiver 镜像已构建并核对导入：`binhu-venue-receiver:58fd0236f08ddf8a3b9a7549959e067102460c26`。
- Docker MySQL 使用 `mysql:8.0.46`，独立数据目录为 `/srv/binhu-venue/mysql`，没有宿主机端口映射；现有宝塔 MySQL 未修改。
- Receiver 只加入 `venue_private` 内部网络，没有宿主机端口映射；新增无密钥 Ingress 容器仅监听 `127.0.0.1:48727` 并固定转发至 Receiver。
- Receiver 运行时已核对只读根文件系统、`cap_drop=ALL`、`no-new-privileges`；Ingress 不挂载密钥、不接收数据库密码。
- `/health/ready`、Docker Compose 状态、MySQL 健康状态均通过。
- 使用虚构场所和测试照片完成端到端闭环：场所同步、公开登记、一次性令牌、AES-256-GCM/RSA-OAEP 加密、重复 `submission_id` 幂等、租约拉取、照片下载、解密和 `accepted` 确认均通过。
- Nginx 已在自动备份和可回退流程下激活场所码候选路由；宝塔实际日志目录使用 `/www/wwwlogs/`。
- 公网验收：`/updates/win10-x64/releases.stable.json` 返回 `200`；虚构 `/api/public/` 和 `/venue/` 返回 Receiver 的 `404`；无 mTLS 访问 `/api/internal/status` 返回 `403`；Nginx 语法通过且无重复 IP 站点告警。
- 当前仍未启用真实二维码、滨湖平台 Outbox 同步、主动拉取 Worker 和生产业务开关。

## 长轮询候选镜像验收（2026-09-02）

- 已从提交 `7390a10f3171670883b3377e40a3f79783d7861a` 构建并发布候选镜像 `binhu-venue-receiver:7390a10f3171670883b3377e40a3f79783d7861a`。
- 镜像 ID 为 `sha256:7461c3deaf79c59304a185cc9a5046ee54814df855fd87718635170d7ff88e4f`；发布用 `image.tar` SHA-256 为 `96e5d4d8cc5f188e8b81b2ff0eeb58d8149dde09056c613485fc7c2896d5da5d`。
- Docker Hub 元数据拉取曾超时，本次使用服务器上已核验的 `python:3.12-slim` 缓存基线继续构建；缓存镜像 ID 为 `sha256:ec7d6c95cd3692a2e2d228a8b1ca74e4025b54121fcc4c5da6f09cfa473315ad`，对应固定摘要 `sha256:2fe5997d249a808b8eeea52c58a1dbffbba28754dc11699ef5c029f2d818ce79`。
- 发布包通过受限 SSH 网关完成长度、SHA-256、提交号、镜像名和内部镜像哈希校验；新容器健康后才原子更新当前候选状态。
- `mysql`、`receiver`、`ingress` 三个容器均为 healthy；Receiver 与 Ingress 均运行上述新提交镜像，Ingress 仍只监听 `127.0.0.1:48727`，Docker MySQL 仍无宿主机端口映射。
- 使用只读预检工具完成公网 mTLS、Ed25519 请求签名和响应签名验证：`status` 验证通过，待处理队列为 0；1 秒 `wait` 正常超时并返回已验证签名。
- 无客户端证书访问 `/api/internal/status` 返回 `403`；Win7、Win10/11 与 Android 当前更新清单均继续返回 `200`。
- 本次只替换隔离候选 Receiver 镜像，没有开启真实场所同步、主动拉取或真实二维码，也没有修改主业务生产数据库。

## 尚未验证

以下项目仍需在获批维护窗口或正式切换前执行：

- Nginx 日志脱敏、证书续签和失败告警的长期运行检查。
- 云端与滨湖服务器的真实双向协议、密钥挂载和防火墙隔离。
- 外部手机扫码、7 天离线积压恢复、租约超时和照片清理。
- 生产备份、切换、回滚和 72 小时观察。

## 发布边界

- 云服务器仅部署隔离候选环境；主业务生产代码尚未部署。
- 本次候选镜像替换未修改生产数据库、Nginx 或证书配置。
- 分支提交和 PR 只包含代码、测试、部署合同与候选验收记录；不负责合并、版本发布或业务启用。
- 云端同步和主动拉取开关保持默认关闭，正式切换前需要人工批准。
