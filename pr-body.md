## PR 摘要

修复饮酒报备提交后刷新只显示“云端暂无待处理登记”、无法判断记录实际处理结果的问题。拉取接口保留 pulled 字段并增加 accepted、rejected、retry_later、uncertain 和安全 reason_codes；前端刷新与状态卡片按结果分类显示。版本提升到 0.29.7。

### 解决了哪些问题

- [x] 饮酒报备刷新只显示总数，无法区分已入库和处理失败。
- [x] 后台拉取与手动刷新共用队列时，已被后台租约消费的记录被误报为没有登记。
- [x] 补充拉取统计单元测试和帮助文档说明。

### 使用了什么方式

- 统一 pull_submissions_once、drain_submissions、wait_for_and_drain 和手动拉取接口的统计结构。
- 运行状态保存最近一次分类统计。
- 前端显示入库、拒绝、稍后重试和不确定结果。
- 不访问、不恢复腾讯链路，沿用现有 Receiver 主动拉取。
- 响应只返回数量和安全原因码，不返回业务正文。

### 数据与安全边界

- 是否修改数据库结构：否
- 是否修改生产数据：否
- 是否向腾讯或其他外部平台写入：否
- 是否新增权限或扩大数据范围：否
- 日志/审计是否可能包含业务正文：否
- 是否需要备份：none

### 建议怎么验收

1. 使用虚构报备资料提交一条饮酒报备。
2. 在二维码管理的饮酒报备页点击“刷新”。
3. 成功处理显示入库数量；暂时失败显示稍后重试及安全原因码；拒绝显示校验拒绝；后台已先处理时再次点击显示当前没有可处理登记，但列表仍可查询已入库记录。

### 测试与验证结果

- 后端测试：34 passed（venue_cloud_worker、venue_codes）
- Receiver 测试：32 passed
- 前端测试：341 passed
- 生产构建：通过（Vite）
- Python 编译：通过
- git diff --check：通过
- 真实 MySQL 验证：未验证，本机无真实 MySQL
- 真实腾讯或外部平台验证：未验证，腾讯业务链路已下线

### 风险、回滚与监控

- 主要风险：真实生产队列状态仍需部署后观察。
- 回滚方式：回滚应用代码至 0.29.6，不删除云端队列和本地记录。
- 上线后检查 POST /api/venue-cloud/pull、GET /api/venue-cloud/status、Receiver 队列状态和饮酒报备列表。

### 兼容性与发布信息

- 版本是否更新：是，目标版本：0.29.7
- 是否新增或修改环境变量：否
- 是否需要数据库备份：none
- 是否创建版本标签：否，待验收通过后按发布流程创建

### 本地数据源切换专用项

- 所属阶段：不涉及
- 是否读取腾讯数据：否
- 是否向腾讯写入、删除或触发同步：否
- 真实 MySQL 迁移验证：未验证

### 提交前检查清单

- [x] 已同步最新 main，并确认没有无关改动
- [x] 已补充或更新测试
- [x] 已检查权限、鉴权、敏感信息和日志边界
- [x] 已说明真实外部系统是否验证
- [x] 未在自动化测试中执行真实外部写入
