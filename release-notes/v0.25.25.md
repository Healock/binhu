# v0.25.25 更新日志

1. 管理员后台任务队列
- 管理员和超级管理员页面右下角新增后台任务悬浮按钮，可集中查看数据获取、在线表同步、任务发布、归档、导入、备份和回写队列。
- 任务抽屉区分运行中、排队中、需关注和最近结束状态，并显示安全的处理阶段、数量、进度和时间；切换页面后仍可继续查看。
- 后台队列只读取真实服务器任务状态，不展示人员资料、业务正文或外部系统返回内容，也不会因为刷新进度延长登录会话。

2. 侧边栏版本信息精简
- 去掉版本号前重复的“数据管理中心”文字，侧边栏现在只显示当前客户端版本，窄屏下更清爽。
