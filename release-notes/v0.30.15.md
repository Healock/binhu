# 滨湖智慧平台 v0.30.15

## 本次发布

- 归并外部只读统计中的社区正式名称与历史别名，避免别名被错误拆分为独立统计行。
- 保持本地业务数据为唯一业务来源；外部监控仍只作为只读观察和汇总输入。

## 验证边界

- PR #775 已合并到 `main`，对应 CI 全部通过。
- 本次版本需要通过正式 Production 发布工作流部署。
- 真实 MySQL、外部平台和真实客户端设备验收仍按上线清单分别核对。

发布分支校验：版本字段已由 sync_versions.py 同步。
