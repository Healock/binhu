# 滨湖智慧平台 v0.30.20

## 本次发布

- 修复离线居住证批量查询对 Excel 身份证列读取不完整时仍发起无效请求的问题。
- 批量查询现在先规范化并校验身份证号码；无效输入明确标记为 `invalid_identity`，不再误报为居住证平台业务失败。
- 统一在线登录探测、Win7 Electron、Win10 Tauri 与前端离线查询的机构代码解析，兼容嵌套响应中的 `orgCode`、`org_code` 和 `departCode`，并使用机构代码前六位查询。
- 更新离线模式帮助说明和架构文档，明确身份证列应按文本导入。

## 验证边界

- 前端离线专项测试：26/26 通过。
- 前端完整测试：390/390 通过；生产构建通过。
- 后端居住证专项测试：35/35 通过；Python 编译通过。
- Win7 Electron 语法检查、Tauri 格式检查和 `git diff --check` 通过。
- Tauri `cargo check` 未完成：本机缺少 Windows MSVC `link.exe`，不是代码诊断失败。
- 真实 MySQL、真实居住证平台和真实设备验收仍需按上线清单单独核对；本次没有外部写入或测试业务数据。
