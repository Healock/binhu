## PR 摘要

将根目录版本从 0.28.17 提升到 0.28.18，并同步前端、桌面端和移动端的版本元数据，作为已合并功能的统一发布版本。

### 解决了哪些问题

- 统一所有客户端和前端包版本，避免版本检查因 VERSION 变化而失败。
- 为已合并的自动保存、Univer 协作和环境账号管理功能提供一致的发布版本。

### 使用了什么方式

- 从根目录 VERSION 读取 0.28.18。
- 使用 desktop/scripts/sync_versions.py 同步桌面、移动端和前端包清单、安装器及 Tauri 配置版本。
- 不修改业务逻辑、数据库结构或生产数据。

### 数据与安全边界

- 是否修改数据库结构：否。
- 是否修改生产数据：否。
- 是否向腾讯或其他外部平台写入：否。
- 是否新增权限或扩大数据范围：否；权限相关代码来自已合并 PR #603。
- 日志/审计是否可能包含敏感业务正文：否。
- 是否需要备份：生产发布前按已合并功能的要求使用 OnlineData 备份门禁。

### 建议怎么验收

1. 前置条件：版本 PR 合并后主线 CI 通过，生产发布环境可用。
2. 操作步骤：检查根目录 VERSION、各端版本检查、生产健康接口和桌面客户端发布工作流。
3. 预期结果：所有版本为 0.28.18；主线 CI、生产部署和客户端发布均成功。
4. 失败或边界场景：任一版本不同步、构建失败或生产备份门禁失败时停止发布。

### 测试与验证结果

- 版本同步脚本：通过。
- desktop/scripts/sync_versions.py --check：通过。
- git diff --check：通过。
- GitHub Actions：版本 PR 将重新运行后端、前端、桌面端和热修补候选检查。
- 真实 MySQL 验证：不涉及本次版本文件变更；生产账号管理发布仍需按 #603 的 OnlineData 门禁核对。

### 风险、回滚与监控

- 主要风险：客户端包版本同步后需要完整客户端构建。
- 回滚方式：回退版本提交和对应发布制品，不自动回滚业务数据库。
- 上线后检查：健康接口版本、前端资源、桌面发布产物、容器状态和近期错误日志。

### 后续发展方向

- 将 0.28.18 作为 Dev、Staging 和 Production 的共同候选版本。
- 继续按 Dev → Staging → Production 晋级。

### 暂时无法解决的问题

- 生产服务器真实环境账号网关和 OnlineData 备份只能在受控发布流程中验证。

### 兼容性与发布信息

- 版本是否更新：是，0.28.18。
- 是否新增或修改环境变量：否；#603 的环境账号配置按其发布门禁处理。
- 是否需要数据库备份：需要，生产发布按 OnlineData 范围执行。
- 是否需要手动迁移或维护窗口：否，本 PR 不含数据库迁移。
- 是否建议立即合并：是，待本 PR 全部 CI 通过。
- 是否建议立即部署：是，合并后主线 CI 通过并完成 OnlineData 备份门禁后部署。
- 是否创建版本标签：部署和健康验收通过后创建 v0.28.18。
